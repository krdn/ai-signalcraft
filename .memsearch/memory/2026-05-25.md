## Session 00:02

### 00:01

<!-- session:d14d7ae0-337a-4d26-bfe0-cab0496ced1a turn:547bfb9a-3804-40dd-80e7-5732526caf50 transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/d14d7ae0-337a-4d26-bfe0-cab0496ced1a.jsonl -->

- User reported that job collection is stuck again (image showing #304 stopped at collection stage).
- Claude Code checked worker process status and found no active worker, but BullMQ queue had 2 zombie active jobs and 71 waiting jobs.
- Claude Code moved 2 zombie active jobs back to waiting queue, then started the worker using `nohup` with explicit ENCRYPTION_KEY to ensure it survives shell closure.
- Worker process started with PID 3410646 and began processing job #302 (keyword: 오세훈); confirmed via PostgreSQL that #302 and #303 show `partial_failure` status and #304 shows `running` status.
- Claude Code explained the root cause: previous `pnpm worker &` approach terminated when Claude Code session ended, but `nohup` keeps the process alive regardless of shell state; logs available at `/tmp/ais-worker.log`.

## Session 00:21

### 00:21

<!-- session:d14d7ae0-337a-4d26-bfe0-cab0496ced1a turn:b23c6d44-68cb-49b7-a6c5-f96a9ab34c50 transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/d14d7ae0-337a-4d26-bfe0-cab0496ced1a.jsonl -->

- 사용자가 opus JSON 파싱 실패 문제 수정을 요청했습니다.
- Claude Code는 kit과 insight-gateway 모두 dist 형태로만 제공되므로, core 레벨에서 `systemPrompt`에 JSON 리마인더를 추가하는 방식으로 해결했습니다.
- `runner.ts`의 `boundModule.buildSystemPrompt`와 `map-reduce.ts`의 Map/Reduce 단계에서 `systemPrompt` 끝에 `[CRITICAL] 반드시 순수 JSON 객체로만 응답하세요` 지시를 추가했습니다.
- 빌드 검증 후 커밋(`fix: opus JSON 파싱 강제 — systemPrompt에 JSON 리마인더 추가`)하고 운영 배포(Docker Compose) 완료했습니다.
- 이전에 failed 상태였던 #304 job을 DB에서 리셋하여 큐에 재등록했으며, 현재 활성 작업 10개 처리 후 대기 중인 99개 중 처리될 예정입니다.

## Session 00:26

### 00:26

<!-- session:d14d7ae0-337a-4d26-bfe0-cab0496ced1a turn:b3ddd4df-8390-4730-9456-717164ee720e transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/d14d7ae0-337a-4d26-bfe0-cab0496ced1a.jsonl -->

- 사용자가 job #305의 수집 진행 상태가 멈춰있다고 보고했습니다.
- Claude Code는 워커 프로세스가 종료되어 있음을 확인하고 좀비 active job 10개를 정리한 후 nohup으로 워커를 재시작했습니다.
- 대기 큐에 job #302가 107개 중복 등록되어 있음을 발견하고 110개 중복을 제거하여 #305만 남겼습니다.
- 워커 로그에서 job #302가 여전히 마크다운으로 응답받고 있음을 발견했으며, 이는 이전 코드로 시작된 워커 때문임을 파악했습니다.
- 워커 프로세스를 종료하고 JSON 리마인더가 적용된 새 코드로 재시작한 결과, `job=305, keyword=오세훈`이 새로 시작되었음을 확인했습니다.
- 20초 후 확인 결과 job #305의 Stage 1 분석(segmentation, macro-view)이 진행 중임을 확인했습니다.

## Session 00:48

### 00:48

<!-- session:d14d7ae0-337a-4d26-bfe0-cab0496ced1a turn:0b7ea1c1-910a-4885-9643-ab96637eb904 transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/d14d7ae0-337a-4d26-bfe0-cab0496ced1a.jsonl -->

- 사용자가 job #305의 현재 진행 상황과 문제를 확인해달라고 요청했습니다.
- Claude Code는 PostgreSQL 쿼리로 job #305의 모듈 상태를 확인해 segmentation/macro-view 완료, sentiment-framing/message-impact 실행 중임을 파악했습니다.
- 워커 로그에서 message-impact Map 청크에서 여전히 JSON 파싱 실패(`Unexpected token '#'`, 마크다운 응답)가 발생함을 발견했습니다.
- map-reduce.ts 수정: JSON_REMINDER를 함수 레벨 상수로 올리고, Map/Reduce 양쪽 user prompt 끝에도 `[CRITICAL]` JSON 지시를 추가했습니다.
- 빌드 검증 후 커밋(`fix: Map/Reduce user prompt 끝에도 JSON 리마인더 추가`)하고 운영 worker를 Docker Compose로 재배포했습니다.

## Session 07:22

### 07:22

<!-- session:d14d7ae0-337a-4d26-bfe0-cab0496ced1a turn:0b7ea1c1-910a-4885-9643-ab96637eb904 transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/d14d7ae0-337a-4d26-bfe0-cab0496ced1a.jsonl -->

- 사용자가 job #305의 AI 분석 모듈에서 Stage 2~4가 JSON 파싱 실패(마크다운 반환)하는 문제를 보고했습니다.
- Claude Code는 실패 원인을 분석하여 kit의 `analyzeStructuredViaText`가 system prompt를 재조합하면서 이전에 추가한 JSON 리마인더가 중간에 묻혀 효과가 없음을 파악했습니다.
- Claude Code는 `runner.ts`의 `buildPrompt`와 `buildPromptWithContext` 메서드에도 JSON 리마인더를 추가하여, user prompt 끝에 `[CRITICAL]` 지시를 배치했습니다.
- `crisis-scenario.schema.ts`의 `currentRiskLevel` 필드에 `.catch('medium')`을 추가하여 필수 필드 검증 실패를 방지했습니다.
- 빌드 검증 후 커밋(`fix: buildPrompt 끝에도 JSON 리마인더 추가 + crisis-scenario 스키마 방어`)하고 Docker Compose로 운영 worker와 web 컨테이너를 재배포했습니다.

## Session 08:08

### 08:08

<!-- session:d14d7ae0-337a-4d26-bfe0-cab0496ced1a turn:0b7ea1c1-910a-4885-9643-ab96637eb904 transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/d14d7ae0-337a-4d26-bfe0-cab0496ced1a.jsonl -->

- 사용자가 job #306의 3개 실패한 분석 모듈(risk-map, final-summary, approval-rating)의 JSON 스키마 검증 실패를 수정해달라고 요청했습니다.
- Claude Code는 PostgreSQL 쿼리로 각 모듈의 구체적 실패 이유를 확인했습니다: risk-map은 riskTrend enum 값 오류, final-summary는 currentState 필드 누락, approval-rating은 estimatedRange와 confidence 필드 누락.
- Claude Code는 3개 스키마 파일을 읽어 `.catch()` 폴백이 없는 필드를 식별했습니다.
- 3개 스키마 파일(risk-map.schema.ts, final-summary.schema.ts, approval-rating.schema.ts)을 수정하여 required 필드들에 `.catch()` 폴백을 추가했습니다.
- 빌드 검증 후 커밋(`fix: risk-map, final-summary, approval-rating 스키마 .catch() 폴백 추가`)을 생성했고, Docker Compose로 운영 환경(web, worker)을 재배포했습니다.

## Session 09:47

### 09:47

<!-- session:d14d7ae0-337a-4d26-bfe0-cab0496ced1a turn:0b7ea1c1-910a-4885-9643-ab96637eb904 transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/d14d7ae0-337a-4d26-bfe0-cab0496ced1a.jsonl -->

- 사용자가 job #307 분석의 win-simulation 모듈만 JSON 스키마 검증 실패를 수정해달라고 요청했습니다.
- Claude Code는 PostgreSQL 쿼리로 에러 메시지를 확인하여 `winProbability`가 object를 받았고 `confidenceLevel`이 Required임을 파악했습니다.
- `win-simulation.schema.ts`를 수정하여 `winProbability`에 preprocess 로직 추가(object→number 변환), `confidenceLevel`과 `simulationSummary`에 `.catch()` 폴백 추가했습니다.
- 빌드 검증 완료 후 커밋(`fix: win-simulation 스키마 수정`)하고 Docker Compose로 web과 worker를 재배포했습니다.

## Session 10:12

## Session 10:18

## Session 10:20

### 10:20

<!-- session:03c547de-77c7-4d14-8f25-0e27bcc13af3 turn:e9cb5f04-db2b-4cb9-85cb-1d8b8735b7cd transcript:/home/gon/.claude/projects/-home-gon-projects-ai-ai-signalcraft/03c547de-77c7-4d14-8f25-0e27bcc13af3.jsonl -->

- 사용자가 8개 프로젝트에서 산재되어 있는 LLM proxy 구현을 프로젝트화하여 재사용 가능하도록 분리하는 최선의 방식을 묻기 위해 심층 분석을 요청했습니다.
- Claude Code가 탐색 에이전트 2개를 병렬로 백그라운드에서 실행하여 (1) AI SignalCraft 내 LLM gateway 코드 구조, (2) 다른 프로젝트들의 LLM 사용 패턴을 조사했습니다.
- 분석 결과 8개 프로젝트가 3가지 패턴(git 의존성, 자체 구현, 직접 SDK)으로 LLM을 사용 중이며, 공통으로 10개 프로바이더, Structured Output, 리트라이 등의 기능이 이미 구현되어 있음을 발견했습니다.
- NPM 패키지, 독립 API 서버, MCP Server, 하이브리드 방식 4가지를 장단점 비교표로 평가한 후, **방식 4(하이브리드: NPM 패키지 우선 + 프록시는 선택적)**를 추천했습니다.
- 추천 근거: TypeScript/Node.js 프로젝트만 있어서 NPM 패키지면 충분하고, MCP는 LLM 라우팅용이 아니며, 프록시는 키 중앙화 필요할 때 나중에 추가 가능하다는 실용성을 제시했습니다.
- 구체적 3단계 실행 계획(NPM 패키지 추출 → 다른 프로젝트 마이그레이션 → 프록시 서버 추가)을 제시하고 사용자의 의사 결정을 대기했습니다.
