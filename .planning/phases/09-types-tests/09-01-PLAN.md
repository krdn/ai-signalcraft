---
phase: 09-types-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types/analysis.ts
  - packages/core/src/types/report.ts
  - packages/core/src/types/pipeline.ts
  - packages/core/src/types/index.ts
  - packages/core/src/analysis/types.ts
  - packages/core/src/analysis/model-config.ts
  - packages/core/src/analysis/provider-keys.ts
  - packages/core/src/report/generator.ts
  - packages/core/src/report/pdf-exporter.ts
  - packages/core/src/pipeline/normalize.ts
  - packages/core/src/queue/worker-config.ts
autonomous: true
requirements: [TYPE-01, TYPE-04]

must_haves:
  truths:
    - 'ModuleModelConfig, ProviderKeyInfo, ReportGenerationInput, PdfExportOptions, CommunitySource 타입이 각 패키지의 types/ 디렉토리에 정의되어 있다'
    - 'AIProvider 타입이 ai-gateway에서 단일 정의되고 core가 re-export한다'
    - '외부 import 경로(@ai-signalcraft/core)에서 모든 타입이 여전히 접근 가능하다'
    - '기존 테스트 96개가 모두 통과한다 (사전 실패 6개 제외)'
  artifacts:
    - path: 'packages/core/src/types/analysis.ts'
      provides: 'ModuleModelConfig, ProviderKeyInfo 타입 정의'
      contains: 'export interface ModuleModelConfig'
    - path: 'packages/core/src/types/report.ts'
      provides: 'ReportGenerationInput, PdfExportOptions 타입 정의'
      contains: 'export interface ReportGenerationInput'
    - path: 'packages/core/src/types/pipeline.ts'
      provides: 'CommunitySource 타입 정의'
      contains: 'export type CommunitySource'
  key_links:
    - from: 'packages/core/src/analysis/model-config.ts'
      to: 'packages/core/src/types/analysis.ts'
      via: "import type { ModuleModelConfig } from '../types/analysis'"
      pattern: 'import.*ModuleModelConfig.*from.*types'
    - from: 'packages/core/src/analysis/types.ts'
      to: 'packages/ai-gateway/src/gateway.ts'
      via: "export type { AIProvider } from '@ai-signalcraft/ai-gateway'"
      pattern: 'export type.*AIProvider.*from.*ai-gateway'
---

<objective>
core 패키지의 5곳에 분산된 인라인 타입 정의를 types/ 디렉토리로 중앙화하고, AIProvider 중복을 제거하여 ai-gateway에서 단일 정의하도록 한다.

Purpose: 타입 검색과 관리를 한 곳으로 집중하고, import 경로를 통일하여 코드 유지보수성을 높인다.
Output: core/src/types/{analysis,report,pipeline}.ts 생성, 원본 파일에서 인라인 타입 제거, barrel export 체인 유지
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-types-tests/09-CONTEXT.md
@.planning/phases/09-types-tests/09-RESEARCH.md

<interfaces>
<!-- core/src/analysis/types.ts 에서 AIProvider + ProviderType 정의 (re-export로 교체 대상) -->
export type AIProvider = 'anthropic' | 'openai' | 'gemini' | 'ollama' | 'deepseek' | 'xai' | 'openrouter' | 'custom';
export type ProviderType = AIProvider;

<!-- ai-gateway/src/gateway.ts 에서 AIProvider 정의 (단일 소스로 유지) -->

export type AIProvider = 'anthropic' | 'openai' | 'gemini' | 'ollama' | 'deepseek' | 'xai' | 'openrouter' | 'custom';

<!-- ai-gateway/src/index.ts 에서 AIProvider export -->

export { analyzeText, analyzeStructured, type AIProvider, type AIGatewayOptions } from './gateway';

<!-- core/src/types/index.ts 현재 구조 -->

export const CollectionTriggerSchema = z.object({...});
export type CollectionTrigger = z.infer<typeof CollectionTriggerSchema>;
export type SourceStatus = {...};
export type JobProgress = Record<string, SourceStatus>;

<!-- core/src/analysis/index.ts barrel export -->

export _ from './types';
export _ from './data-loader';
export _ from './persist-analysis';
export _ from './modules';
export _ from './runner';
export _ from './pipeline-orchestrator';
export _ from './item-analyzer';
export _ from './schemas';
export _ from './model-config';
export _ from './provider-keys';
export \* from './provider-test';

<!-- core/src/index.ts barrel export -->

export { db, getDb } from './db';
export type { Database } from './db';
export _ from './db/schema';
export { triggerCollection, createCollectorWorker, createPipelineWorker, redisConnection } from './queue';
export _ from './types';
export _ from './pipeline';
export _ from './analysis';
export _ from './report';
export _ from './utils';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types/ 파일 생성 + 원본에서 타입 제거 + AIProvider re-export</name>
  <files>
    packages/core/src/types/analysis.ts,
    packages/core/src/types/report.ts,
    packages/core/src/types/pipeline.ts,
    packages/core/src/types/index.ts,
    packages/core/src/analysis/types.ts,
    packages/core/src/analysis/model-config.ts,
    packages/core/src/analysis/provider-keys.ts,
    packages/core/src/report/generator.ts,
    packages/core/src/report/pdf-exporter.ts,
    packages/core/src/pipeline/normalize.ts,
    packages/core/src/queue/worker-config.ts
  </files>
  <read_first>
    packages/core/src/types/index.ts,
    packages/core/src/analysis/types.ts,
    packages/core/src/analysis/model-config.ts,
    packages/core/src/analysis/provider-keys.ts,
    packages/core/src/report/generator.ts,
    packages/core/src/report/pdf-exporter.ts,
    packages/core/src/pipeline/normalize.ts,
    packages/core/src/queue/worker-config.ts,
    packages/ai-gateway/src/index.ts,
    packages/ai-gateway/src/gateway.ts
  </read_first>
  <action>
**Step 1: 새 타입 파일 3개 생성**

`packages/core/src/types/analysis.ts` (신규):

```typescript
import type { AIProvider } from '@ai-signalcraft/ai-gateway';

// 모듈별 AI 모델 설정 (per D-02, from model-config.ts)
export interface ModuleModelConfig {
  provider: AIProvider;
  model: string;
  baseUrl?: string;
  apiKey?: string;
}

// 프로바이더 키 정보 (per D-02, from provider-keys.ts)
export interface ProviderKeyInfo {
  id: number;
  providerName: string;
  providerType: string;
  name: string;
  maskedKey: string | null;
  baseUrl: string | null;
  selectedModel: string | null;
  availableModels: string[] | null;
  isActive: boolean;
  createdAt: Date;
  updatedAt: Date;
}
```

`packages/core/src/types/report.ts` (신규):

```typescript
import type { AnalysisModuleResult } from '../analysis/types';

// 리포트 생성 입력 (per D-02, from generator.ts)
export interface ReportGenerationInput {
  jobId: number;
  keyword: string;
  dateRange: { start: Date; end: Date };
  results: Record<string, AnalysisModuleResult>;
  completedModules: string[];
  failedModules: string[];
}

// PDF 내보내기 옵션 (per D-02, from pdf-exporter.ts)
export interface PdfExportOptions {
  title?: string;
  format?: 'A4' | 'Letter';
  margin?: { top?: string; bottom?: string; left?: string; right?: string };
}
```

`packages/core/src/types/pipeline.ts` (신규):

```typescript
// 커뮤니티 소스 타입 (per D-02, from normalize.ts)
export type CommunitySource = 'dcinside' | 'fmkorea' | 'clien';
```

**Step 2: types/index.ts 업데이트** -- 기존 내용 유지 + 새 파일 re-export 추가

`packages/core/src/types/index.ts` 파일 끝에 3줄 추가:

```typescript
export * from './analysis';
export * from './report';
export * from './pipeline';
```

**Step 3: AIProvider 중복 제거 (per D-04)**

`packages/core/src/analysis/types.ts`에서:

- `export type AIProvider = '...'` 라인을 삭제
- `export type ProviderType = AIProvider` 라인을 삭제
- 대신 다음 2줄 추가 (파일 최상단, import { z } from 'zod' 다음):

```typescript
export type { AIProvider } from '@ai-signalcraft/ai-gateway';
export type ProviderType = import('@ai-signalcraft/ai-gateway').AIProvider;
```

주의: ProviderType는 re-export 형태가 아니라 type alias이므로 위와 같이 작성하거나, 아래 방식 사용:

```typescript
import type { AIProvider } from '@ai-signalcraft/ai-gateway';
export type { AIProvider };
export type ProviderType = AIProvider;
```

**Step 4: 원본 파일에서 인라인 타입 제거 + import 변경**

`packages/core/src/analysis/model-config.ts`:

- `export interface ModuleModelConfig { ... }` 블록 (9~14줄) 삭제
- 기존 `import type { AIProvider } from './types';` 유지 (types.ts가 이제 ai-gateway에서 re-export)
- 파일 상단에 추가: `import type { ModuleModelConfig } from '../types/analysis';`
- `export { type ModuleModelConfig } from '../types/analysis';` 추가하여 기존 import 경로 호환 (model-config.ts에서 직접 import하던 코드 대응)

`packages/core/src/analysis/provider-keys.ts`:

- `export interface ProviderKeyInfo { ... }` 블록 (8~20줄) 삭제
- 파일 상단에 추가: `import type { ProviderKeyInfo } from '../types/analysis';`
- `export type { ProviderKeyInfo } from '../types/analysis';` 추가

`packages/core/src/report/generator.ts`:

- `export interface ReportGenerationInput { ... }` 블록 (7~14줄) 삭제
- 파일 상단에 추가: `import type { ReportGenerationInput } from '../types/report';`
- `export type { ReportGenerationInput } from '../types/report';` 추가

`packages/core/src/report/pdf-exporter.ts`:

- `export interface PdfExportOptions { ... }` 블록 (4~8줄) 삭제
- 파일 상단에 추가: `import type { PdfExportOptions } from '../types/report';`
- `export type { PdfExportOptions } from '../types/report';` 추가

`packages/core/src/pipeline/normalize.ts`:

- `export type CommunitySource = 'dcinside' | 'fmkorea' | 'clien';` 라인 삭제
- 파일 상단에 추가: `import type { CommunitySource } from '../types/pipeline';`
- `export type { CommunitySource } from '../types/pipeline';` 추가

`packages/core/src/queue/worker-config.ts`:

- `import type { CommunitySource } from '../pipeline/normalize';` 를 `import type { CommunitySource } from '../types/pipeline';` 로 변경 (또는 기존 경로 유지 -- normalize.ts가 re-export하므로 둘 다 동작)
  </action>
  <verify>
  <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria> - packages/core/src/types/analysis.ts 파일이 존재하며 `export interface ModuleModelConfig` 포함 - packages/core/src/types/analysis.ts 파일에 `export interface ProviderKeyInfo` 포함 - packages/core/src/types/report.ts 파일이 존재하며 `export interface ReportGenerationInput` 포함 - packages/core/src/types/report.ts 파일에 `export interface PdfExportOptions` 포함 - packages/core/src/types/pipeline.ts 파일이 존재하며 `export type CommunitySource` 포함 - packages/core/src/types/index.ts에 `export * from './analysis'` 포함 - packages/core/src/types/index.ts에 `export * from './report'` 포함 - packages/core/src/types/index.ts에 `export * from './pipeline'` 포함 - packages/core/src/analysis/types.ts에 AIProvider 인라인 정의가 없고 ai-gateway에서 re-export - packages/core/src/analysis/model-config.ts에 `interface ModuleModelConfig` 인라인 정의가 없음 - packages/core/src/analysis/provider-keys.ts에 `interface ProviderKeyInfo` 인라인 정의가 없음 - packages/core/src/report/generator.ts에 `interface ReportGenerationInput` 인라인 정의가 없음 - packages/core/src/report/pdf-exporter.ts에 `interface PdfExportOptions` 인라인 정의가 없음 - packages/core/src/pipeline/normalize.ts에 `export type CommunitySource =` 인라인 정의가 없음 - `tsc --noEmit` 명령이 에러 없이 통과
  </acceptance_criteria>
  <done>5개 인라인 타입이 types/ 디렉토리로 이동되고, AIProvider가 ai-gateway에서 re-export되며, TypeScript 컴파일이 에러 없이 통과한다</done>
  </task>

<task type="auto">
  <name>Task 2: 전체 빌드 및 테스트 검증 (TYPE-04)</name>
  <files>
    (검증 전용 -- 파일 수정 없음, Task 1 결과 확인)
  </files>
  <read_first>
    packages/core/src/types/index.ts,
    packages/core/src/analysis/index.ts,
    packages/core/src/index.ts
  </read_first>
  <action>
빌드와 테스트를 실행하여 타입 이동이 기존 기능을 깨뜨리지 않았는지 확인한다.

1. `pnpm -r build` 실행하여 모든 패키지 TypeScript 컴파일 통과 확인
2. `pnpm --filter @ai-signalcraft/core test` 실행하여 core 패키지 테스트 통과 확인
3. `pnpm --filter @ai-signalcraft/collectors test` 실행하여 collectors 패키지 테스트 통과 확인

사전 실패 6개 (analysis-runner.test.ts 4개 + report.test.ts 2개)는 DB mock 부재로 Phase 9 범위 밖이므로 무시한다. 그 외 96개 테스트가 모두 통과해야 한다.

만약 barrel export 체인 문제로 `Module has no exported member` 에러가 발생하면:

- core/src/analysis/index.ts의 `export * from './types'` 가 analysis/types.ts의 re-export를 올바르게 전달하는지 확인
- core/src/types/index.ts의 새 re-export가 core/src/index.ts를 통해 전달되는지 확인
  </action>
  <verify>
  <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria> - `pnpm -r build` 명령이 에러 없이 완료 (exit code 0) - `pnpm --filter @ai-signalcraft/core test` 결과에서 사전 실패 6개를 제외한 96개 테스트 통과 - `pnpm --filter @ai-signalcraft/collectors test` 결과에서 49개 테스트 모두 통과
  </acceptance_criteria>
  <done>타입 이동 후 빌드와 테스트가 모두 통과하여 기존 기능 호환성이 확인된다</done>
  </task>

</tasks>

<verification>
- `pnpm -r build` 성공 (TypeScript 컴파일 에러 없음)
- `pnpm --filter @ai-signalcraft/core test` -- 사전 실패 6개 제외 96개 통과
- `pnpm --filter @ai-signalcraft/collectors test` -- 49개 통과
- `grep -r "export interface ModuleModelConfig" packages/core/src/types/` -- types/ 디렉토리에 존재
- `grep -r "AIProvider.*=.*'" packages/core/src/analysis/types.ts` -- 인라인 정의 없음 (re-export만)
</verification>

<success_criteria>

1. 5개 인라인 타입이 core/src/types/{analysis,report,pipeline}.ts로 이동됨
2. AIProvider가 ai-gateway에서 단일 정의되고 core에서 re-export됨
3. 외부 import 경로(@ai-signalcraft/core)에서 모든 타입이 여전히 접근 가능
4. 빌드와 테스트가 모두 통과 (사전 실패 6개 제외)
   </success_criteria>

<output>
After completion, create `.planning/phases/09-types-tests/09-01-SUMMARY.md`
</output>
