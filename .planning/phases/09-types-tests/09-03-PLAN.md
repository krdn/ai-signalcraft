---
phase: 09-types-tests
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - packages/core/tests/advn-schema.test.ts
  - packages/core/tests/advn-approval-rating.test.ts
  - packages/core/tests/advn-frame-war.test.ts
  - packages/core/tests/advn-crisis-scenario.test.ts
  - packages/core/tests/advn-win-simulation.test.ts
  - packages/core/tests/advn-exports.test.ts
autonomous: true
requirements: [TYPE-03, TYPE-04]

must_haves:
  truths:
    - 'advn-schema.test.ts가 삭제되고 5개 파일로 분할되어 있다'
    - '각 분할 파일이 300줄 이하이다'
    - '분할된 5개 파일의 모든 테스트가 통과한다'
    - '모든 패키지의 기존 테스트가 통과한다 (사전 실패 6개 제외)'
  artifacts:
    - path: 'packages/core/tests/advn-approval-rating.test.ts'
      provides: 'ADVN-01 ApprovalRatingSchema 테스트'
      contains: "describe('ADVN-01: ApprovalRatingSchema'"
    - path: 'packages/core/tests/advn-frame-war.test.ts'
      provides: 'ADVN-02 FrameWarSchema 테스트'
      contains: "describe('ADVN-02: FrameWarSchema'"
    - path: 'packages/core/tests/advn-crisis-scenario.test.ts'
      provides: 'ADVN-03 CrisisScenarioSchema 테스트'
      contains: "describe('ADVN-03: CrisisScenarioSchema'"
    - path: 'packages/core/tests/advn-win-simulation.test.ts'
      provides: 'ADVN-04 WinSimulationSchema 테스트'
      contains: "describe('ADVN-04: WinSimulationSchema'"
    - path: 'packages/core/tests/advn-exports.test.ts'
      provides: 'ADVN 모듈 export 확인 테스트'
      contains: "describe('ADVN 모듈 export 확인'"
  key_links:
    - from: 'packages/core/tests/advn-*.test.ts'
      to: 'packages/core/src/analysis/schemas/'
      via: 'dynamic import'
      pattern: "await import\\("
---

<objective>
300줄인 advn-schema.test.ts를 5개 describe 블록 단위로 분할하고, 전체 패키지 테스트를 실행하여 모든 리팩토링의 최종 검증을 수행한다.

Purpose: 테스트 파일을 관리 가능한 크기로 분할하고 Phase 9 전체 작업의 최종 검증을 완료한다.
Output: 5개 분할 테스트 파일, advn-schema.test.ts 삭제, 전체 테스트 통과
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-types-tests/09-CONTEXT.md
@.planning/phases/09-types-tests/09-RESEARCH.md
@.planning/phases/09-types-tests/09-01-SUMMARY.md
@.planning/phases/09-types-tests/09-02-SUMMARY.md

<interfaces>
<!-- advn-schema.test.ts 현재 구조 (300줄, 5개 describe 블록) -->
import { describe, it, expect } from 'vitest';
import { ZodError } from 'zod';

describe('ADVN-01: ApprovalRatingSchema', () => { ... }); // 줄 4-75 (72줄)
describe('ADVN-02: FrameWarSchema', () => { ... }); // 줄 77-126 (50줄)
describe('ADVN-03: CrisisScenarioSchema', () => { ... }); // 줄 128-186 (59줄)
describe('ADVN-04: WinSimulationSchema', () => { ... }); // 줄 188-263 (76줄)
describe('ADVN 모듈 export 확인', () => { ... }); // 줄 265-300 (36줄)

<!-- 각 describe는 dynamic import 사용 -->

const { ApprovalRatingSchema } = await import('../src/analysis/schemas/approval-rating.schema');
const { FrameWarSchema } = await import('../src/analysis/schemas/frame-war.schema');
const { CrisisScenarioSchema } = await import('../src/analysis/schemas/crisis-scenario.schema');
const { WinSimulationSchema } = await import('../src/analysis/schemas/win-simulation.schema');

<!-- 공통 import -->

import { describe, it, expect } from 'vitest';
import { ZodError } from 'zod';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: advn-schema.test.ts를 5개 파일로 분할 (per D-08, D-09)</name>
  <files>
    packages/core/tests/advn-schema.test.ts,
    packages/core/tests/advn-approval-rating.test.ts,
    packages/core/tests/advn-frame-war.test.ts,
    packages/core/tests/advn-crisis-scenario.test.ts,
    packages/core/tests/advn-win-simulation.test.ts,
    packages/core/tests/advn-exports.test.ts
  </files>
  <read_first>
    packages/core/tests/advn-schema.test.ts
  </read_first>
  <action>
advn-schema.test.ts의 5개 describe 블록을 각각 별도 파일로 분할한다 (per D-09: 모듈/describe 블록 단위 분할).

**분할 매핑:**

| 원본 줄 범위 | 새 파일                      | 내용                                        |
| ------------ | ---------------------------- | ------------------------------------------- |
| 4-75         | advn-approval-rating.test.ts | ADVN-01: ApprovalRatingSchema describe 블록 |
| 77-126       | advn-frame-war.test.ts       | ADVN-02: FrameWarSchema describe 블록       |
| 128-186      | advn-crisis-scenario.test.ts | ADVN-03: CrisisScenarioSchema describe 블록 |
| 188-263      | advn-win-simulation.test.ts  | ADVN-04: WinSimulationSchema describe 블록  |
| 265-300      | advn-exports.test.ts         | ADVN 모듈 export 확인 describe 블록         |

**각 파일 구조:**

```typescript
import { describe, it, expect } from 'vitest';
import { ZodError } from 'zod';

describe('ADVN-XX: ...', () => {
  // 원본 describe 블록 내용 그대로 복사
});
```

주의: ZodError는 일부 describe 블록에서만 사용됨. 사용하지 않는 파일에서는 import하지 않아도 되지만, 간단하게 모든 파일에 포함해도 무방 (tree-shaking됨).

**원본 파일 삭제:**
advn-schema.test.ts를 `git rm` 또는 파일 삭제한다.
</action>
<verify>
<automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test 2>&1 | tail -15</automated>
</verify>
<acceptance_criteria> - packages/core/tests/advn-schema.test.ts 파일이 존재하지 않음 - packages/core/tests/advn-approval-rating.test.ts 파일이 존재하며 `describe('ADVN-01: ApprovalRatingSchema'` 포함 - packages/core/tests/advn-frame-war.test.ts 파일이 존재하며 `describe('ADVN-02: FrameWarSchema'` 포함 - packages/core/tests/advn-crisis-scenario.test.ts 파일이 존재하며 `describe('ADVN-03: CrisisScenarioSchema'` 포함 - packages/core/tests/advn-win-simulation.test.ts 파일이 존재하며 `describe('ADVN-04: WinSimulationSchema'` 포함 - packages/core/tests/advn-exports.test.ts 파일이 존재하며 `describe('ADVN 모듈 export 확인'` 포함 - 각 분할 파일이 `wc -l` 기준 300줄 이하 - `pnpm --filter @ai-signalcraft/core test` 명령에서 사전 실패 6개 외 모든 테스트 통과
</acceptance_criteria>
<done>advn-schema.test.ts가 5개 파일로 분할되고 모든 테스트가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: Phase 9 전체 검증 (TYPE-04 최종)</name>
  <files>
    (검증 전용 -- 파일 수정 없음)
  </files>
  <read_first>
    packages/core/src/types/index.ts,
    packages/ai-gateway/tests/gateway.test.ts
  </read_first>
  <action>
Phase 9 모든 작업(타입 중앙화 + ai-gateway 테스트 + 테스트 분할)의 최종 통합 검증을 수행한다.

1. `pnpm -r build` 실행 -- 모든 패키지 TypeScript 컴파일 통과
2. `pnpm -r test` 실행 -- 전체 테스트 실행
3. 결과 확인:
   - collectors: 49 tests passed
   - core: 사전 실패 6개 제외 96개+ 통과 (분할된 advn 테스트 포함)
   - ai-gateway: 10개+ 신규 테스트 통과
4. 타입 중앙화 확인:
   - `grep -r "export interface ModuleModelConfig" packages/core/src/types/` -- types/에 존재
   - `grep -r "export interface ModuleModelConfig" packages/core/src/analysis/model-config.ts` -- 원본에서 제거됨 (re-export만)
5. 테스트 분할 확인:
   - `ls packages/core/tests/advn-*.test.ts` -- 5개 파일 존재
   - `test ! -f packages/core/tests/advn-schema.test.ts` -- 원본 삭제됨

문제 발생 시 이전 Plan(09-01, 09-02)의 SUMMARY를 참조하여 원인 파악.
</action>
<verify>
<automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm -r test 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - `pnpm -r build` 명령이 exit code 0 - `pnpm -r test` 결과에서 collectors 49개 통과 - `pnpm -r test` 결과에서 core 96개+ 통과 (사전 실패 6개 제외) - `pnpm -r test` 결과에서 ai-gateway 10개+ 통과 - packages/core/tests/advn-schema.test.ts 파일이 존재하지 않음 - packages/core/tests/advn-approval-rating.test.ts 등 5개 파일 존재
</acceptance_criteria>
<done>Phase 9 전체 리팩토링(TYPE-01~04)이 완료되고 모든 테스트가 통과한다</done>
</task>

</tasks>

<verification>
- `pnpm -r build` 성공
- `pnpm -r test` -- collectors 49, core 96+, ai-gateway 10+ 통과
- advn-schema.test.ts 삭제됨
- 5개 분할 파일 각각 300줄 이하
- 타입 중앙화 유지 확인 (types/ 디렉토리에 타입 존재)
</verification>

<success_criteria>

1. advn-schema.test.ts가 5개 파일로 분할됨
2. 각 파일이 300줄 이하
3. 모든 패키지의 테스트가 통과 (사전 실패 6개 제외)
4. Phase 9 전체 목표(타입 중앙화 + ai-gateway 테스트 + 테스트 분할 + 전체 테스트 통과) 달성
   </success_criteria>

<output>
After completion, create `.planning/phases/09-types-tests/09-03-SUMMARY.md`
</output>
