---
phase: 09-types-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ai-gateway/src/gateway.ts
  - packages/ai-gateway/tests/gateway.test.ts
autonomous: true
requirements: [TYPE-02, TYPE-04]

must_haves:
  truths:
    - "ai-gateway 패키지에 단위 테스트가 존재하며 vitest run으로 통과한다"
    - "getModel이 프로바이더별로 올바른 SDK 클라이언트를 호출한다"
    - "analyzeText/analyzeStructured가 AI SDK 함수에 올바른 옵션을 전달한다"
  artifacts:
    - path: "packages/ai-gateway/tests/gateway.test.ts"
      provides: "getModel, analyzeText, analyzeStructured 단위 테스트"
      contains: "describe('getModel'"
    - path: "packages/ai-gateway/src/gateway.ts"
      provides: "getModel export 추가"
      contains: "export function getModel"
  key_links:
    - from: "packages/ai-gateway/tests/gateway.test.ts"
      to: "packages/ai-gateway/src/gateway.ts"
      via: "import { getModel, analyzeText, analyzeStructured }"
      pattern: "import.*getModel.*from.*gateway"
---

<objective>
ai-gateway 패키지에 vi.mock 기반 단위 테스트를 추가하여 테스트 커버리지를 0%에서 주요 함수 수준으로 올린다.

Purpose: getModel의 프로바이더별 라우팅, baseUrl 정규화, analyzeText/analyzeStructured 옵션 전달을 검증한다.
Output: gateway.test.ts 생성, getModel export 변경
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-types-tests/09-CONTEXT.md
@.planning/phases/09-types-tests/09-RESEARCH.md

<interfaces>
<!-- packages/ai-gateway/src/gateway.ts -- 전체 112줄, 테스트 대상 -->
export type AIProvider = 'anthropic' | 'openai' | 'gemini' | 'ollama' | 'deepseek' | 'xai' | 'openrouter' | 'custom';
export interface AIGatewayOptions {
  provider?: AIProvider;
  model?: string;
  maxOutputTokens?: number;
  systemPrompt?: string;
  baseUrl?: string;
  apiKey?: string;
}
// getModel은 현재 non-export, D-07에 따라 export 필요
function getModel(provider: AIProvider, model?: string, baseUrl?: string, apiKey?: string) {...}
export async function analyzeText(prompt: string, options: AIGatewayOptions = {}) {...}
export async function analyzeStructured<T>(prompt: string, schema: z.ZodType<T>, options: AIGatewayOptions = {}) {...}

<!-- DEFAULT_MODELS -->
const DEFAULT_MODELS: Partial<Record<AIProvider, string>> = {
  anthropic: 'claude-sonnet-4-20250514',
  openai: 'gpt-4o-mini',
};

<!-- DEFAULT_BASE_URLS -->
const DEFAULT_BASE_URLS: Partial<Record<AIProvider, string>> = {
  openai: 'https://api.openai.com/v1',
  deepseek: 'https://api.deepseek.com/v1',
  xai: 'https://api.x.ai/v1',
  openrouter: 'https://openrouter.ai/api/v1',
  ollama: 'http://localhost:11434/v1',
};

<!-- vitest.config.ts -->
export default defineConfig({ test: { globals: true } });

<!-- package.json test script -->
"test": "vitest run"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getModel export 변경 + gateway.test.ts 작성 (per D-06, D-07)</name>
  <files>
    packages/ai-gateway/src/gateway.ts,
    packages/ai-gateway/tests/gateway.test.ts
  </files>
  <read_first>
    packages/ai-gateway/src/gateway.ts,
    packages/ai-gateway/src/index.ts,
    packages/ai-gateway/vitest.config.ts,
    packages/ai-gateway/package.json
  </read_first>
  <behavior>
    - getModel('anthropic') -> createAnthropic 호출, 반환값으로 모델 생성
    - getModel('openai') -> createOpenAI 호출, client(modelName) 반환
    - getModel('ollama') -> createOpenAI({ baseURL: 'http://localhost:11434/v1' }) 호출, client.chat(modelName) 반환
    - getModel('deepseek') -> createOpenAI({ baseURL: 'https://api.deepseek.com/v1' }) 호출, client.chat 반환
    - getModel('xai') -> createOpenAI({ baseURL: 'https://api.x.ai/v1' }) 호출, client.chat 반환
    - getModel('openrouter') -> createOpenAI({ baseURL: 'https://openrouter.ai/api/v1' }) 호출, client.chat 반환
    - getModel('custom', 'model', 'http://example.com') -> baseURL에서 trailing slash 제거 후 /v1 추가
    - getModel('custom', 'model', 'http://example.com/v1') -> /v1 이미 있으면 추가하지 않음
    - getModel('custom', 'model', 'http://example.com/v1/') -> trailing slash 제거만
    - getModel('anthropic') -> 기본 모델 'claude-sonnet-4-20250514' 사용
    - getModel('openai') -> 기본 모델 'gpt-4o-mini' 사용
    - getModel('gemini') -> 기본 모델 'gpt-4o-mini' 사용 (DEFAULT_MODELS에 없으면 fallback)
    - analyzeText('prompt', { provider: 'anthropic', systemPrompt: 'sys' }) -> generateText에 system: 'sys' 전달
    - analyzeText('prompt', {}) -> provider 기본값 'anthropic', maxOutputTokens 기본값 4096
    - analyzeStructured('prompt', schema, { provider: 'openai' }) -> generateObject에 schema 전달
  </behavior>
  <action>
**Step 1: getModel을 export로 변경 (per D-07)**

`packages/ai-gateway/src/gateway.ts` 31줄:
- `function getModel(` 을 `export function getModel(` 로 변경
- ai-gateway/src/index.ts는 변경하지 않음 (패키지 외부 API 유지, per D-07 권장사항)

**Step 2: tests/ 디렉토리 생성 + gateway.test.ts 작성 (per D-06)**

`packages/ai-gateway/tests/gateway.test.ts` 생성:

vi.mock 패턴으로 'ai', '@ai-sdk/anthropic', '@ai-sdk/openai' 3개 모듈을 mock.

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { z } from 'zod';

// mock 함수 정의
const mockGenerateText = vi.fn();
const mockGenerateObject = vi.fn();

vi.mock('ai', () => ({
  generateText: (...args: unknown[]) => mockGenerateText(...args),
  generateObject: (...args: unknown[]) => mockGenerateObject(...args),
}));

const mockCreateAnthropic = vi.fn();
const mockCreateOpenAI = vi.fn();

vi.mock('@ai-sdk/anthropic', () => ({
  createAnthropic: (...args: unknown[]) => mockCreateAnthropic(...args),
}));

vi.mock('@ai-sdk/openai', () => ({
  createOpenAI: (...args: unknown[]) => mockCreateOpenAI(...args),
}));

// mock 이후 import
import { analyzeText, analyzeStructured, getModel } from '../src/gateway';
```

테스트 케이스 구조:

**describe('getModel'):**
- beforeEach: vi.clearAllMocks(), mockCreateAnthropic.mockReturnValue(vi.fn(() => 'anthropic-model')), mockCreateOpenAI에 chat 메서드 포함 mock 설정
- it('anthropic 프로바이더에 createAnthropic 호출')
- it('openai 프로바이더에 createOpenAI 호출, client(modelName) 반환')
- it('ollama 프로바이더에 createOpenAI({ baseURL: "http://localhost:11434/v1" }) + client.chat 호출')
- it('deepseek/xai/openrouter 프로바이더에 각 기본 baseURL 적용')
- it('custom 프로바이더에 trailing slash 제거 후 /v1 추가')
- it('baseUrl이 이미 /v1로 끝나면 추가하지 않음')
- it('anthropic 기본 모델은 claude-sonnet-4-20250514')
- it('openai 기본 모델은 gpt-4o-mini')
- it('DEFAULT_MODELS에 없는 프로바이더는 gpt-4o-mini 폴백')

**describe('analyzeText'):**
- beforeEach: mockGenerateText.mockResolvedValue({ text: 'result', usage: {}, finishReason: 'stop' })
- it('기본 provider는 anthropic')
- it('systemPrompt가 있으면 system으로 전달')
- it('maxOutputTokens 기본값 4096')
- it('반환 형태는 { text, usage, finishReason }')

**describe('analyzeStructured'):**
- beforeEach: mockGenerateObject.mockResolvedValue({ object: { key: 'value' }, usage: {}, finishReason: 'stop' })
- it('schema를 generateObject에 전달')
- it('반환 형태는 { object, usage, finishReason }')

console.log는 테스트에서 무시 (getModel의 콘솔 로그는 테스트 출력에 표시되지만 검증 대상 아님).
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/ai-gateway test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/ai-gateway/tests/gateway.test.ts 파일이 존재
    - gateway.test.ts에 `describe('getModel'` 블록 포함
    - gateway.test.ts에 `describe('analyzeText'` 블록 포함
    - gateway.test.ts에 `describe('analyzeStructured'` 블록 포함
    - gateway.test.ts에 `vi.mock('ai'` 포함
    - gateway.test.ts에 `vi.mock('@ai-sdk/anthropic'` 포함
    - gateway.test.ts에 `vi.mock('@ai-sdk/openai'` 포함
    - packages/ai-gateway/src/gateway.ts에 `export function getModel(` 포함
    - `pnpm --filter @ai-signalcraft/ai-gateway test` 명령이 exit code 0으로 완료
    - 최소 10개 이상 테스트 통과
  </acceptance_criteria>
  <done>ai-gateway에 getModel/analyzeText/analyzeStructured 단위 테스트가 존재하고 모두 통과한다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/ai-gateway test` -- 모든 테스트 통과
- `grep "export function getModel" packages/ai-gateway/src/gateway.ts` -- export 확인
- gateway.test.ts에 vi.mock 패턴이 파일 상단에 위치 (호이스팅 이슈 방지)
</verification>

<success_criteria>
1. ai-gateway 패키지에 gateway.test.ts가 존재
2. getModel이 export되어 직접 테스트 가능
3. 프로바이더별 라우팅, baseUrl 정규화, 기본값 처리 테스트 통과
4. analyzeText/analyzeStructured의 옵션 전달 테스트 통과
5. 실제 AI API 호출 없이 vi.mock으로 테스트 완료
</success_criteria>

<output>
After completion, create `.planning/phases/09-types-tests/09-02-SUMMARY.md`
</output>
