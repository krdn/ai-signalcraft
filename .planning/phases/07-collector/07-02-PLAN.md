---
phase: 07-collector
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - packages/collectors/src/adapters/clien.ts
  - packages/collectors/src/adapters/dcinside.ts
  - packages/collectors/src/adapters/fmkorea.ts
autonomous: true
requirements: [COL-02, COL-05]

must_haves:
  truths:
    - 'ClienCollector가 CommunityBaseCollector를 상속하고 collect() 메서드를 직접 구현하지 않는다'
    - 'DCInsideCollector가 CommunityBaseCollector를 상속하고 collect() 메서드를 직접 구현하지 않는다'
    - 'FMKoreaCollector가 CommunityBaseCollector를 상속하고 collect() 메서드를 직접 구현하지 않는다'
    - '3개 어댑터에서 USER_AGENTS 배열, 브라우저 초기화 코드, 페이지 순회 루프가 제거되었다'
    - '기존 테스트 49개가 모두 통과한다'
  artifacts:
    - path: 'packages/collectors/src/adapters/clien.ts'
      provides: 'ClienCollector extends CommunityBaseCollector'
      contains: 'extends CommunityBaseCollector'
    - path: 'packages/collectors/src/adapters/dcinside.ts'
      provides: 'DCInsideCollector extends CommunityBaseCollector'
      contains: 'extends CommunityBaseCollector'
    - path: 'packages/collectors/src/adapters/fmkorea.ts'
      provides: 'FMKoreaCollector extends CommunityBaseCollector'
      contains: 'extends CommunityBaseCollector'
  key_links:
    - from: 'packages/collectors/src/adapters/clien.ts'
      to: 'packages/collectors/src/adapters/community-base-collector.ts'
      via: 'extends CommunityBaseCollector'
      pattern: 'extends CommunityBaseCollector'
    - from: 'packages/collectors/src/adapters/dcinside.ts'
      to: 'packages/collectors/src/adapters/community-base-collector.ts'
      via: 'extends CommunityBaseCollector'
      pattern: 'extends CommunityBaseCollector'
    - from: 'packages/collectors/src/adapters/fmkorea.ts'
      to: 'packages/collectors/src/adapters/community-base-collector.ts'
      via: 'extends CommunityBaseCollector'
      pattern: 'extends CommunityBaseCollector'
---

<objective>
clien/dcinside/fmkorea 3개 어댑터를 CommunityBaseCollector 상속으로 마이그레이션

Purpose: 3개 어댑터의 중복 코드 ~315줄(브라우저 초기화, 페이지 순회, 에러처리, USER_AGENTS)을 제거하고 사이트별 고유 로직(셀렉터, DOM 파싱)만 남긴다.
Output: 리팩토링된 clien.ts (~120줄), dcinside.ts (~110줄), fmkorea.ts (~120줄)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-collector/07-RESEARCH.md
@.planning/phases/07-collector/07-01-SUMMARY.md

@packages/collectors/src/adapters/browser-collector.ts
@packages/collectors/src/adapters/community-base-collector.ts
@packages/collectors/src/utils/browser.ts
@packages/collectors/src/adapters/clien.ts
@packages/collectors/src/adapters/dcinside.ts
@packages/collectors/src/adapters/fmkorea.ts
@packages/collectors/src/utils/community-parser.ts
@packages/collectors/src/types/community.ts

<interfaces>
<!-- Plan 01에서 생성하는 인터페이스 -- executor는 이 타입에 맞춰 구현 -->

From packages/collectors/src/adapters/browser-collector.ts:

```typescript
export interface BrowserCollectorConfig {
  pageDelay: { min: number; max: number };
  postDelay: { min: number; max: number };
  defaultMaxItems: number;
  maxSearchPages: number;
}

export abstract class BrowserCollector<T> implements Collector<T> {
  abstract readonly source: string;
  protected abstract readonly config: BrowserCollectorConfig;
  async *collect(options: CollectionOptions): AsyncGenerator<T[], void, unknown>;
  protected abstract doCollect(
    page: Page,
    options: CollectionOptions,
  ): AsyncGenerator<T[], void, unknown>;
}
```

From packages/collectors/src/adapters/community-base-collector.ts:

```typescript
export interface SiteSelectors {
  list: string[];
  content: string[];
  comment: string[];
}

export abstract class CommunityBaseCollector extends BrowserCollector<CommunityPost> {
  protected abstract readonly selectors: SiteSelectors;
  protected abstract readonly baseUrl: string;
  protected abstract buildSearchUrl(keyword: string, page: number): string;
  protected abstract parseSearchResults(html: string): { url: string; title: string }[];
  protected abstract fetchPost(
    page: Page,
    url: string,
    title: string,
    maxComments: number,
  ): Promise<CommunityPost | null>;
  protected detectBlocked(html: string): boolean; // 기본 false
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: clien.ts를 CommunityBaseCollector 상속으로 리팩토링</name>
  <files>packages/collectors/src/adapters/clien.ts</files>
  <read_first>
    - packages/collectors/src/adapters/clien.ts
    - packages/collectors/src/adapters/community-base-collector.ts
    - packages/collectors/src/adapters/browser-collector.ts
    - packages/collectors/src/utils/browser.ts
  </read_first>
  <action>
    ClienCollector를 `extends CommunityBaseCollector`로 변경한다.

    **제거할 것:**
    - `import { type Browser } from 'playwright'` (BrowserCollector가 처리)
    - `import { launchBrowser } from '../utils/browser'` (BrowserCollector가 처리)
    - `USER_AGENTS` 배열 (browser.ts로 통합됨)
    - `PAGE_DELAY`, `POST_DELAY`, `DEFAULT_MAX_ITEMS`, `MAX_SEARCH_PAGES` 상수 -> `config` 프로퍼티로
    - `collect()` 메서드 전체 (CommunityBaseCollector.doCollect이 담당)
    - `import { sleep } from '../utils/community-parser'` (community-base-collector가 import)

    **유지/변경할 것:**
    - `import * as cheerio from 'cheerio'` 유지
    - `import { parseDateText, sanitizeContent, buildSearchUrl } from '../utils/community-parser'` (sleep 제거)
    - `import type { CommunityPost, CommunityComment } from '../types/community'` 유지
    - LIST_SELECTORS, CONTENT_SELECTORS, COMMENT_SELECTORS -> `selectors` 프로퍼티로 통합

    **추가할 것:**
    - `import { CommunityBaseCollector, type SiteSelectors } from './community-base-collector'`
    - `import type { BrowserCollectorConfig } from './browser-collector'`

    **리팩토링 후 ClienCollector 구조:**
    ```typescript
    export class ClienCollector extends CommunityBaseCollector {
      readonly source = 'clien';
      protected readonly baseUrl = 'https://www.clien.net';
      protected readonly config: BrowserCollectorConfig = {
        pageDelay: { min: 3000, max: 5000 },
        postDelay: { min: 1500, max: 2500 },
        defaultMaxItems: 50,
        maxSearchPages: 15,
      };
      protected readonly selectors: SiteSelectors = {
        list: ['/* 기존 LIST_SELECTORS 값 */'],
        content: ['/* 기존 CONTENT_SELECTORS 값 */'],
        comment: ['/* 기존 COMMENT_SELECTORS 값 */'],
      };

      // 차단 감지 override (clien 전용)
      protected detectBlocked(html: string): boolean {
        return html.includes('접근이 제한') || html.includes('403') || html.includes('차단');
      }

      protected buildSearchUrl(keyword: string, page: number): string {
        return buildSearchUrl('clien', keyword, page);
      }

      protected parseSearchResults(html: string): { url: string; title: string }[] {
        // 기존 private parseSearchResults 로직 그대로 (private -> protected)
      }

      protected async fetchPost(page: import('playwright').Page, url: string, title: string, maxComments: number): Promise<CommunityPost | null> {
        // 기존 private fetchPost 로직 그대로 (private -> protected)
      }

      private parseComments($: cheerio.CheerioAPI, postSourceId: string, maxComments: number): CommunityComment[] {
        // 기존 로직 그대로 유지
      }

      private extractSourceId(url: string): string { /* 기존 */ }
      private extractBoardFromUrl(url: string): string { /* 기존 */ }
    }
    ```

    핵심: collect() 메서드 전체와 브라우저 초기화/순회 코드를 제거하고, parseSearchResults/fetchPost를 private -> protected로 변경한다. parseComments, extractSourceId, extractBoardFromUrl은 private 유지.

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/collectors exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - clien.ts에 `extends CommunityBaseCollector` 존재
    - clien.ts에 `collect(` 메서드가 없음 (부모 클래스에서 상속)
    - clien.ts에 `USER_AGENTS` 배열이 없음
    - clien.ts에 `launchBrowser` import가 없음
    - clien.ts에 `protected readonly config: BrowserCollectorConfig` 존재
    - clien.ts에 `protected readonly selectors: SiteSelectors` 존재
    - clien.ts에 `protected detectBlocked(` 존재
    - clien.ts에 `protected buildSearchUrl(` 존재
    - clien.ts에 `protected parseSearchResults(` 존재 (private -> protected)
    - clien.ts에 `protected async fetchPost(` 존재 (private -> protected)
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>ClienCollector가 CommunityBaseCollector를 상속하고 사이트별 로직(셀렉터, DOM 파싱, 차단 감지)만 남아 있다</done>
</task>

<task type="auto">
  <name>Task 2: dcinside.ts + fmkorea.ts를 CommunityBaseCollector 상속으로 리팩토링</name>
  <files>
    packages/collectors/src/adapters/dcinside.ts
    packages/collectors/src/adapters/fmkorea.ts
  </files>
  <read_first>
    - packages/collectors/src/adapters/dcinside.ts
    - packages/collectors/src/adapters/fmkorea.ts
    - packages/collectors/src/adapters/community-base-collector.ts
    - packages/collectors/src/adapters/browser-collector.ts
    - packages/collectors/src/adapters/clien.ts (Task 1 결과 -- 패턴 확인)
  </read_first>
  <action>
    DCInsideCollector와 FMKoreaCollector를 clien.ts와 동일한 패턴으로 리팩토링한다.

    **DCInsideCollector:**
    - `extends CommunityBaseCollector`
    - config: `{ pageDelay: { min: 2000, max: 3000 }, postDelay: { min: 1000, max: 2000 }, defaultMaxItems: 50, maxSearchPages: 20 }`
    - selectors: 기존 LIST_SELECTORS/CONTENT_SELECTORS/COMMENT_SELECTORS 값
    - baseUrl: `'https://gall.dcinside.com'`
    - detectBlocked 미구현 (기본 false 사용)
    - buildSearchUrl: `buildSearchUrl('dcinside', keyword, page)` 호출
    - parseSearchResults/fetchPost: private -> protected (로직 동일)
    - parseComments/extractSourceId/extractBoardFromUrl: private 유지
    - 제거: collect(), USER_AGENTS, 상수 선언, launchBrowser import, Browser import, sleep import

    **FMKoreaCollector:**
    - `extends CommunityBaseCollector`
    - config: `{ pageDelay: { min: 2000, max: 4000 }, postDelay: { min: 1000, max: 2000 }, defaultMaxItems: 50, maxSearchPages: 20 }`
    - selectors: 기존 LIST_SELECTORS/CONTENT_SELECTORS/COMMENT_SELECTORS 값
    - baseUrl: `'https://www.fmkorea.com'`
    - detectBlocked override: `html.includes('자동등록방지') || html.includes('captcha') || html.includes('접근이 제한')`
    - buildSearchUrl: `buildSearchUrl('fmkorea', keyword, page)` 호출
    - parseSearchResults/fetchPost: private -> protected (로직 동일)
    - parseComments/extractSourceId/extractBoardFromUrl: private 유지
    - 제거: collect(), USER_AGENTS, 상수 선언, launchBrowser import, Browser import, sleep import

    각 파일의 import 변경:
    - 제거: `import { type Browser } from 'playwright'`, `import { launchBrowser } from '../utils/browser'`
    - 제거: community-parser에서 `sleep` import
    - 추가: `import { CommunityBaseCollector, type SiteSelectors } from './community-base-collector'`
    - 추가: `import type { BrowserCollectorConfig } from './browser-collector'`
    - 유지: `import * as cheerio from 'cheerio'`, `import { parseDateText, sanitizeContent, buildSearchUrl } from '../utils/community-parser'`

    리팩토링 후 모든 테스트 실행하여 기존 49개 테스트가 통과하는지 확인한다.

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/collectors test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - dcinside.ts에 `extends CommunityBaseCollector` 존재
    - dcinside.ts에 `collect(` 메서드가 없음
    - dcinside.ts에 `USER_AGENTS` 배열이 없음
    - dcinside.ts에 `launchBrowser` import가 없음
    - dcinside.ts에 `protected readonly config: BrowserCollectorConfig` 존재
    - fmkorea.ts에 `extends CommunityBaseCollector` 존재
    - fmkorea.ts에 `collect(` 메서드가 없음
    - fmkorea.ts에 `USER_AGENTS` 배열이 없음
    - fmkorea.ts에 `launchBrowser` import가 없음
    - fmkorea.ts에 `protected readonly config: BrowserCollectorConfig` 존재
    - fmkorea.ts에 `protected detectBlocked(` 존재
    - `pnpm --filter @ai-signalcraft/collectors test`에서 49개 테스트 모두 통과
  </acceptance_criteria>
  <done>DCInsideCollector와 FMKoreaCollector가 CommunityBaseCollector를 상속하고, 기존 49개 테스트가 모두 통과한다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/collectors exec tsc --noEmit` 통과
- `pnpm --filter @ai-signalcraft/collectors test` 기존 49개 테스트 모두 통과
- 3개 어댑터 모두 `extends CommunityBaseCollector` 사용
- 3개 어댑터에서 `collect()`, `USER_AGENTS`, `launchBrowser` 모두 제거됨
</verification>

<success_criteria>

- clien.ts, dcinside.ts, fmkorea.ts 모두 CommunityBaseCollector를 상속
- 3개 파일에서 collect() 메서드, USER_AGENTS 배열, 브라우저 초기화 코드가 모두 제거됨
- 기존 49개 테스트 모두 통과
- 각 어댑터 파일 줄 수가 리팩토링 전보다 ~40% 이상 감소
  </success_criteria>

<output>
After completion, create `.planning/phases/07-collector/07-02-SUMMARY.md`
</output>
