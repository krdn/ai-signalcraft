---
phase: 07-collector
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - packages/collectors/src/adapters/naver-news.ts
  - packages/collectors/src/utils/community-parser.ts
  - packages/collectors/src/adapters/index.ts
autonomous: true
requirements: [COL-02, COL-04, COL-05]

must_haves:
  truths:
    - 'NaverNewsCollector가 BrowserCollector를 상속하고 브라우저 초기화/종료를 직접 관리하지 않는다'
    - 'naver-news.ts의 자체 delay() 함수가 제거되고 browser.ts의 sleep()을 사용한다'
    - 'naver-news.ts의 자체 parseDateText()가 제거되고 community-parser.ts의 parseDateTextOrNull()을 사용한다'
    - 'community-parser.ts에 parseDateTextOrNull() 함수가 추가되어 Date | null을 반환한다'
    - 'community-parser.ts의 기존 함수들에 반환 타입이 명시되어 있다'
    - 'index.ts에 BrowserCollector, CommunityBaseCollector가 export된다'
    - '기존 49개 테스트가 모두 통과한다'
  artifacts:
    - path: 'packages/collectors/src/adapters/naver-news.ts'
      provides: 'NaverNewsCollector extends BrowserCollector<NaverArticle>'
      contains: 'extends BrowserCollector'
    - path: 'packages/collectors/src/utils/community-parser.ts'
      provides: 'parseDateTextOrNull 함수 + 기존 함수 타입 강화'
      exports:
        [
          'parseDateText',
          'parseDateTextOrNull',
          'randomDelay',
          'sleep',
          'sanitizeContent',
          'buildSearchUrl',
        ]
    - path: 'packages/collectors/src/adapters/index.ts'
      provides: 'BrowserCollector, CommunityBaseCollector re-export'
      contains: 'BrowserCollector'
  key_links:
    - from: 'packages/collectors/src/adapters/naver-news.ts'
      to: 'packages/collectors/src/adapters/browser-collector.ts'
      via: 'extends BrowserCollector<NaverArticle>'
      pattern: 'extends BrowserCollector'
    - from: 'packages/collectors/src/adapters/naver-news.ts'
      to: 'packages/collectors/src/utils/community-parser.ts'
      via: 'import { parseDateTextOrNull }'
      pattern: 'parseDateTextOrNull'
---

<objective>
naver-news를 BrowserCollector 상속으로 마이그레이션 + community-parser 타입 강화 + index.ts export 정리

Purpose: naver-news의 중복 코드(delay, parseDateText) 제거, community-parser의 타입 안전성 강화, 새로운 추상 클래스 export 추가
Output: 리팩토링된 naver-news.ts, 타입 강화된 community-parser.ts, 업데이트된 index.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-collector/07-RESEARCH.md
@.planning/phases/07-collector/07-01-SUMMARY.md

@packages/collectors/src/adapters/browser-collector.ts
@packages/collectors/src/adapters/naver-news.ts
@packages/collectors/src/utils/community-parser.ts
@packages/collectors/src/utils/naver-parser.ts
@packages/collectors/src/adapters/index.ts
@packages/collectors/src/adapters/base.ts

<interfaces>
<!-- Plan 01에서 생성한 인터페이스 -->

From packages/collectors/src/adapters/browser-collector.ts:

```typescript
export interface BrowserCollectorConfig {
  pageDelay: { min: number; max: number };
  postDelay: { min: number; max: number };
  defaultMaxItems: number;
  maxSearchPages: number;
}

export abstract class BrowserCollector<T> implements Collector<T> {
  abstract readonly source: string;
  protected abstract readonly config: BrowserCollectorConfig;
  async *collect(options: CollectionOptions): AsyncGenerator<T[], void, unknown>;
  protected abstract doCollect(
    page: Page,
    options: CollectionOptions,
  ): AsyncGenerator<T[], void, unknown>;
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: community-parser.ts 타입 강화 + parseDateTextOrNull 추가</name>
  <files>packages/collectors/src/utils/community-parser.ts</files>
  <read_first>
    - packages/collectors/src/utils/community-parser.ts
    - packages/collectors/src/adapters/naver-news.ts (parseDateText 메서드 확인 -- 344-366줄)
  </read_first>
  <action>
    community-parser.ts에 다음 변경을 적용한다:

    1. `parseDateTextOrNull(text: string): Date | null` 함수 추가:
       naver-news.ts의 자체 parseDateText (344-366줄)를 기반으로 구현. 파싱 실패 시 null 반환 (기존 parseDateText의 `new Date()` fallback과 다름).
       ```typescript
       /**
        * 날짜 텍스트를 Date 객체로 변환 -- 파싱 실패 시 null 반환
        * naver-news 등 null 체크가 필요한 곳에서 사용
        */
       export function parseDateTextOrNull(text: string): Date | null {
         if (!text) return null;

         // "N시간 전", "N분 전", "N일 전" 형식
         const relativeMatch = text.match(/(\d+)(시간|분|일)\s*전/);
         if (relativeMatch) {
           const now = new Date();
           const amount = parseInt(relativeMatch[1], 10);
           const unit = relativeMatch[2];
           if (unit === '시간') now.setHours(now.getHours() - amount);
           else if (unit === '분') now.setMinutes(now.getMinutes() - amount);
           else if (unit === '일') now.setDate(now.getDate() - amount);
           return now;
         }

         // "YYYY.MM.DD" 형식
         const fullDateMatch = text.match(/(\d{4})\.(\d{1,2})\.(\d{1,2})(?:\s+(\d{1,2}):(\d{1,2}))?/);
         if (fullDateMatch) {
           const year = parseInt(fullDateMatch[1], 10);
           const month = parseInt(fullDateMatch[2], 10) - 1;
           const day = parseInt(fullDateMatch[3], 10);
           const hour = fullDateMatch[4] ? parseInt(fullDateMatch[4], 10) : 0;
           const min = fullDateMatch[5] ? parseInt(fullDateMatch[5], 10) : 0;
           return new Date(year, month, day, hour, min);
         }

         // "MM.DD HH:mm" 형식
         const shortDateMatch = text.match(/^(\d{1,2})\.(\d{1,2})\s+(\d{1,2}):(\d{1,2})$/);
         if (shortDateMatch) {
           const now = new Date();
           return new Date(now.getFullYear(), parseInt(shortDateMatch[1], 10) - 1, parseInt(shortDateMatch[2], 10), parseInt(shortDateMatch[3], 10), parseInt(shortDateMatch[4], 10));
         }

         // "MM/DD HH:mm" 형식
         const slashDateMatch = text.match(/^(\d{1,2})\/(\d{1,2})\s+(\d{1,2}):(\d{1,2})$/);
         if (slashDateMatch) {
           const now = new Date();
           return new Date(now.getFullYear(), parseInt(slashDateMatch[1], 10) - 1, parseInt(slashDateMatch[2], 10), parseInt(slashDateMatch[3], 10), parseInt(slashDateMatch[4], 10));
         }

         return null;
       }
       ```

    2. 기존 함수 반환 타입 명시 확인 -- 이미 명시되어 있으면 변경 불필요:
       - `parseDateText(text: string): Date` -- 이미 명시
       - `randomDelay(min: number, max: number): number` -- 이미 명시
       - `sleep(min: number, max?: number): Promise<void>` -- 이미 명시
       - `sanitizeContent(html: string): string` -- 이미 명시
       - `buildSearchUrl(site: ..., keyword: string, page: number): string` -- 이미 명시

    기존 함수는 모두 타입이 명시되어 있으므로, parseDateTextOrNull만 추가하면 된다.

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/collectors exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - community-parser.ts에 `export function parseDateTextOrNull(text: string): Date | null` 존재
    - community-parser.ts의 기존 함수(parseDateText, randomDelay, sleep, sanitizeContent, buildSearchUrl)가 변경 없이 유지
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>community-parser.ts에 parseDateTextOrNull 함수가 추가되고 기존 함수 호환성이 유지된다</done>
</task>

<task type="auto">
  <name>Task 2: naver-news.ts를 BrowserCollector 상속으로 리팩토링 + index.ts export 정리</name>
  <files>
    packages/collectors/src/adapters/naver-news.ts
    packages/collectors/src/adapters/index.ts
  </files>
  <read_first>
    - packages/collectors/src/adapters/naver-news.ts
    - packages/collectors/src/adapters/browser-collector.ts
    - packages/collectors/src/utils/community-parser.ts (Task 1 결과)
    - packages/collectors/src/adapters/index.ts
  </read_first>
  <action>
    **naver-news.ts 리팩토링:**

    NaverNewsCollector를 `extends BrowserCollector<NaverArticle>`로 변경한다.

    제거:
    - `import { type Browser, type Page } from 'playwright'` -> Page만 유지 (fetchPost에서 사용)
    - `import { launchBrowser } from '../utils/browser'`
    - `function delay(minMs, maxMs)` 자체 함수 (30-33줄)
    - `private parseDateText(text: string): Date | null` 메서드 (344-366줄)
    - `collect()` 메서드의 브라우저 초기화/종료 코드 (BrowserCollector.collect()가 처리)

    추가:
    - `import { BrowserCollector, type BrowserCollectorConfig } from './browser-collector'`
    - `import { parseDateTextOrNull } from '../utils/community-parser'`
    - `import { sleep } from '../utils/browser'` (자체 delay 대체)

    변경:
    - `class NaverNewsCollector implements Collector<NaverArticle>` -> `class NaverNewsCollector extends BrowserCollector<NaverArticle>`
    - config 프로퍼티 추가: `protected readonly config: BrowserCollectorConfig = { pageDelay: { min: 1500, max: 2000 }, postDelay: { min: 500, max: 1000 }, defaultMaxItems: 100, maxSearchPages: 40 }`
    - collect() -> doCollect(page, options)로 리네임. 브라우저 초기화/종료 코드 제거. page 파라미터 사용.
    - `this.parseDateText(...)` 호출을 `parseDateTextOrNull(...)` 호출로 변경 (3곳: 174줄, 246줄, 276줄)
    - `delay(500, 1000)` -> `sleep(500, 1000)`, `delay(PAGE_DELAY_MS, PAGE_DELAY_MS + 500)` -> `sleep(this.config.pageDelay.min, this.config.pageDelay.max)`
    - PAGE_DELAY_MS, DEFAULT_MAX_ITEMS, MAX_SEARCH_PAGES 상수를 config로 이동 (상수 선언 제거)

    **doCollect 구조:**
    ```typescript
    protected async *doCollect(page: Page, options: CollectionOptions): AsyncGenerator<NaverArticle[], void, unknown> {
      const maxItems = options.maxItems ?? this.config.defaultMaxItems;
      let totalCollected = 0;

      for (let pageNum = 1; pageNum <= this.config.maxSearchPages; pageNum++) {
        // 기존 collect() 내부 로직 그대로, browser 초기화/종료만 제거
        // delay() -> sleep(), this.parseDateText() -> parseDateTextOrNull()
      }
    }
    ```

    NaverArticle 인터페이스와 export는 그대로 유지 (외부 consumer 호환성).

    **index.ts 업데이트:**
    기존 export 모두 유지하고, 새로운 추상 클래스 export 추가:
    ```typescript
    export { BrowserCollector, type BrowserCollectorConfig } from './browser-collector';
    export { CommunityBaseCollector, type SiteSelectors } from './community-base-collector';
    ```
    기존 export 순서 유지 -- 맨 위에 base, 그 다음 abstract classes, 그 다음 concrete collectors 순서.

    전체 테스트 실행하여 49개 테스트 통과 확인.

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/collectors test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - naver-news.ts에 `extends BrowserCollector<NaverArticle>` 존재
    - naver-news.ts에 `collect(` 메서드가 없고 `doCollect(` 메서드가 존재
    - naver-news.ts에 `function delay(` 자체 함수가 없음
    - naver-news.ts에 `private parseDateText(` 메서드가 없음
    - naver-news.ts에 `parseDateTextOrNull` import 존재
    - naver-news.ts에 `sleep` import가 `../utils/browser`에서 가져옴
    - naver-news.ts에 `launchBrowser` import가 없음
    - naver-news.ts에 `protected readonly config: BrowserCollectorConfig` 존재
    - index.ts에 `BrowserCollector` export 존재
    - index.ts에 `CommunityBaseCollector` export 존재
    - `pnpm --filter @ai-signalcraft/collectors test`에서 49개 테스트 모두 통과
  </acceptance_criteria>
  <done>NaverNewsCollector가 BrowserCollector를 상속하고, 자체 delay/parseDateText가 제거되었으며, index.ts에 새 추상 클래스가 export되고, 49개 테스트 모두 통과한다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/collectors exec tsc --noEmit` 통과
- `pnpm --filter @ai-signalcraft/collectors test` 기존 49개 테스트 모두 통과
- naver-news.ts에서 자체 delay(), parseDateText() 함수 제거 확인
- community-parser.ts에 parseDateTextOrNull 함수 존재 확인
- index.ts에 BrowserCollector, CommunityBaseCollector export 확인
</verification>

<success_criteria>

- NaverNewsCollector가 BrowserCollector<NaverArticle>를 상속
- naver-news.ts에서 중복 코드(delay ~4줄, parseDateText ~23줄) 제거
- community-parser.ts에 parseDateTextOrNull 함수 추가
- index.ts에 BrowserCollector, CommunityBaseCollector 추가 export
- 기존 49개 테스트 모두 통과
  </success_criteria>

<output>
After completion, create `.planning/phases/07-collector/07-03-SUMMARY.md`
</output>
