---
phase: 08-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/utils/errors.ts
  - packages/core/src/utils/logger.ts
  - packages/core/src/utils/index.ts
  - packages/core/src/analysis/provider-keys.ts
  - packages/core/src/analysis/provider-test.ts
  - packages/core/src/analysis/index.ts
autonomous: true
requirements: [CORE-02, CORE-04]
must_haves:
  truths:
    - "SignalCraftError 계층 에러 클래스가 존재하며 errorCode, context 필드를 포함한다"
    - "createLogger 함수가 모듈 prefix를 자동 부여하며 info/warn/error 레벨을 지원한다"
    - "provider-keys.ts가 CRUD 5개 함수만 포함하여 200줄 이하이다"
    - "provider-test.ts에 testProviderConnection, chatWithProvider, getDefaultBaseUrl이 분리되어 있다"
    - "기존 import 경로(@ai-signalcraft/core)가 barrel export로 유지된다"
  artifacts:
    - path: "packages/core/src/utils/errors.ts"
      provides: "SignalCraftError > CollectionError / AnalysisError / PipelineError / ProviderError"
      contains: "class SignalCraftError"
    - path: "packages/core/src/utils/logger.ts"
      provides: "createLogger 팩토리 함수"
      exports: ["createLogger"]
    - path: "packages/core/src/analysis/provider-keys.ts"
      provides: "CRUD 5개 함수 (getAllProviderKeys, addProviderKey, updateProviderKey, deleteProviderKey, getDecryptedKey)"
      min_lines: 80
    - path: "packages/core/src/analysis/provider-test.ts"
      provides: "testProviderConnection, chatWithProvider, getDefaultBaseUrl"
      exports: ["testProviderConnection", "chatWithProvider", "getDefaultBaseUrl"]
  key_links:
    - from: "packages/core/src/analysis/provider-test.ts"
      to: "packages/core/src/analysis/provider-keys.ts"
      via: "getDecryptedKey import"
      pattern: "import.*getDecryptedKey.*from.*provider-keys"
    - from: "packages/core/src/analysis/index.ts"
      to: "packages/core/src/analysis/provider-test.ts"
      via: "barrel re-export"
      pattern: "export.*from.*provider-test"
---

<objective>
에러 클래스/로거 도입 및 provider-keys.ts 분할

Purpose: CORE-04(에러 처리 통일)와 CORE-02(provider-keys 분할)를 동시에 처리하여 코드 구조를 개선한다
Output: errors.ts, logger.ts 유틸 + provider-keys.ts/provider-test.ts 분할 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 에러 클래스 계층 + 구조화 로거 생성</name>
  <files>packages/core/src/utils/errors.ts, packages/core/src/utils/logger.ts, packages/core/src/utils/index.ts</files>
  <read_first>
    - packages/core/src/utils/index.ts (현재 barrel export 확인)
    - packages/core/src/utils/crypto.ts (기존 유틸 패턴 참조)
    - packages/core/src/queue/worker-process.ts (console.warn/error 패턴 확인)
    - packages/core/src/analysis/runner.ts (console.log/error 패턴 확인)
  </read_first>
  <action>
    per D-04: packages/core/src/utils/errors.ts 생성:
    - SignalCraftError(base) 클래스: extends Error, 필드 errorCode: string, context?: Record&lt;string, unknown&gt;
    - CollectionError extends SignalCraftError (errorCode 기본값 'COLLECTION_ERROR')
    - AnalysisError extends SignalCraftError (errorCode 기본값 'ANALYSIS_ERROR')
    - PipelineError extends SignalCraftError (errorCode 기본값 'PIPELINE_ERROR')
    - ProviderError extends SignalCraftError (errorCode 기본값 'PROVIDER_ERROR')
    - 각 클래스 constructor(message: string, errorCode?: string, context?: Record&lt;string, unknown&gt;)
    - 모든 클래스 export

    per D-05: packages/core/src/utils/logger.ts 생성:
    - createLogger(moduleName: string) 팩토리 함수
    - 반환 객체: { info(...args: unknown[]): void, warn(...args: unknown[]): void, error(...args: unknown[]): void }
    - info는 console.log(`[${moduleName}]`, ...args)
    - warn은 console.warn(`[${moduleName}]`, ...args)
    - error는 console.error(`[${moduleName}]`, ...args)
    - export { createLogger }

    packages/core/src/utils/index.ts 수정:
    - 기존 `export { encrypt, decrypt, maskKey } from './crypto';` 유지
    - 추가: `export * from './errors';`
    - 추가: `export * from './logger';`
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && npx tsc --noEmit -p packages/core/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/utils/errors.ts 파일에 `export class SignalCraftError extends Error` 존재
    - packages/core/src/utils/errors.ts 파일에 `export class CollectionError extends SignalCraftError` 존재
    - packages/core/src/utils/errors.ts 파일에 `export class AnalysisError extends SignalCraftError` 존재
    - packages/core/src/utils/errors.ts 파일에 `export class PipelineError extends SignalCraftError` 존재
    - packages/core/src/utils/errors.ts 파일에 `export class ProviderError extends SignalCraftError` 존재
    - packages/core/src/utils/errors.ts 파일에 `errorCode: string` 필드 존재
    - packages/core/src/utils/logger.ts 파일에 `export function createLogger` 존재
    - packages/core/src/utils/index.ts 파일에 `export * from './errors'` 존재
    - packages/core/src/utils/index.ts 파일에 `export * from './logger'` 존재
    - tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>SignalCraftError 계층 5개 클래스와 createLogger 팩토리가 utils/에 생성되고 barrel export로 노출된다</done>
</task>

<task type="auto">
  <name>Task 2: provider-keys.ts CRUD/테스트 분리</name>
  <files>packages/core/src/analysis/provider-keys.ts, packages/core/src/analysis/provider-test.ts, packages/core/src/analysis/index.ts</files>
  <read_first>
    - packages/core/src/analysis/provider-keys.ts (전체 443줄 -- 분할 대상)
    - packages/core/src/analysis/index.ts (barrel export 패턴)
  </read_first>
  <action>
    per D-02: provider-keys.ts를 CRUD와 테스트로 분리

    packages/core/src/analysis/provider-test.ts 신규 생성:
    - provider-keys.ts에서 다음 함수/상수를 이동:
      - ANTHROPIC_MODELS 상수 배열
      - testProviderConnection(id: number) 함수 (176~291줄)
      - chatWithProvider(id: number, prompt: string) 함수 (297~427줄)
      - getDefaultBaseUrl(providerType: string) 함수 (430~443줄)
    - import 문: `import { eq } from 'drizzle-orm'; import { getDb } from '../db'; import { providerKeys } from '../db/schema/settings'; import { decrypt } from '../utils/crypto';`
    - 모든 함수 export

    packages/core/src/analysis/provider-keys.ts 수정:
    - 남는 함수: ProviderKeyInfo 인터페이스, getAllProviderKeys, addProviderKey, updateProviderKey, deleteProviderKey, getDecryptedKey
    - ANTHROPIC_MODELS, testProviderConnection, chatWithProvider, getDefaultBaseUrl 제거
    - 결과: ~170줄 이하 (200줄 이하 목표 달성)

    per D-06: packages/core/src/analysis/index.ts 수정:
    - 기존 `export * from './provider-keys';` 유지
    - 추가: `export * from './provider-test';`
    - 이로써 기존 `import { testProviderConnection } from '@ai-signalcraft/core'` 경로 유지
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && npx tsc --noEmit -p packages/core/tsconfig.json 2>&1 | head -20 && wc -l packages/core/src/analysis/provider-keys.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/analysis/provider-keys.ts가 200줄 이하 (wc -l 출력 확인)
    - packages/core/src/analysis/provider-test.ts 파일에 `export async function testProviderConnection` 존재
    - packages/core/src/analysis/provider-test.ts 파일에 `export async function chatWithProvider` 존재
    - packages/core/src/analysis/provider-test.ts 파일에 `function getDefaultBaseUrl` 존재
    - packages/core/src/analysis/provider-test.ts 파일에 `ANTHROPIC_MODELS` 존재
    - packages/core/src/analysis/provider-keys.ts 파일에 `testProviderConnection` 미포함
    - packages/core/src/analysis/provider-keys.ts 파일에 `chatWithProvider` 미포함
    - packages/core/src/analysis/index.ts 파일에 `export * from './provider-test'` 존재
    - tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>provider-keys.ts가 CRUD 5개 함수만 남아 200줄 이하이고, provider-test.ts에 테스트/채팅/URL 함수가 분리되었다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/core/tsconfig.json` 성공
- `wc -l packages/core/src/analysis/provider-keys.ts` < 200
- `pnpm --filter @ai-signalcraft/core test` 기존 통과 테스트 동일 유지 (9/11 파일, DB 미연결 2개 제외)
</verification>

<success_criteria>
1. errors.ts에 5개 에러 클래스 계층이 존재
2. logger.ts에 createLogger 팩토리가 존재
3. provider-keys.ts가 200줄 이하
4. provider-test.ts에 3개 함수 + 1개 상수가 분리
5. barrel export로 기존 import 경로 유지
6. TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/08-core/08-01-SUMMARY.md`
</output>
