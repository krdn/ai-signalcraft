---
phase: 08-core
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - packages/core/src/queue/worker-process.ts
  - packages/core/src/queue/worker-config.ts
  - packages/core/src/queue/collector-worker.ts
  - packages/core/src/queue/pipeline-worker.ts
  - packages/core/src/queue/analysis-worker.ts
  - packages/core/src/queue/index.ts
autonomous: true
requirements: [CORE-01, CORE-05]
must_haves:
  truths:
    - "worker-process.ts가 진입점/shutdown만 담당하며 100줄 이하이다"
    - "collector-worker.ts에 수집 Worker 핸들러가 분리되어 200줄 이하이다"
    - "pipeline-worker.ts에 파이프라인 Worker 핸들러가 분리되어 200줄 이하이다"
    - "analysis-worker.ts에 분석 Worker가 분리되어 있다"
    - "worker-config.ts에 env 로드/수집기 등록/유틸 함수가 분리되어 있다"
    - "기존 11개 테스트 파일 중 9개가 리팩토링 후에도 통과한다 (DB 미연결 2개 제외)"
  artifacts:
    - path: "packages/core/src/queue/worker-process.ts"
      provides: "진입점 + graceful shutdown"
      min_lines: 20
    - path: "packages/core/src/queue/worker-config.ts"
      provides: "findMonorepoRoot, dotenv 로드, API 키 검증, 수집기 등록, countBySourceType, progressKey, COMMUNITY_SOURCES"
      contains: "findMonorepoRoot"
    - path: "packages/core/src/queue/collector-worker.ts"
      provides: "수집 Worker 핸들러"
      exports: ["createCollectorHandler"]
    - path: "packages/core/src/queue/pipeline-worker.ts"
      provides: "파이프라인 Worker 핸들러 (normalize + persist)"
      exports: ["createPipelineHandler"]
    - path: "packages/core/src/queue/analysis-worker.ts"
      provides: "분석 Worker"
      exports: ["createAnalysisWorker"]
  key_links:
    - from: "packages/core/src/queue/worker-process.ts"
      to: "packages/core/src/queue/worker-config.ts"
      via: "init() 호출로 env 로드 및 수집기 등록"
      pattern: "import.*worker-config"
    - from: "packages/core/src/queue/worker-process.ts"
      to: "packages/core/src/queue/collector-worker.ts"
      via: "Worker 생성"
      pattern: "import.*collector-worker"
    - from: "packages/core/src/queue/worker-process.ts"
      to: "packages/core/src/queue/pipeline-worker.ts"
      via: "Worker 생성"
      pattern: "import.*pipeline-worker"
---

<objective>
worker-process.ts(452줄)를 5개 파일로 분할 + 전체 테스트 검증

Purpose: CORE-01(worker-process 분할)과 CORE-05(E2E 동작 유지 검증)를 처리한다. Plan 01의 에러 클래스/로거와 Plan 02의 runner 분할이 완료된 후 실행한다.
Output: worker-process.ts(진입점) + worker-config.ts + collector-worker.ts + pipeline-worker.ts + analysis-worker.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core/08-CONTEXT.md

<interfaces>
<!-- worker-process.ts가 사용하는 핵심 import들 -->
From packages/core/src/queue/workers.ts:
```typescript
export function createCollectorWorker(handler: (job: Job) => Promise<any>): Worker
export function createPipelineWorker(handler: (job: Job) => Promise<any>): Worker
```

From packages/core/src/queue/connection.ts:
```typescript
export function getRedisConnection(): IORedis
```

From packages/core/src/queue/flows.ts:
```typescript
export function triggerAnalysis(dbJobId: number, keyword: string): Promise<void>
```

From packages/core/src/analysis/runner.ts (Plan 02 이후):
```typescript
export { runAnalysisPipeline } from './pipeline-orchestrator';
```

<!-- Plan 01에서 생성된 유틸 -->
From packages/core/src/utils/logger.ts:
```typescript
export function createLogger(moduleName: string): { info, warn, error }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: worker-process.ts를 5개 파일로 분할</name>
  <files>packages/core/src/queue/worker-process.ts, packages/core/src/queue/worker-config.ts, packages/core/src/queue/collector-worker.ts, packages/core/src/queue/pipeline-worker.ts, packages/core/src/queue/analysis-worker.ts</files>
  <read_first>
    - packages/core/src/queue/worker-process.ts (전체 452줄 -- 분할 대상)
    - packages/core/src/queue/workers.ts (createCollectorWorker, createPipelineWorker 시그니처)
    - packages/core/src/queue/connection.ts (getRedisConnection)
    - packages/core/src/queue/flows.ts (triggerAnalysis)
    - packages/core/src/utils/logger.ts (Plan 01에서 생성된 createLogger)
    - packages/core/tests/worker.test.ts (기존 테스트 -- readFileSync로 worker-process.ts 내용 검증)
  </read_first>
  <action>
    per D-01: worker-process.ts를 Worker별로 분리

    1. packages/core/src/queue/worker-config.ts 신규 생성 (~100줄):
       - worker-process.ts에서 이동할 코드:
         - import { config } from 'dotenv' + import { resolve } from 'path' + import { existsSync } from 'fs'
         - findMonorepoRoot 함수 (8~17줄)
         - dotenv 로드 로직 (19~24줄) -- initEnv() 함수로 래핑
         - requiredApiKeys 배열 + API 키 검증 루프 (57~68줄) -- validateApiKeys() 함수로 래핑
         - 수집기 import + registerCollector 호출 (71~77줄) -- registerAllCollectors() 함수로 래핑
         - COMMUNITY_SOURCES 상수 (80줄)
         - countBySourceType 함수 (83~93줄)
         - progressKey 함수 (96~100줄)
       - createLogger('worker') 사용하여 console.warn 대체 (Plan 01 의존)
       - export: initEnv, validateApiKeys, registerAllCollectors, COMMUNITY_SOURCES, countBySourceType, progressKey

    2. packages/core/src/queue/collector-worker.ts 신규 생성 (~60줄):
       - worker-process.ts 104~149줄의 collectorWorker 핸들러 로직
       - export function createCollectorHandler(): (job: Job) => Promise<CollectorResult> 형태
       - import: Job from 'bullmq', getCollector from '@ai-signalcraft/collectors', updateJobProgress from '../pipeline', progressKey/countBySourceType from './worker-config'
       - createLogger('collector-worker') 사용

    3. packages/core/src/queue/pipeline-worker.ts 신규 생성 (~200줄):
       - worker-process.ts 152~420줄의 pipelineWorker 핸들러 로직
       - export function createPipelineHandler(): (job: Job) => Promise<any> 형태
       - 내부에 normalize-naver, normalize-youtube, normalize-community, persist 분기 모두 포함
       - import: 필요한 collector, pipeline normalize/persist 함수들, triggerAnalysis from './flows'
       - import: COMMUNITY_SOURCES, countBySourceType from './worker-config'
       - createLogger('pipeline-worker') 사용

    4. packages/core/src/queue/analysis-worker.ts 신규 생성 (~25줄):
       - worker-process.ts 423~438줄의 analysisWorker 로직
       - export function createAnalysisWorker(): Worker
       - import: Worker/Job from 'bullmq', getRedisConnection from './connection', runAnalysisPipeline from '../analysis/runner'
       - createLogger('analysis-worker') 사용

    5. packages/core/src/queue/worker-process.ts 재작성 (~50줄):
       - initEnv(), validateApiKeys(), registerAllCollectors() 호출
       - createCollectorWorker(createCollectorHandler()) 호출
       - createPipelineWorker(createPipelineHandler()) 호출
       - createAnalysisWorker() 호출
       - console.log 시작 메시지
       - SIGTERM graceful shutdown
       - import: 위 4개 모듈 + createCollectorWorker/createPipelineWorker from './workers'

    주의: worker.test.ts의 readFileSync 테스트가 worker-process.ts 내용을 직접 검사함
    - 'collectForArticle' 문자열은 pipeline-worker.ts로 이동됨
    - "job.name === 'normalize-naver'" 문자열도 pipeline-worker.ts로 이동됨
    - 이 테스트들이 실패하게 되므로, worker.test.ts의 해당 assert를 pipeline-worker.ts를 읽도록 수정해야 함

    packages/core/tests/worker.test.ts 수정:
    - 40~44줄: readFileSync 경로를 '../src/queue/worker-process.ts'에서 '../src/queue/pipeline-worker.ts'로 변경
    - 이유: collectForArticle과 normalize-naver 로직이 pipeline-worker.ts로 이동했으므로
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && npx tsc --noEmit -p packages/core/tsconfig.json 2>&1 | head -20 && wc -l packages/core/src/queue/worker-process.ts packages/core/src/queue/worker-config.ts packages/core/src/queue/collector-worker.ts packages/core/src/queue/pipeline-worker.ts packages/core/src/queue/analysis-worker.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/queue/worker-process.ts가 100줄 이하 (wc -l 확인)
    - packages/core/src/queue/worker-config.ts 파일에 `function findMonorepoRoot` 존재
    - packages/core/src/queue/worker-config.ts 파일에 `export function initEnv` 존재
    - packages/core/src/queue/worker-config.ts 파일에 `export function registerAllCollectors` 존재
    - packages/core/src/queue/worker-config.ts 파일에 `export const COMMUNITY_SOURCES` 존재
    - packages/core/src/queue/collector-worker.ts 파일에 `export function createCollectorHandler` 존재
    - packages/core/src/queue/pipeline-worker.ts 파일에 `export function createPipelineHandler` 존재
    - packages/core/src/queue/pipeline-worker.ts 파일에 `collectForArticle` 존재
    - packages/core/src/queue/pipeline-worker.ts 파일에 `normalize-naver` 존재
    - packages/core/src/queue/pipeline-worker.ts가 200줄 이하
    - packages/core/src/queue/analysis-worker.ts 파일에 `export function createAnalysisWorker` 존재
    - packages/core/tests/worker.test.ts의 readFileSync 경로에 `pipeline-worker.ts` 포함
    - tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>worker-process.ts가 5개 파일로 분할되어 각 파일이 200줄 이하이고, 진입점은 100줄 이하이다</done>
</task>

<task type="auto">
  <name>Task 2: 전체 테스트 통과 검증 (CORE-05)</name>
  <files>packages/core/tests/worker.test.ts</files>
  <read_first>
    - packages/core/tests/worker.test.ts (분할 후 업데이트된 테스트)
    - packages/core/src/queue/worker-process.ts (분할 후 진입점)
  </read_first>
  <action>
    per D-07: 기존 테스트 통과 확인만 수행

    1. `pnpm --filter @ai-signalcraft/core test` 실행
    2. 기존 통과 9/11 파일이 동일하게 통과하는지 확인
    3. 실패하는 테스트가 있다면:
       - DB 연결 관련 실패(SASL 에러)는 기존 문제이므로 무시
       - 리팩토링으로 인한 새로운 실패가 있으면 즉시 수정
    4. `npx tsc --noEmit -p packages/core/tsconfig.json` TypeScript 컴파일 최종 확인
    5. 전체 프로젝트 빌드: `pnpm build` (모든 패키지의 빌드가 성공하는지)

    통과 기준:
    - tests/normalize.test.ts: 5 tests pass
    - tests/queue.test.ts: 6 tests pass
    - tests/analysis-modules-stage2.test.ts: 14 tests pass
    - tests/analysis-modules-stage1.test.ts: 21 tests pass
    - tests/advn-schema.test.ts: 15 tests pass
    - tests/db.test.ts: 6 tests pass
    - tests/analysis-schema.test.ts: 8 tests pass
    - tests/dedup.test.ts: 5 tests pass
    - tests/worker.test.ts: 7 tests pass
    - tests/analysis-runner.test.ts: 4 pass, 4 fail (DB 미연결)
    - tests/report.test.ts: 5 pass, 2 fail (DB 미연결)
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test 출력에 `9 passed` (테스트 파일 기준) 표시
    - pnpm test 출력에 `96 passed` (개별 테스트 기준) 이상 표시
    - pnpm test 출력에 기존 DB 관련 실패(2 failed 파일) 외 새로운 실패 없음
    - npx tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>리팩토링 후 기존 11개 테스트 파일 중 9개가 동일하게 통과하고 96개 개별 테스트가 통과한다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/core/tsconfig.json` 성공
- `wc -l packages/core/src/queue/worker-process.ts` < 100
- `wc -l packages/core/src/queue/pipeline-worker.ts` < 200
- `pnpm --filter @ai-signalcraft/core test` 9/11 파일 통과 (기존과 동일)
- `pnpm build` 전체 빌드 성공
</verification>

<success_criteria>
1. worker-process.ts가 100줄 이하 (진입점+shutdown만)
2. 5개 분할 파일이 모두 200줄 이하
3. 기존 96개 테스트 동일하게 통과 (CORE-05)
4. TypeScript 컴파일 에러 없음
5. 전체 프로젝트 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/08-core/08-03-SUMMARY.md`
</output>
