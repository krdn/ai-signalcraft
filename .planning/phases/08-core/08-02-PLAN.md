---
phase: 08-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/analysis/runner.ts
  - packages/core/src/analysis/pipeline-orchestrator.ts
  - packages/core/src/analysis/index.ts
autonomous: true
requirements: [CORE-03]
must_haves:
  truths:
    - "runner.ts에 runModule 함수와 Stage 상수만 남아 있다"
    - "pipeline-orchestrator.ts에 runAnalysisPipeline과 buildResult가 분리되어 있다"
    - "기존 import 경로(@ai-signalcraft/core의 runAnalysisPipeline, runModule)가 유지된다"
    - "Stage 0~4 오케스트레이션 로직이 pipeline-orchestrator.ts에서 동작한다"
  artifacts:
    - path: "packages/core/src/analysis/runner.ts"
      provides: "runModule 함수 + STAGE 상수 export"
      exports: ["runModule", "STAGE1_MODULES", "STAGE2_MODULES", "STAGE4_PARALLEL", "STAGE4_SEQUENTIAL"]
    - path: "packages/core/src/analysis/pipeline-orchestrator.ts"
      provides: "runAnalysisPipeline 오케스트레이션 + buildResult 헬퍼"
      exports: ["runAnalysisPipeline"]
  key_links:
    - from: "packages/core/src/analysis/pipeline-orchestrator.ts"
      to: "packages/core/src/analysis/runner.ts"
      via: "runModule, STAGE 상수 import"
      pattern: "import.*runModule.*from.*runner"
    - from: "packages/core/src/queue/worker-process.ts"
      to: "packages/core/src/analysis/runner.ts"
      via: "runAnalysisPipeline import (barrel export 경유)"
      pattern: "import.*runAnalysisPipeline"
---

<objective>
runner.ts를 단일 모듈 실행과 오케스트레이션으로 분리

Purpose: CORE-03 -- runner.ts(383줄)의 runModule(단일 실행)과 runAnalysisPipeline(Stage 0~4 오케스트레이션)을 분리하여 각 파일의 책임을 명확히 한다
Output: runner.ts(~120줄, runModule + Stage 상수) + pipeline-orchestrator.ts(~270줄, runAnalysisPipeline + buildResult)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core/08-CONTEXT.md

<interfaces>
<!-- runner.ts의 핵심 export -- pipeline-orchestrator.ts에서 import 필요 -->
From packages/core/src/analysis/runner.ts:
```typescript
export async function runModule<T>(
  module: AnalysisModule<T>,
  input: AnalysisInput,
  priorResults?: Record<string, unknown>,
): Promise<AnalysisModuleResult<T>>

export const STAGE1_MODULES: AnalysisModule[]
export const STAGE2_MODULES: AnalysisModule[]
export const STAGE4_PARALLEL: AnalysisModule[]
export const STAGE4_SEQUENTIAL: AnalysisModule[]
```

From packages/core/src/analysis/types.ts:
```typescript
export type AnalysisModule<T = unknown>
export type AnalysisInput
export type AnalysisModuleResult<T = unknown>
```

<!-- worker-process.ts에서의 기존 import 경로 (변경 불가) -->
From packages/core/src/queue/worker-process.ts:
```typescript
import { runAnalysisPipeline } from '../analysis/runner';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pipeline-orchestrator.ts 생성 (runAnalysisPipeline + buildResult 이동)</name>
  <files>packages/core/src/analysis/pipeline-orchestrator.ts, packages/core/src/analysis/runner.ts</files>
  <read_first>
    - packages/core/src/analysis/runner.ts (전체 383줄 -- 분할 대상)
    - packages/core/src/analysis/types.ts (AnalysisModule, AnalysisInput, AnalysisModuleResult 타입)
    - packages/core/src/analysis/data-loader.ts (loadAnalysisInput import 확인)
    - packages/core/src/pipeline/control.ts (isPipelineCancelled, waitIfPaused 등 import 확인)
  </read_first>
  <action>
    per D-03: runner.ts를 단일 실행과 오케스트레이션으로 분리

    1. packages/core/src/analysis/pipeline-orchestrator.ts 신규 생성:
       - runner.ts의 119~383줄(runAnalysisPipeline 함수 + buildResult 헬퍼)을 이동
       - import 문 구성:
         ```typescript
         import { runModule, STAGE1_MODULES, STAGE2_MODULES, STAGE4_PARALLEL, STAGE4_SEQUENTIAL } from './runner';
         import { generateIntegratedReport } from '../report/generator';
         import { finalSummaryModule } from './modules';
         import { loadAnalysisInput } from './data-loader';
         import { persistAnalysisResult } from './persist-analysis';
         import { isPipelineCancelled, waitIfPaused, checkCostLimit, getSkippedModules } from '../pipeline/control';
         import { analyzeItems } from './item-analyzer';
         import { getDb } from '../db';
         import { collectionJobs } from '../db/schema/collections';
         import { eq } from 'drizzle-orm';
         import type { AnalysisModuleResult, AnalysisInput } from './types';
         ```
       - runAnalysisPipeline 함수를 export (시그니처 동일 유지)
       - buildResult 헬퍼를 internal로 유지 (export 불필요)

    2. packages/core/src/analysis/runner.ts 수정:
       - 남는 코드: import 문, STAGE1_MODULES~STAGE4_SEQUENTIAL 상수 4개, runModule 함수
       - 제거: runAnalysisPipeline 함수, buildResult 함수
       - 제거 후 불필요해진 import도 정리:
         - generateIntegratedReport, finalSummaryModule, loadAnalysisInput 제거
         - isPipelineCancelled, waitIfPaused, checkCostLimit, getSkippedModules 제거
         - analyzeItems, getDb, collectionJobs, eq 제거
       - 남는 import: analyzeStructured, 12개 모듈 import, persistAnalysisResult, getModuleModelConfig, AnalysisModule/AnalysisInput/AnalysisModuleResult 타입
       - runAnalysisPipeline을 pipeline-orchestrator.ts에서 re-export:
         마지막에 `export { runAnalysisPipeline } from './pipeline-orchestrator';` 추가
         -- 이렇게 하면 worker-process.ts의 `import { runAnalysisPipeline } from '../analysis/runner'` 경로가 그대로 동작

    결과 예상: runner.ts ~120줄, pipeline-orchestrator.ts ~270줄
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && npx tsc --noEmit -p packages/core/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/analysis/pipeline-orchestrator.ts 파일에 `export async function runAnalysisPipeline` 존재
    - packages/core/src/analysis/pipeline-orchestrator.ts 파일에 `async function buildResult` 존재
    - packages/core/src/analysis/pipeline-orchestrator.ts 파일에 `import { runModule` 존재
    - packages/core/src/analysis/runner.ts 파일에 `export async function runModule` 존재
    - packages/core/src/analysis/runner.ts 파일에 `export { runAnalysisPipeline } from './pipeline-orchestrator'` 존재
    - packages/core/src/analysis/runner.ts 파일에 `async function buildResult` 미포함
    - tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>runner.ts에 runModule+Stage 상수만 남고, pipeline-orchestrator.ts에 runAnalysisPipeline+buildResult가 분리되었다</done>
</task>

<task type="auto">
  <name>Task 2: barrel export 업데이트 + 테스트 통과 확인</name>
  <files>packages/core/src/analysis/index.ts</files>
  <read_first>
    - packages/core/src/analysis/index.ts (현재 barrel export)
    - packages/core/tests/analysis-runner.test.ts (runner import 패턴 확인)
    - packages/core/src/queue/worker-process.ts (runAnalysisPipeline import 확인 -- 54줄)
  </read_first>
  <action>
    per D-06: barrel export 유지

    packages/core/src/analysis/index.ts 수정:
    - 기존 `export * from './runner';` 유지 (runner.ts가 pipeline-orchestrator를 re-export하므로 runAnalysisPipeline도 노출됨)
    - 추가: `export * from './pipeline-orchestrator';` (직접 import 경로도 지원)

    테스트 통과 확인:
    - `pnpm --filter @ai-signalcraft/core test` 실행
    - analysis-runner.test.ts의 `import('../src/analysis/runner')` 경로가 runAnalysisPipeline을 re-export하므로 기존 테스트 동작 유지
    - worker-process.ts의 `import { runAnalysisPipeline } from '../analysis/runner'` 경로도 re-export로 유지
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/analysis/index.ts 파일에 `export * from './runner'` 존재
    - packages/core/src/analysis/index.ts 파일에 `export * from './pipeline-orchestrator'` 존재
    - pnpm test 출력에 `9 passed` 표시 (기존 통과 테스트 수 유지)
    - pnpm test 출력에 새로운 실패 없음 (기존 DB 관련 2개 파일 실패만 허용)
  </acceptance_criteria>
  <done>barrel export가 업데이트되어 기존 import 경로가 모두 유지되고 기존 테스트가 동일하게 통과한다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/core/tsconfig.json` 성공
- `wc -l packages/core/src/analysis/runner.ts` < 200
- `pnpm --filter @ai-signalcraft/core test` 기존 9개 파일 통과 유지
- worker-process.ts의 `import { runAnalysisPipeline } from '../analysis/runner'` 경로 유효
</verification>

<success_criteria>
1. runner.ts가 200줄 이하 (runModule + Stage 상수 + re-export만)
2. pipeline-orchestrator.ts에 runAnalysisPipeline 오케스트레이션 전체 로직 존재
3. 기존 import 경로 모두 유효 (barrel export + re-export)
4. TypeScript 컴파일 에러 없음
5. 기존 테스트 통과 수 동일
</success_criteria>

<output>
After completion, create `.planning/phases/08-core/08-02-SUMMARY.md`
</output>
