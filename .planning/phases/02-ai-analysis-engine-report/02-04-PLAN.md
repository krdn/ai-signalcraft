---
phase: 02-ai-analysis-engine-report
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - packages/core/src/analysis/runner.ts
  - packages/core/src/analysis/index.ts
  - packages/core/src/queue/flows.ts
  - packages/core/src/queue/worker-process.ts
  - packages/core/tests/analysis-runner.test.ts
autonomous: true
requirements: [ANLZ-01, ANLZ-02, ANLZ-03, ANLZ-04, DEEP-01, DEEP-02, DEEP-03, DEEP-04, DEEP-05, REPT-01, REPT-02]

must_haves:
  truths:
    - "분석 러너가 Stage1(병렬) -> Stage2(순차) -> Final Summary 3단계로 모듈을 실행한다"
    - "BullMQ Flow에 analyze 단계가 추가되어 persist 완료 후 자동으로 분석이 시작된다"
    - "분석 Worker가 analysis 큐의 작업을 처리한다"
    - "부분 실패 시에도 가용한 결과로 리포트 생성을 계속한다"
    - "각 모듈 결과와 usage가 analysis_results 테이블에 저장된다"
  artifacts:
    - path: "packages/core/src/analysis/runner.ts"
      provides: "3단계 분석 실행 오케스트레이터"
      exports: ["runAnalysisPipeline"]
    - path: "packages/core/src/queue/flows.ts"
      provides: "triggerAnalysis 함수 (persist 후 분석 트리거)"
      exports: ["triggerCollection", "triggerAnalysis"]
    - path: "packages/core/src/queue/worker-process.ts"
      provides: "분석 Worker 핸들러가 추가된 Worker 프로세스"
  key_links:
    - from: "packages/core/src/analysis/runner.ts"
      to: "packages/ai-gateway/src/gateway.ts"
      via: "analyzeStructured/analyzeText 호출"
      pattern: "analyzeStructured|analyzeText"
    - from: "packages/core/src/analysis/runner.ts"
      to: "packages/core/src/analysis/persist-analysis.ts"
      via: "persistAnalysisResult 호출"
      pattern: "persistAnalysisResult"
    - from: "packages/core/src/queue/worker-process.ts"
      to: "packages/core/src/analysis/runner.ts"
      via: "runAnalysisPipeline 호출"
      pattern: "runAnalysisPipeline"
    - from: "packages/core/src/queue/flows.ts"
      to: "analysis 큐"
      via: "FlowProducer.add"
      pattern: "triggerAnalysis"
---

<objective>
분석 실행 러너(runner.ts)를 구현하고, BullMQ Flow를 확장하여 persist 완료 후 자동 분석을 트리거하며, Worker에 분석 핸들러를 추가한다.

Purpose: 8개 분석 모듈을 D-10 병렬/순차 패턴으로 오케스트레이션하고, 수집 파이프라인과 분석 파이프라인을 연결한다 (per D-09).
Output: runner.ts, 확장된 flows.ts, 확장된 worker-process.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-analysis-engine-report/02-CONTEXT.md
@.planning/phases/02-ai-analysis-engine-report/02-RESEARCH.md
@.planning/phases/02-ai-analysis-engine-report/02-01-SUMMARY.md
@.planning/phases/02-ai-analysis-engine-report/02-02-SUMMARY.md
@.planning/phases/02-ai-analysis-engine-report/02-03-SUMMARY.md

<interfaces>
<!-- Plan 01~03에서 생성된 핵심 인터페이스 -->

From packages/core/src/analysis/types.ts:
```typescript
export interface AnalysisModule<T = unknown> {
  readonly name: string;
  readonly provider: AIProvider;
  readonly model: string;
  readonly schema: z.ZodType<T>;
  buildPrompt(data: AnalysisInput): string;
  buildSystemPrompt(): string;
  buildPromptWithContext?(data: AnalysisInput, priorResults: Record<string, unknown>): string;
}
export interface AnalysisInput { jobId, keyword, articles, videos, comments, dateRange }
export interface AnalysisModuleResult<T = unknown> { module, status, result?, usage?, errorMessage? }
export const MODULE_MODEL_MAP: Record<string, { provider, model }>;
```

From packages/core/src/analysis/modules/index.ts:
```typescript
export { macroViewModule } from './macro-view';
export { segmentationModule } from './segmentation';
export { sentimentFramingModule } from './sentiment-framing';
export { messageImpactModule } from './message-impact';
export { riskMapModule } from './risk-map';
export { opportunityModule } from './opportunity';
export { strategyModule } from './strategy';
export { finalSummaryModule } from './final-summary';
```

From packages/core/src/analysis/persist-analysis.ts:
```typescript
export async function persistAnalysisResult(data: { jobId, module, status, result?, usage?, errorMessage? }): Promise<...>;
export async function persistAnalysisReport(data: { jobId, title, markdownContent, oneLiner?, metadata? }): Promise<...>;
```

From packages/core/src/analysis/data-loader.ts:
```typescript
export async function loadAnalysisInput(jobId: number): Promise<AnalysisInput>;
```

From packages/ai-gateway/src/gateway.ts:
```typescript
export async function analyzeStructured<T>(prompt, schema, options?): Promise<{ object, usage, finishReason }>;
export async function analyzeText(prompt, options?): Promise<{ text, usage, ... }>;
```

From packages/core/src/queue/flows.ts:
```typescript
export async function triggerCollection(params: CollectionTrigger, dbJobId: number): Promise<...>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 분석 실행 러너 (3단계 병렬/순차 오케스트레이션)</name>
  <files>
    packages/core/src/analysis/runner.ts,
    packages/core/src/analysis/index.ts,
    packages/core/tests/analysis-runner.test.ts
  </files>
  <read_first>
    packages/core/src/analysis/types.ts,
    packages/core/src/analysis/modules/index.ts,
    packages/core/src/analysis/data-loader.ts,
    packages/core/src/analysis/persist-analysis.ts,
    packages/ai-gateway/src/gateway.ts,
    packages/core/src/analysis/index.ts
  </read_first>
  <behavior>
    - Test: runAnalysisPipeline 함수가 export된다
    - Test: runModule 함수가 export된다
    - Test: STAGE1_MODULES 배열이 4개 모듈을 포함한다
    - Test: STAGE2_MODULES 배열이 3개 모듈(risk-map, opportunity, strategy)을 포함한다
  </behavior>
  <action>
    **packages/core/src/analysis/runner.ts 생성 (per D-10):**

    ```typescript
    import { analyzeStructured } from '@ai-signalcraft/ai-gateway';
    import { macroViewModule, segmentationModule, sentimentFramingModule, messageImpactModule,
             riskMapModule, opportunityModule, strategyModule, finalSummaryModule } from './modules';
    import { loadAnalysisInput } from './data-loader';
    import { persistAnalysisResult } from './persist-analysis';
    import type { AnalysisModule, AnalysisInput, AnalysisModuleResult } from './types';

    export const STAGE1_MODULES = [macroViewModule, segmentationModule, sentimentFramingModule, messageImpactModule];
    export const STAGE2_MODULES = [riskMapModule, opportunityModule, strategyModule];

    // 단일 모듈 실행 (AI Gateway 호출 + DB 저장)
    export async function runModule<T>(
      module: AnalysisModule<T>,
      input: AnalysisInput,
      priorResults?: Record<string, unknown>,
    ): Promise<AnalysisModuleResult<T>> {
      try {
        const prompt = (priorResults && module.buildPromptWithContext)
          ? module.buildPromptWithContext(input, priorResults)
          : module.buildPrompt(input);

        const result = await analyzeStructured(
          prompt,
          module.schema,
          {
            provider: module.provider,
            model: module.model,
            systemPrompt: module.buildSystemPrompt(),
            maxOutputTokens: 8192,
          },
        );

        const moduleResult: AnalysisModuleResult<T> = {
          module: module.name,
          status: 'completed',
          result: result.object,
          usage: {
            // AI SDK v6 usage 필드명 확인 필요 -- promptTokens/completionTokens 또는 inputTokens/outputTokens
            inputTokens: (result.usage as any)?.promptTokens ?? (result.usage as any)?.inputTokens ?? 0,
            outputTokens: (result.usage as any)?.completionTokens ?? (result.usage as any)?.outputTokens ?? 0,
            totalTokens: (result.usage as any)?.totalTokens ?? 0,
            provider: module.provider,
            model: module.model,
          },
        };

        // DB에 결과 저장
        await persistAnalysisResult({
          jobId: input.jobId,
          module: module.name,
          status: 'completed',
          result: moduleResult.result,
          usage: moduleResult.usage,
        });

        return moduleResult;
      } catch (error) {
        const errorMessage = error instanceof Error ? error.message : String(error);
        await persistAnalysisResult({
          jobId: input.jobId,
          module: module.name,
          status: 'failed',
          errorMessage,
        });
        return { module: module.name, status: 'failed', errorMessage };
      }
    }

    // 전체 분석 파이프라인 실행 (D-10: 3단계)
    export async function runAnalysisPipeline(jobId: number): Promise<{
      results: Record<string, AnalysisModuleResult>;
      completedModules: string[];
      failedModules: string[];
    }> {
      const input = await loadAnalysisInput(jobId);
      const allResults: Record<string, AnalysisModuleResult> = {};

      // Stage 1: 병렬 실행 (모듈1~4, 독립)
      const stage1Settled = await Promise.allSettled(
        STAGE1_MODULES.map(m => runModule(m, input)),
      );

      // Stage 1 결과 수집
      const priorResults: Record<string, unknown> = {};
      for (const settled of stage1Settled) {
        if (settled.status === 'fulfilled') {
          allResults[settled.value.module] = settled.value;
          if (settled.value.status === 'completed') {
            priorResults[settled.value.module] = settled.value.result;
          }
        }
      }

      // Stage 2: 순차 실행 (모듈5~7, Stage 1 결과 의존)
      for (const module of STAGE2_MODULES) {
        const result = await runModule(module, input, priorResults);
        allResults[result.module] = result;
        if (result.status === 'completed') {
          priorResults[result.module] = result.result;
        }
      }

      // Stage 3: 최종 요약 (모듈8, 모든 선행 결과 참조)
      const finalResult = await runModule(finalSummaryModule, input, priorResults);
      allResults[finalResult.module] = finalResult;

      const completedModules = Object.values(allResults)
        .filter(r => r.status === 'completed').map(r => r.module);
      const failedModules = Object.values(allResults)
        .filter(r => r.status === 'failed').map(r => r.module);

      return { results: allResults, completedModules, failedModules };
    }
    ```

    **analysis/index.ts 수정:**
    `export * from './runner';` 추가.
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/analysis/runner.ts contains `export async function runAnalysisPipeline`
    - packages/core/src/analysis/runner.ts contains `export async function runModule`
    - packages/core/src/analysis/runner.ts contains `export const STAGE1_MODULES`
    - packages/core/src/analysis/runner.ts contains `export const STAGE2_MODULES`
    - packages/core/src/analysis/runner.ts contains `Promise.allSettled` (Stage 1 병렬)
    - packages/core/src/analysis/runner.ts contains `persistAnalysisResult` (결과 DB 저장)
    - packages/core/src/analysis/index.ts contains `export * from './runner'`
    - `pnpm -r build` exits 0
    - `pnpm --filter @ai-signalcraft/core test` exits 0
  </acceptance_criteria>
  <done>분석 러너가 Stage1(병렬) -> Stage2(순차) -> Final Summary 3단계로 실행되고, 각 결과가 DB에 저장되며, 부분 실패를 허용함</done>
</task>

<task type="auto">
  <name>Task 2: BullMQ Flow 확장 + Worker 분석 핸들러 추가</name>
  <files>
    packages/core/src/queue/flows.ts,
    packages/core/src/queue/worker-process.ts
  </files>
  <read_first>
    packages/core/src/queue/flows.ts,
    packages/core/src/queue/worker-process.ts,
    packages/core/src/queue/workers.ts,
    packages/core/src/queue/connection.ts,
    packages/core/src/analysis/runner.ts,
    packages/core/src/analysis/types.ts
  </read_first>
  <action>
    **1. packages/core/src/queue/flows.ts 확장 (per D-09):**

    `triggerAnalysis` 함수를 추가한다. persist 완료 후 호출되어 분석 파이프라인을 시작:

    ```typescript
    // D-09: 분석 파이프라인 트리거 -- persist 완료 후 호출
    export async function triggerAnalysis(dbJobId: number, keyword: string) {
      const flow = await getFlowProducer().add({
        name: 'generate-report',
        queueName: 'analysis',
        data: { dbJobId, keyword, stage: 'report' },
        children: [
          {
            name: 'analyze-stage2',
            queueName: 'analysis',
            data: { dbJobId, keyword, stage: 2 },
            children: [
              {
                name: 'analyze-stage1',
                queueName: 'analysis',
                data: { dbJobId, keyword, stage: 1 },
              },
            ],
          },
        ],
      });
      return flow;
    }
    ```

    **주의:** BullMQ Flow에서 children이 먼저 실행되므로 analyze-stage1 -> analyze-stage2 -> generate-report 순서 (Pitfall 3 대응). 단, 실제 3단계 오케스트레이션은 runner.ts의 `runAnalysisPipeline`이 담당하므로 Flow는 전체 분석 작업의 시작/완료만 관리. 따라서 **단순 구조를 권장**: analysis 큐에 단일 작업으로 `runAnalysisPipeline`을 호출하는 것이 더 적합할 수 있음. runner.ts가 이미 내부적으로 병렬/순차를 관리하므로, BullMQ Flow는 `triggerAnalysis -> 단일 analyze 작업`으로 단순화:

    ```typescript
    export async function triggerAnalysis(dbJobId: number, keyword: string) {
      const flow = await getFlowProducer().add({
        name: 'run-analysis',
        queueName: 'analysis',
        data: { dbJobId, keyword },
      });
      return flow;
    }
    ```

    **2. packages/core/src/queue/worker-process.ts 확장:**

    - 기존 import에 `runAnalysisPipeline` 추가
    - 기존 import에 `triggerAnalysis` 추가
    - analysis 큐 Worker 생성:

    ```typescript
    import { runAnalysisPipeline } from '../analysis/runner';
    import { triggerAnalysis } from './flows';

    // 분석 Worker -- analysis 큐
    const analysisWorker = new Worker('analysis', async (job: Job) => {
      const { dbJobId, keyword } = job.data;

      if (job.name === 'run-analysis') {
        const result = await runAnalysisPipeline(dbJobId);
        await job.updateProgress({
          completedModules: result.completedModules,
          failedModules: result.failedModules,
        });
        return result;
      }
    }, { connection: redisConnection });
    ```

    - persist 핸들러에서 분석 자동 트리거 추가 (D-09):

    ```typescript
    // persist 핸들러 끝에 (기존 updateJobProgress 후):
    // D-09: 수집 완료 후 자동 분석 트리거
    const keyword = job.data.keyword;
    if (keyword) {
      await triggerAnalysis(dbJobId, keyword);
    }
    ```

    - Worker에 `createCollectorWorker` 대신 직접 `Worker` 사용하거나, 기존 `workers.ts`에 `createAnalysisWorker` 팩토리 추가.

    - Graceful shutdown에 analysisWorker.close() 추가.
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/queue/flows.ts contains `export async function triggerAnalysis`
    - packages/core/src/queue/worker-process.ts contains `runAnalysisPipeline`
    - packages/core/src/queue/worker-process.ts contains `analysis` (큐 이름)
    - packages/core/src/queue/worker-process.ts contains `triggerAnalysis` (persist 후 자동 트리거)
    - `pnpm -r build` exits 0
  </acceptance_criteria>
  <done>BullMQ Flow에 triggerAnalysis가 추가되고, Worker가 analysis 큐를 처리하며, persist 완료 후 자동으로 분석이 트리거됨</done>
</task>

</tasks>

<verification>
- `pnpm -r build` 전체 빌드 통과
- `pnpm --filter @ai-signalcraft/core test` 테스트 통과
- triggerAnalysis가 flows.ts에 export됨
- worker-process.ts에 analysisWorker가 존재
- runner.ts에 runAnalysisPipeline이 3단계 실행을 구현
</verification>

<success_criteria>
1. `pnpm -r build` exits 0
2. `pnpm --filter @ai-signalcraft/core test` exits 0
3. 수집 파이프라인 완료 후 자동으로 분석 파이프라인이 트리거됨 (코드 경로 확인)
4. 분석 러너가 Stage1 병렬 -> Stage2 순차 -> Final Summary 순서로 실행
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-analysis-engine-report/02-04-SUMMARY.md`
</output>
