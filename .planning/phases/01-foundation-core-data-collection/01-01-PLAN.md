---
phase: 01-foundation-core-data-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .env.example
  - .gitignore
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/vitest.config.ts
  - packages/core/src/db/schema/collections.ts
  - packages/core/src/db/schema/index.ts
  - packages/core/src/db/index.ts
  - packages/core/src/index.ts
  - packages/core/drizzle.config.ts
  - packages/collectors/package.json
  - packages/collectors/tsconfig.json
  - packages/collectors/vitest.config.ts
  - packages/collectors/src/index.ts
  - packages/ai-gateway/package.json
  - packages/ai-gateway/tsconfig.json
  - packages/ai-gateway/src/index.ts
  - docker/docker-compose.dev.yml
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "pnpm -r build 명령이 모든 패키지에서 성공한다"
    - "운영 서버(192.168.0.5:5436) PostgreSQL에 Drizzle 스키마가 push되어 테이블이 생성된다"
    - "Vitest가 packages/core와 packages/collectors에서 실행된다"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "모노리포 워크스페이스 정의"
      contains: "packages/*"
    - path: "packages/core/src/db/schema/collections.ts"
      provides: "collection_jobs, articles, videos, comments 테이블 스키마"
      contains: "collectionJobs"
    - path: "packages/core/src/db/index.ts"
      provides: "Drizzle DB 클라이언트 export"
      exports: ["db"]
    - path: "packages/core/drizzle.config.ts"
      provides: "Drizzle Kit 설정"
      contains: "defineConfig"
  key_links:
    - from: "packages/core/src/db/index.ts"
      to: "packages/core/src/db/schema/collections.ts"
      via: "import schema"
      pattern: "import.*schema"
---

<objective>
pnpm 모노리포 프로젝트를 스캐폴딩하고 PostgreSQL DB 스키마를 설계/배포한다.

Purpose: 모든 후속 Plan이 의존하는 프로젝트 기반 인프라를 구축한다.
Output: 빌드 가능한 모노리포 구조 + 운영 DB에 테이블 생성 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-data-collection/01-CONTEXT.md
@.planning/phases/01-foundation-core-data-collection/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pnpm 모노리포 스캐폴딩 + Next.js 앱 + 패키지 구조 생성</name>
  <files>
    pnpm-workspace.yaml,
    package.json,
    tsconfig.base.json,
    .env.example,
    .gitignore,
    apps/web/package.json,
    apps/web/next.config.ts,
    apps/web/tsconfig.json,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    packages/core/package.json,
    packages/core/tsconfig.json,
    packages/core/vitest.config.ts,
    packages/core/src/index.ts,
    packages/collectors/package.json,
    packages/collectors/tsconfig.json,
    packages/collectors/vitest.config.ts,
    packages/collectors/src/index.ts,
    packages/ai-gateway/package.json,
    packages/ai-gateway/tsconfig.json,
    packages/ai-gateway/src/index.ts,
    docker/docker-compose.dev.yml
  </files>
  <read_first>
    CLAUDE.md,
    .planning/phases/01-foundation-core-data-collection/01-RESEARCH.md
  </read_first>
  <action>
1. 루트 `pnpm-workspace.yaml` 생성:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

2. 루트 `package.json` 생성:
- name: "ai-signalcraft"
- private: true
- scripts: { "build": "pnpm -r build", "dev": "pnpm --filter @ai-signalcraft/web dev", "test": "pnpm -r test", "db:push": "pnpm --filter @ai-signalcraft/core db:push", "db:studio": "pnpm --filter @ai-signalcraft/core db:studio", "worker": "pnpm --filter @ai-signalcraft/core worker" }
- devDependencies: { "typescript": "^5.8.0", "vitest": "^3.0.0" }

3. `tsconfig.base.json` 생성:
- compilerOptions: { target: "ES2022", module: "ESNext", moduleResolution: "bundler", strict: true, esModuleInterop: true, skipLibCheck: true, forceConsistentCasingInFileNames: true, declaration: true, declarationMap: true, sourceMap: true, outDir: "dist", rootDir: "src", baseUrl: ".", resolveJsonModule: true }

4. `.env.example` 생성:
```
# Database (운영 서버 192.168.0.5)
DATABASE_URL=postgresql://postgres:password@192.168.0.5:5436/ai_signalcraft

# Redis (BullMQ)
REDIS_URL=redis://192.168.0.5:6383

# YouTube Data API
YOUTUBE_API_KEY=your_youtube_api_key

# AI SDK
ANTHROPIC_API_KEY=your_anthropic_api_key
OPENAI_API_KEY=your_openai_api_key
```

5. `.gitignore`에 추가: node_modules, dist, .env, .env.local, .next, *.tsbuildinfo

6. `apps/web/` Next.js 앱:
- `pnpm add next@latest react@latest react-dom@latest` 실행
- package.json: name "@ai-signalcraft/web", scripts: { dev: "next dev", build: "next build", start: "next start" }
- next.config.ts: transpilePackages 설정으로 @ai-signalcraft/core, @ai-signalcraft/collectors 포함
- tsconfig.json: extends "../../tsconfig.base.json", paths에 "@/*": ["./src/*"] 설정
- src/app/layout.tsx: 기본 RootLayout (metadata: title "AI SignalCraft", description "여론 분석 대시보드")
- src/app/page.tsx: "AI SignalCraft" 제목만 표시하는 플레이스홀더 페이지

7. `packages/core/` 패키지:
- `pnpm add drizzle-orm pg ioredis bullmq zod` 실행
- `pnpm add -D drizzle-kit @types/pg tsx` 실행
- package.json: name "@ai-signalcraft/core", main "src/index.ts", types "src/index.ts", scripts: { "build": "tsc", "test": "vitest run", "db:push": "drizzle-kit push", "db:studio": "drizzle-kit studio", "worker": "tsx watch src/queue/worker.ts" }
- tsconfig.json: extends "../../tsconfig.base.json"
- vitest.config.ts: defineConfig with test: { globals: true }
- src/index.ts: placeholder export

8. `packages/collectors/` 패키지:
- `pnpm add playwright cheerio googleapis` 실행
- `pnpm add -D tsx` 실행
- package.json: name "@ai-signalcraft/collectors", main "src/index.ts", scripts: { "build": "tsc", "test": "vitest run" }
- tsconfig.json: extends "../../tsconfig.base.json"
- vitest.config.ts: defineConfig with test: { globals: true }
- src/index.ts: placeholder export

9. `packages/ai-gateway/` 패키지:
- `pnpm add ai @ai-sdk/anthropic @ai-sdk/openai zod` 실행
- package.json: name "@ai-signalcraft/ai-gateway", main "src/index.ts", scripts: { "build": "tsc", "test": "vitest run" }
- tsconfig.json: extends "../../tsconfig.base.json"
- src/index.ts: placeholder export (Phase 1에서는 골격만)

10. `docker/docker-compose.dev.yml` 생성:
```yaml
services:
  postgres:
    image: postgres:16
    container_name: ais-dev-postgres
    ports:
      - "5437:5432"
    environment:
      POSTGRES_DB: ai_signalcraft_dev
      POSTGRES_PASSWORD: devpassword
    volumes:
      - ais-dev-pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: ais-dev-redis
    ports:
      - "6384:6379"
    volumes:
      - ais-dev-redis:/data

volumes:
  ais-dev-pgdata:
  ais-dev-redis:
```

11. 루트에서 `pnpm install` 실행

RESEARCH.md 참고: CLAUDE.md에 Next.js 15.x로 명시되어 있으나 npm latest는 16.x -- 새 프로젝트이므로 `next@latest`로 설치하여 최신 버전 사용. CLAUDE.md의 버전 표기는 추후 업데이트.
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml 파일이 존재하고 `packages:` 섹션에 `"apps/*"`와 `"packages/*"`를 포함
    - packages/core/package.json에 `"name": "@ai-signalcraft/core"` 존재
    - packages/collectors/package.json에 `"name": "@ai-signalcraft/collectors"` 존재
    - packages/ai-gateway/package.json에 `"name": "@ai-signalcraft/ai-gateway"` 존재
    - apps/web/package.json에 `"name": "@ai-signalcraft/web"` 존재
    - tsconfig.base.json에 `"strict": true` 존재
    - .env.example에 DATABASE_URL, REDIS_URL, YOUTUBE_API_KEY, ANTHROPIC_API_KEY, OPENAI_API_KEY 존재
    - docker/docker-compose.dev.yml에 postgres와 redis 서비스 정의 존재
    - packages/core/vitest.config.ts 파일 존재
    - packages/collectors/vitest.config.ts 파일 존재
    - `pnpm -r build` 명령이 exit code 0으로 완료
  </acceptance_criteria>
  <done>모노리포 구조가 생성되고 모든 패키지의 TypeScript 빌드가 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: Drizzle ORM DB 스키마 정의 + 운영 서버 DB 연결 확인</name>
  <files>
    packages/core/src/db/schema/collections.ts,
    packages/core/src/db/schema/index.ts,
    packages/core/src/db/index.ts,
    packages/core/drizzle.config.ts,
    packages/core/src/index.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-core-data-collection/01-RESEARCH.md,
    docs/prompt.md,
    packages/core/package.json
  </read_first>
  <action>
1. `packages/core/src/db/schema/collections.ts` 생성 (per D-06, D-07):

```typescript
import { pgTable, text, timestamp, integer, jsonb, uniqueIndex } from 'drizzle-orm/pg-core';

// 수집 작업 (D-06: 소스별 상세 추적)
export const collectionJobs = pgTable('collection_jobs', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  keyword: text('keyword').notNull(),
  startDate: timestamp('start_date').notNull(),
  endDate: timestamp('end_date').notNull(),
  status: text('status', {
    enum: ['pending', 'running', 'completed', 'partial_failure', 'failed']
  }).notNull().default('pending'),
  progress: jsonb('progress').$type<{
    naver: { status: string; articles: number; comments: number };
    youtube: { status: string; videos: number; comments: number };
  }>(),
  limits: jsonb('limits').$type<{
    naverArticles: number;
    youtubeVideos: number;
    commentsPerItem: number;
  }>(),
  errorDetails: jsonb('error_details').$type<Record<string, string>>(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
});

// 뉴스 기사 (D-07: URL 기반 중복 제거)
export const articles = pgTable('articles', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  jobId: integer('job_id').references(() => collectionJobs.id),
  source: text('source').notNull(),
  sourceId: text('source_id').notNull(),
  url: text('url').notNull(),
  title: text('title').notNull(),
  content: text('content'),
  author: text('author'),
  publisher: text('publisher'),
  publishedAt: timestamp('published_at'),
  rawData: jsonb('raw_data'),
  collectedAt: timestamp('collected_at').defaultNow().notNull(),
}, (table) => [
  uniqueIndex('articles_source_id_idx').on(table.source, table.sourceId),
]);

// 영상 (유튜브)
export const videos = pgTable('videos', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  jobId: integer('job_id').references(() => collectionJobs.id),
  source: text('source').notNull(),
  sourceId: text('source_id').notNull(),
  url: text('url').notNull(),
  title: text('title').notNull(),
  description: text('description'),
  channelId: text('channel_id'),
  channelTitle: text('channel_title'),
  viewCount: integer('view_count'),
  likeCount: integer('like_count'),
  commentCount: integer('comment_count'),
  publishedAt: timestamp('published_at'),
  rawData: jsonb('raw_data'),
  collectedAt: timestamp('collected_at').defaultNow().notNull(),
}, (table) => [
  uniqueIndex('videos_source_id_idx').on(table.source, table.sourceId),
]);

// 댓글 (네이버 + 유튜브 통합, D-07)
export const comments = pgTable('comments', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  jobId: integer('job_id').references(() => collectionJobs.id),
  source: text('source').notNull(),
  sourceId: text('source_id').notNull(),
  parentId: text('parent_id'),
  articleId: integer('article_id').references(() => articles.id),
  videoId: integer('video_id').references(() => videos.id),
  content: text('content').notNull(),
  author: text('author'),
  likeCount: integer('like_count').default(0),
  dislikeCount: integer('dislike_count').default(0),
  publishedAt: timestamp('published_at'),
  rawData: jsonb('raw_data'),
  collectedAt: timestamp('collected_at').defaultNow().notNull(),
}, (table) => [
  uniqueIndex('comments_source_id_idx').on(table.source, table.sourceId),
]);
```

2. `packages/core/src/db/schema/index.ts`:
```typescript
export * from './collections';
```

3. `packages/core/src/db/index.ts`:
```typescript
import { drizzle } from 'drizzle-orm/node-postgres';
import pg from 'pg';
import * as schema from './schema';

const pool = new pg.Pool({
  connectionString: process.env.DATABASE_URL,
});

export const db = drizzle(pool, { schema });
export type Database = typeof db;
```

4. `packages/core/drizzle.config.ts`:
```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/db/schema/index.ts',
  out: './src/db/migrations',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
});
```

5. `packages/core/src/index.ts` 업데이트:
```typescript
export { db } from './db';
export type { Database } from './db';
export * from './db/schema';
```

6. `.env` 파일 생성 (gitignore됨):
```
DATABASE_URL=postgresql://postgres:password@192.168.0.5:5436/ai_signalcraft
```
실제 비밀번호는 운영 서버 설정에 따라 조정 필요.

7. Drizzle 스키마를 운영 서버 DB에 push:
```bash
cd packages/core && pnpm db:push
```
운영 서버 DB(`ais-postgres` 컨테이너, 포트 5436)에 연결하여 테이블 생성.
만약 DB명이 `ai_afterschool`이면 `ai_signalcraft`로 변경하거나 새 DB 생성 필요 -- RESEARCH.md에서 기존 컨테이너 확인됨.
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft/packages/core && npx drizzle-kit push 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/db/schema/collections.ts에 `collectionJobs`, `articles`, `videos`, `comments` 4개 테이블 정의 존재
    - packages/core/src/db/schema/collections.ts에 `uniqueIndex('articles_source_id_idx')` 존재
    - packages/core/src/db/schema/collections.ts에 `uniqueIndex('comments_source_id_idx')` 존재
    - packages/core/src/db/schema/collections.ts에 `generatedAlwaysAsIdentity()` 패턴 사용
    - packages/core/src/db/schema/collections.ts에 `status` 컬럼이 enum으로 `'pending', 'running', 'completed', 'partial_failure', 'failed'` 포함
    - packages/core/src/db/index.ts에 `export const db` 존재
    - packages/core/drizzle.config.ts에 `defineConfig` 존재하고 dialect이 `'postgresql'`
    - packages/core/src/index.ts에서 db와 schema를 re-export
    - drizzle-kit push 명령 실행 시 에러 없음 (DB 연결 실패 시 checkpoint:human-action으로 전환)
  </acceptance_criteria>
  <done>Drizzle 스키마 4개 테이블(collection_jobs, articles, videos, comments)이 정의되고 운영 서버 DB에 push 완료</done>
</task>

</tasks>

<verification>
- `pnpm -r build` 성공
- packages/core, packages/collectors, packages/ai-gateway, apps/web 4개 패키지가 pnpm workspace에 등록
- DB 스키마 4개 테이블이 운영 서버 PostgreSQL에 존재
- Vitest 설정 파일이 core, collectors 패키지에 존재
</verification>

<success_criteria>
- 모든 패키지 TypeScript 빌드 성공 (exit code 0)
- Drizzle 스키마 push 성공하여 collection_jobs, articles, videos, comments 테이블 존재
- .env.example에 모든 필수 환경변수 문서화
- docker-compose.dev.yml로 로컬 개발 환경 구성 가능
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-data-collection/01-01-SUMMARY.md`
</output>
