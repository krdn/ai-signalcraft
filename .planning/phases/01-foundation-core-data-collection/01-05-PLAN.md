---
phase: 01-foundation-core-data-collection
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - packages/core/src/queue/worker-process.ts
  - packages/core/src/pipeline/normalize.ts
  - packages/core/src/pipeline/persist.ts
  - packages/core/src/pipeline/index.ts
  - packages/core/src/index.ts
  - packages/core/tests/dedup.test.ts
  - packages/core/tests/normalize.test.ts
  - scripts/trigger-collection.ts
autonomous: false
requirements:
  - COLL-10

must_haves:
  truths:
    - "수집된 데이터가 정규화되어 articles/videos/comments 테이블에 DB upsert로 저장된다"
    - "동일 기사 URL이나 유튜브 댓글 ID로 중복 수집 시 기존 데이터가 업데이트되고 중복 생성되지 않는다"
    - "CLI 스크립트로 키워드를 입력하면 전체 파이프라인(collect -> normalize -> persist)이 실행된다"
    - "소스별 부분 실패 시 성공한 소스의 데이터는 정상 저장된다"
  artifacts:
    - path: "packages/core/src/pipeline/normalize.ts"
      provides: "수집 데이터를 DB 스키마에 맞게 정규화"
      exports: ["normalizeNaverArticle", "normalizeNaverComment", "normalizeYoutubeVideo", "normalizeYoutubeComment"]
    - path: "packages/core/src/pipeline/persist.ts"
      provides: "정규화된 데이터를 DB에 upsert"
      exports: ["persistArticles", "persistVideos", "persistComments"]
    - path: "packages/core/src/queue/worker-process.ts"
      provides: "BullMQ Worker 실행 프로세스"
      contains: "createCollectorWorker"
    - path: "scripts/trigger-collection.ts"
      provides: "수집 트리거 CLI 스크립트"
      contains: "triggerCollection"
  key_links:
    - from: "packages/core/src/pipeline/persist.ts"
      to: "packages/core/src/db/index.ts"
      via: "db import for upsert"
      pattern: "import.*db.*from.*db"
    - from: "packages/core/src/pipeline/persist.ts"
      to: "packages/core/src/db/schema/collections.ts"
      via: "table schema for onConflictDoUpdate"
      pattern: "onConflictDoUpdate"
    - from: "packages/core/src/queue/worker-process.ts"
      to: "packages/collectors/src/adapters/index.ts"
      via: "collector registry"
      pattern: "getCollector|registerCollector"
---

<objective>
수집 데이터 정규화, DB 중복 제거 upsert, BullMQ Worker 프로세스를 완성하여 전체 파이프라인을 E2E로 연결한다.

Purpose: collect -> normalize -> persist 3단계 파이프라인을 실제 동작하도록 연결하고, CLI로 수집을 트리거할 수 있게 한다.
Output: 정규화 모듈 + DB upsert + Worker 프로세스 + 트리거 CLI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-data-collection/01-CONTEXT.md
@.planning/phases/01-foundation-core-data-collection/01-RESEARCH.md
@.planning/phases/01-foundation-core-data-collection/01-02-SUMMARY.md
@.planning/phases/01-foundation-core-data-collection/01-03-SUMMARY.md
@.planning/phases/01-foundation-core-data-collection/01-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Plan 01-02에서 생성된 DB 스키마 + Queue -->

From packages/core/src/db/schema/collections.ts:
```typescript
export const collectionJobs = pgTable('collection_jobs', { id, keyword, startDate, endDate, status, progress, limits, errorDetails, createdAt, updatedAt });
export const articles = pgTable('articles', { id, jobId, source, sourceId, url, title, content, author, publisher, publishedAt, rawData, collectedAt },
  (table) => [uniqueIndex('articles_source_id_idx').on(table.source, table.sourceId)]);
export const videos = pgTable('videos', { id, jobId, source, sourceId, url, title, description, channelId, channelTitle, viewCount, likeCount, commentCount, publishedAt, rawData, collectedAt },
  (table) => [uniqueIndex('videos_source_id_idx').on(table.source, table.sourceId)]);
export const comments = pgTable('comments', { id, jobId, source, sourceId, parentId, articleId, videoId, content, author, likeCount, dislikeCount, publishedAt, rawData, collectedAt },
  (table) => [uniqueIndex('comments_source_id_idx').on(table.source, table.sourceId)]);
```

From packages/core/src/queue/flows.ts:
```typescript
export async function triggerCollection(params: CollectionTrigger): Promise<{ jobId: string; flow: any }>;
```

From packages/core/src/queue/workers.ts:
```typescript
export function createCollectorWorker(processJob: (job: Job) => Promise<any>): Worker;
export function createPipelineWorker(processJob: (job: Job) => Promise<any>): Worker;
```

<!-- Plan 03-04에서 생성된 수집기 -->

From packages/collectors/src/adapters/index.ts:
```typescript
export { NaverNewsCollector } from './naver-news';
export { NaverCommentsCollector } from './naver-comments';
export { YoutubeVideosCollector } from './youtube-videos';
export { YoutubeCommentsCollector } from './youtube-comments';
export { registerCollector, getCollector } from './registry';
```

NaverArticle: { sourceId, url, title, content, author, publisher, publishedAt, rawData }
NaverComment: { sourceId, parentId, articleSourceId, content, author, likeCount, dislikeCount, publishedAt, rawData }
YoutubeVideo: { sourceId, url, title, description, channelId, channelTitle, viewCount, likeCount, commentCount, publishedAt, rawData }
YoutubeComment: { sourceId, parentId, videoSourceId, content, author, likeCount, publishedAt, rawData }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정규화 모듈 + DB Upsert (중복 제거)</name>
  <files>
    packages/core/src/pipeline/normalize.ts,
    packages/core/src/pipeline/persist.ts,
    packages/core/src/pipeline/index.ts,
    packages/core/src/index.ts,
    packages/core/tests/dedup.test.ts,
    packages/core/tests/normalize.test.ts
  </files>
  <read_first>
    packages/core/src/db/schema/collections.ts,
    packages/core/src/db/index.ts,
    packages/core/src/index.ts,
    packages/collectors/src/adapters/naver-news.ts,
    packages/collectors/src/adapters/youtube-videos.ts,
    .planning/phases/01-foundation-core-data-collection/01-RESEARCH.md
  </read_first>
  <action>
1. `packages/core/src/pipeline/normalize.ts` -- 수집 데이터를 DB insert 형식으로 변환:

각 수집기의 출력 타입(NaverArticle, NaverComment, YoutubeVideo, YoutubeComment)을 DB 스키마의 insert 형식으로 변환하는 함수들:

```typescript
// NaverArticle -> articles 테이블 insert 형식
export function normalizeNaverArticle(article: NaverArticle, jobId: number): typeof articles.$inferInsert {
  return {
    jobId,
    source: 'naver-news',
    sourceId: article.sourceId,
    url: article.url,
    title: article.title,
    content: article.content,
    author: article.author,
    publisher: article.publisher,
    publishedAt: article.publishedAt,
    rawData: article.rawData,
  };
}

// NaverComment -> comments 테이블 insert 형식
export function normalizeNaverComment(comment: NaverComment, jobId: number, articleDbId?: number): typeof comments.$inferInsert {
  return {
    jobId,
    source: 'naver-news',
    sourceId: comment.sourceId,
    parentId: comment.parentId,
    articleId: articleDbId ?? null,
    videoId: null,
    content: comment.content,
    author: comment.author,
    likeCount: comment.likeCount,
    dislikeCount: comment.dislikeCount,
    publishedAt: comment.publishedAt,
    rawData: comment.rawData,
  };
}

// YoutubeVideo -> videos 테이블 insert 형식
export function normalizeYoutubeVideo(video: YoutubeVideo, jobId: number): typeof videos.$inferInsert { ... }

// YoutubeComment -> comments 테이블 insert 형식
export function normalizeYoutubeComment(comment: YoutubeComment, jobId: number, videoDbId?: number): typeof comments.$inferInsert { ... }
```

각 함수의 반환 타입은 Drizzle의 `$inferInsert` 타입을 사용하여 타입 안전성 보장.

2. `packages/core/src/pipeline/persist.ts` -- DB Upsert 함수 (per D-07):

Drizzle ORM의 `onConflictDoUpdate`를 사용하여 unique index 충돌 시 기존 레코드를 업데이트:

```typescript
import { db } from '../db';
import { articles, videos, comments, collectionJobs } from '../db/schema';
import { sql } from 'drizzle-orm';

// 기사 upsert -- articles_source_id_idx(source + sourceId) 충돌 시 업데이트
export async function persistArticles(data: (typeof articles.$inferInsert)[]) {
  if (data.length === 0) return [];
  return db.insert(articles)
    .values(data)
    .onConflictDoUpdate({
      target: [articles.source, articles.sourceId],
      set: {
        title: sql`excluded.title`,
        content: sql`excluded.content`,
        rawData: sql`excluded.raw_data`,
        collectedAt: sql`excluded.collected_at`,
      },
    })
    .returning();
}

// 영상 upsert -- videos_source_id_idx 충돌 시 통계 업데이트
export async function persistVideos(data: (typeof videos.$inferInsert)[]) {
  if (data.length === 0) return [];
  return db.insert(videos)
    .values(data)
    .onConflictDoUpdate({
      target: [videos.source, videos.sourceId],
      set: {
        viewCount: sql`excluded.view_count`,
        likeCount: sql`excluded.like_count`,
        commentCount: sql`excluded.comment_count`,
        rawData: sql`excluded.raw_data`,
        collectedAt: sql`excluded.collected_at`,
      },
    })
    .returning();
}

// 댓글 upsert -- comments_source_id_idx 충돌 시 업데이트
export async function persistComments(data: (typeof comments.$inferInsert)[]) {
  if (data.length === 0) return [];
  return db.insert(comments)
    .values(data)
    .onConflictDoUpdate({
      target: [comments.source, comments.sourceId],
      set: {
        content: sql`excluded.content`,
        likeCount: sql`excluded.like_count`,
        dislikeCount: sql`excluded.dislike_count`,
        rawData: sql`excluded.raw_data`,
        collectedAt: sql`excluded.collected_at`,
      },
    })
    .returning();
}

// 수집 작업 상태 업데이트 (D-06)
export async function updateJobProgress(jobId: number, progress: Record<string, unknown>, status?: string) {
  return db.update(collectionJobs)
    .set({
      progress: progress as any,
      ...(status ? { status } : {}),
      updatedAt: new Date(),
    })
    .where(sql`id = ${jobId}`);
}

// 수집 작업 생성
export async function createCollectionJob(params: {
  keyword: string;
  startDate: Date;
  endDate: Date;
  limits?: Record<string, number>;
}) {
  const [job] = await db.insert(collectionJobs)
    .values({
      keyword: params.keyword,
      startDate: params.startDate,
      endDate: params.endDate,
      status: 'pending',
      limits: params.limits as any,
      progress: {
        naver: { status: 'pending', articles: 0, comments: 0 },
        youtube: { status: 'pending', videos: 0, comments: 0 },
      } as any,
    })
    .returning();
  return job;
}
```

3. `packages/core/src/pipeline/index.ts`:
```typescript
export * from './normalize';
export * from './persist';
```

4. `packages/core/src/index.ts` 업데이트 -- pipeline 모듈 추가:
기존 export에 추가:
```typescript
export * from './pipeline';
```

5. `packages/core/tests/normalize.test.ts` -- 정규화 단위 테스트:
```typescript
import { describe, it, expect } from 'vitest';
import { normalizeNaverArticle, normalizeYoutubeVideo } from '../src/pipeline/normalize';

describe('Normalize - NaverArticle', () => {
  it('should normalize NaverArticle to articles insert format', () => {
    const article = {
      sourceId: '016-0002042395',
      url: 'https://n.news.naver.com/article/016/0002042395',
      title: '테스트 기사',
      content: '본문 내용',
      author: null,
      publisher: '헤럴드경제',
      publishedAt: new Date('2026-03-20'),
      rawData: {},
    };
    const result = normalizeNaverArticle(article, 1);
    expect(result.source).toBe('naver-news');
    expect(result.sourceId).toBe('016-0002042395');
    expect(result.jobId).toBe(1);
    expect(result.title).toBe('테스트 기사');
  });
});

describe('Normalize - YoutubeVideo', () => {
  it('should normalize YoutubeVideo to videos insert format', () => {
    const video = {
      sourceId: 'dQw4w9WgXcQ',
      url: 'https://www.youtube.com/watch?v=dQw4w9WgXcQ',
      title: '테스트 영상',
      description: '설명',
      channelId: 'UC123',
      channelTitle: '테스트 채널',
      viewCount: 1000,
      likeCount: 100,
      commentCount: 50,
      publishedAt: new Date('2026-03-20'),
      rawData: {},
    };
    const result = normalizeYoutubeVideo(video, 1);
    expect(result.source).toBe('youtube');
    expect(result.sourceId).toBe('dQw4w9WgXcQ');
    expect(result.viewCount).toBe(1000);
  });
});
```

6. `packages/core/tests/dedup.test.ts` -- 중복 제거 테스트 (DB 연결 필요):
```typescript
import { describe, it, expect } from 'vitest';

// DB 연결이 필요한 통합 테스트 -- 실제 DB가 없으면 skip
describe('Dedup - DB Upsert', () => {
  it('should export persistArticles function', async () => {
    const { persistArticles } = await import('../src/pipeline/persist');
    expect(typeof persistArticles).toBe('function');
  });

  it('should export persistVideos function', async () => {
    const { persistVideos } = await import('../src/pipeline/persist');
    expect(typeof persistVideos).toBe('function');
  });

  it('should export persistComments function', async () => {
    const { persistComments } = await import('../src/pipeline/persist');
    expect(typeof persistComments).toBe('function');
  });

  it('should export createCollectionJob function', async () => {
    const { createCollectionJob } = await import('../src/pipeline/persist');
    expect(typeof createCollectionJob).toBe('function');
  });
});
```
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/pipeline/normalize.ts에 `normalizeNaverArticle`, `normalizeNaverComment`, `normalizeYoutubeVideo`, `normalizeYoutubeComment` 4개 함수 존재
    - packages/core/src/pipeline/persist.ts에 `persistArticles`, `persistVideos`, `persistComments` 3개 함수 존재
    - packages/core/src/pipeline/persist.ts에 `onConflictDoUpdate` 패턴 존재
    - packages/core/src/pipeline/persist.ts에 `updateJobProgress` 함수 존재
    - packages/core/src/pipeline/persist.ts에 `createCollectionJob` 함수 존재
    - packages/core/src/index.ts에서 pipeline 모듈 re-export
    - `pnpm --filter @ai-signalcraft/core test` exit code 0
  </acceptance_criteria>
  <done>정규화 함수 4개 + DB upsert 함수 3개 + 작업 관리 함수 2개 구현, 단위 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: BullMQ Worker 프로세스 + 수집 트리거 CLI</name>
  <files>
    packages/core/src/queue/worker-process.ts,
    scripts/trigger-collection.ts
  </files>
  <read_first>
    packages/core/src/queue/workers.ts,
    packages/core/src/queue/flows.ts,
    packages/core/src/pipeline/normalize.ts,
    packages/core/src/pipeline/persist.ts,
    packages/collectors/src/adapters/index.ts
  </read_first>
  <action>
1. `packages/core/src/queue/worker-process.ts` -- Worker 실행 프로세스:

별도 Node.js 프로세스로 실행 (RESEARCH Pitfall 4: Next.js와 분리).
`pnpm --filter @ai-signalcraft/core worker` 또는 `tsx watch packages/core/src/queue/worker-process.ts`로 실행.

```typescript
import 'dotenv/config';
import { Job } from 'bullmq';
import { createCollectorWorker, createPipelineWorker } from './workers';
import {
  NaverNewsCollector,
  NaverCommentsCollector,
  YoutubeVideosCollector,
  YoutubeCommentsCollector,
  registerCollector,
  getCollector,
} from '@ai-signalcraft/collectors';
import {
  normalizeNaverArticle,
  normalizeNaverComment,
  normalizeYoutubeVideo,
  normalizeYoutubeComment,
  persistArticles,
  persistVideos,
  persistComments,
  updateJobProgress,
} from '../pipeline';

// 수집기 등록
registerCollector(new NaverNewsCollector());
registerCollector(new NaverCommentsCollector());
registerCollector(new YoutubeVideosCollector());
registerCollector(new YoutubeCommentsCollector());

// 수집 Worker -- collectors 큐
const collectorWorker = createCollectorWorker(async (job: Job) => {
  const { source, keyword, startDate, endDate, maxItems, maxComments, jobId } = job.data;
  const collector = getCollector(source);
  if (!collector) throw new Error(`Unknown source: ${source}`);

  const allItems: unknown[] = [];
  for await (const chunk of collector.collect({ keyword, startDate, endDate, maxItems, maxComments })) {
    allItems.push(...chunk);
    // D-06: 진행률 업데이트
    await job.updateProgress({ collected: allItems.length });
  }

  return { source, items: allItems, count: allItems.length };
});

// 파이프라인 Worker -- pipeline 큐 (normalize + persist)
const pipelineWorker = createPipelineWorker(async (job: Job) => {
  const { source, jobId } = job.data;

  if (job.name.startsWith('normalize-')) {
    // 자식 작업(collect)의 결과를 가져와 정규화
    const childValues = await job.getChildrenValues();
    const results: Record<string, unknown> = {};

    for (const [key, value] of Object.entries(childValues)) {
      const childResult = value as { source: string; items: unknown[]; count: number };
      results[childResult.source] = childResult;
    }

    return { source, normalized: true, results };
  }

  if (job.name === 'persist') {
    // 자식 작업(normalize)의 결과를 가져와 DB에 저장
    const childValues = await job.getChildrenValues();

    for (const [key, value] of Object.entries(childValues)) {
      const normalizeResult = value as any;
      const results = normalizeResult.results || {};

      for (const [src, data] of Object.entries(results)) {
        const items = (data as any).items || [];
        if (src === 'naver-news') {
          const normalized = items.map((a: any) => normalizeNaverArticle(a, parseInt(jobId)));
          await persistArticles(normalized);
        } else if (src === 'naver-comments') {
          const normalized = items.map((c: any) => normalizeNaverComment(c, parseInt(jobId)));
          await persistComments(normalized);
        } else if (src === 'youtube-videos') {
          const normalized = items.map((v: any) => normalizeYoutubeVideo(v, parseInt(jobId)));
          await persistVideos(normalized);
        } else if (src === 'youtube-comments') {
          const normalized = items.map((c: any) => normalizeYoutubeComment(c, parseInt(jobId)));
          await persistComments(normalized);
        }
      }
    }

    // D-06: 최종 상태 업데이트
    await updateJobProgress(parseInt(jobId), {}, 'completed');
    return { persisted: true };
  }
});

console.log('Workers started. Waiting for jobs...');
console.log('  - Collector worker (collectors queue)');
console.log('  - Pipeline worker (pipeline queue)');

// Graceful shutdown
process.on('SIGTERM', async () => {
  await collectorWorker.close();
  await pipelineWorker.close();
  process.exit(0);
});
```

2. `scripts/trigger-collection.ts` -- 수집 트리거 CLI:

```typescript
import 'dotenv/config';
import { triggerCollection, createCollectionJob } from '@ai-signalcraft/core';
import { CollectionTriggerSchema } from '@ai-signalcraft/core';

async function main() {
  const keyword = process.argv[2];
  if (!keyword) {
    console.error('Usage: tsx scripts/trigger-collection.ts <keyword> [days=7]');
    process.exit(1);
  }

  const days = parseInt(process.argv[3] || '7');
  const endDate = new Date();
  const startDate = new Date();
  startDate.setDate(startDate.getDate() - days);

  const params = CollectionTriggerSchema.parse({
    keyword,
    startDate: startDate.toISOString(),
    endDate: endDate.toISOString(),
  });

  // DB에 수집 작업 레코드 생성
  const job = await createCollectionJob({
    keyword: params.keyword,
    startDate: new Date(params.startDate),
    endDate: new Date(params.endDate),
  });

  console.log(`Collection job created: #${job.id}`);
  console.log(`  Keyword: ${keyword}`);
  console.log(`  Period: ${startDate.toLocaleDateString('ko-KR')} ~ ${endDate.toLocaleDateString('ko-KR')}`);

  // BullMQ Flow 트리거
  const { jobId, flow } = await triggerCollection(params);
  console.log(`Pipeline triggered: ${jobId}`);
  console.log('Worker가 실행 중이어야 합니다: pnpm worker');
}

main().catch(console.error);
```

3. 루트 package.json scripts에 추가: `"trigger": "tsx scripts/trigger-collection.ts"`
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/queue/worker-process.ts에 `registerCollector` 호출로 4개 수집기 등록
    - packages/core/src/queue/worker-process.ts에 `createCollectorWorker`와 `createPipelineWorker` 사용
    - packages/core/src/queue/worker-process.ts에 `normalizeNaverArticle`, `persistArticles` 등 정규화/저장 함수 호출
    - packages/core/src/queue/worker-process.ts에 `SIGTERM` graceful shutdown 핸들러 존재
    - scripts/trigger-collection.ts에 `triggerCollection` 호출 존재
    - scripts/trigger-collection.ts에 `createCollectionJob` 호출 존재
    - scripts/trigger-collection.ts에 `CollectionTriggerSchema.parse` 입력 검증 존재
    - `pnpm -r build` exit code 0
  </acceptance_criteria>
  <done>Worker 프로세스가 4개 수집기를 등록하고 파이프라인을 처리하며, CLI로 수집을 트리거할 수 있다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E 수집 파이프라인 동작 확인</name>
  <files>none (verification only)</files>
  <action>
사용자가 전체 파이프라인을 E2E로 검증한다. Claude가 자동화한 모든 코드(Task 1, 2)가 실제 외부 서비스(네이버, 유튜브, PostgreSQL, Redis)와 연결되어 정상 동작하는지 확인한다.
  </action>
  <verify>사용자가 아래 how-to-verify 단계를 수행하고 "approved" 입력</verify>
  <done>CLI로 키워드 수집 트리거 시 데이터가 DB에 저장되고 중복 수집 시 upsert로 처리됨을 확인</done>
  <what-built>
    전체 수집 파이프라인: CLI 트리거 -> BullMQ Flow -> 4개 수집기(네이버 뉴스/댓글, 유튜브 영상/댓글) -> 정규화 -> DB Upsert
  </what-built>
  <how-to-verify>
    1. 환경변수 확인:
       - `.env` 파일에 DATABASE_URL, REDIS_URL, YOUTUBE_API_KEY 설정 확인
       - Redis 서버 접근 가능 확인: `redis-cli -u $REDIS_URL ping`

    2. Worker 프로세스 실행:
       ```bash
       pnpm worker
       ```
       "Workers started. Waiting for jobs..." 메시지 확인

    3. 별도 터미널에서 수집 트리거:
       ```bash
       pnpm trigger "윤석열" 3
       ```
       "Collection job created: #N" + "Pipeline triggered" 메시지 확인

    4. Worker 터미널에서 수집 진행 로그 확인

    5. DB에서 수집 결과 확인:
       ```bash
       cd packages/core && pnpm db:studio
       ```
       Drizzle Studio에서:
       - collection_jobs 테이블에 레코드 존재하고 status가 'completed' 또는 'partial_failure'
       - articles 테이블에 네이버 뉴스 기사 존재
       - videos 테이블에 유튜브 영상 존재
       - comments 테이블에 댓글 존재

    6. 중복 제거 확인 -- 동일 키워드로 다시 수집 실행:
       ```bash
       pnpm trigger "윤석열" 3
       ```
       articles/videos/comments 테이블의 레코드 수가 크게 증가하지 않아야 함 (upsert로 기존 레코드 업데이트)
  </how-to-verify>
  <resume-signal>"approved"를 입력하거나 발견된 문제를 설명해 주세요</resume-signal>
</task>

</tasks>

<verification>
- `pnpm -r build` 성공
- `pnpm --filter @ai-signalcraft/core test` 통과
- Worker 프로세스가 정상 기동
- CLI로 수집 트리거 시 데이터가 DB에 저장
- 중복 수집 시 upsert로 처리
</verification>

<success_criteria>
- 정규화 함수가 4개 수집기 출력을 DB 스키마로 변환 (per COLL-10)
- DB upsert가 unique index 충돌 시 업데이트 (per D-07)
- Worker가 collect -> normalize -> persist 3단계 실행 (per D-05)
- CLI로 키워드 입력 시 전체 파이프라인 동작 확인 (per D-01)
- 소스별 부분 실패 시 성공 소스 데이터 저장 (per D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-data-collection/01-05-SUMMARY.md`
</output>
