---
phase: 05-integration-flow-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types/index.ts
  - packages/core/src/queue/flows.ts
  - apps/web/src/server/trpc/routers/analysis.ts
  - packages/core/src/db/schema/analysis.ts
  - packages/core/src/analysis/persist-analysis.ts
  - apps/web/src/components/auth/login-form.tsx
  - apps/web/src/app/login/page.tsx
  - apps/web/src/server/trpc/routers/team.ts
  - packages/core/tests/queue.test.ts
  - packages/core/tests/report.test.ts
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-06
  - COLL-07
  - COLL-08
  - FOUND-03
  - ADVN-01
  - ADVN-02
  - ADVN-03
  - ADVN-04
  - REPT-01
  - TEAM-01
  - TEAM-03
gap_closure: true

must_haves:
  truths:
    - "트리거 폼에서 선택한 소스만 수집기가 실행되고, 미선택 소스는 건너뛴다"
    - "Stage 4 ADVN 분석 완료 후 리포트 재생성 시 기존 리포트가 갱신(upsert)된다"
    - "callbackUrl이 포함된 로그인 시 해당 URL로 리다이렉트되어 초대 수락 플로우가 완료된다"
    - "getPendingInvites가 acceptedAt IS NULL 조건으로 정확히 미수락 초대만 반환한다"
  artifacts:
    - path: "packages/core/src/types/index.ts"
      provides: "CollectionTriggerSchema에 sources optional 필드"
      contains: "sources: z.array"
    - path: "packages/core/src/queue/flows.ts"
      provides: "조건부 children 생성 로직"
      contains: "enabledSources"
    - path: "packages/core/src/analysis/persist-analysis.ts"
      provides: "리포트 upsert 로직"
      contains: "onConflictDoUpdate"
    - path: "apps/web/src/components/auth/login-form.tsx"
      provides: "callbackUrl 리다이렉트 지원"
      contains: "useSearchParams"
    - path: "apps/web/src/server/trpc/routers/team.ts"
      provides: "acceptedAt IS NULL SQL 필터"
      contains: "isNull(invitations.acceptedAt)"
  key_links:
    - from: "apps/web/src/server/trpc/routers/analysis.ts"
      to: "packages/core/src/queue/flows.ts"
      via: "triggerCollection({ ...params, sources: input.sources })"
      pattern: "sources: input\\.sources"
    - from: "packages/core/src/queue/flows.ts"
      to: "BullMQ Flow children"
      via: "enabledSources.includes() 분기"
      pattern: "enabledSources\\.includes"
    - from: "packages/core/src/analysis/persist-analysis.ts"
      to: "packages/core/src/db/schema/analysis.ts"
      via: "onConflictDoUpdate target jobId unique index"
      pattern: "onConflictDoUpdate.*target.*jobId"
    - from: "apps/web/src/components/auth/login-form.tsx"
      to: "apps/web/src/app/login/page.tsx"
      via: "Suspense boundary로 useSearchParams 래핑"
      pattern: "Suspense"
---

<objective>
감사에서 식별된 통합/플로우 갭 3건(INT-01, INT-02, FLOW-01) + 부가 이슈 1건(getPendingInvites 필터)을 수정한다.

Purpose: v1.0 마일스톤 감사에서 발견된 데이터 플로우 단절과 인증 리다이렉트 버그를 해소하여 소스 선택 전달, 리포트 갱신, 초대 수락 플로우를 정상화한다.
Output: 7개 파일 수정 + 2개 테스트 파일 확장
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-flow-gap-closure/05-RESEARCH.md

<interfaces>
<!-- 수정 대상 파일의 현재 상태 -- executor가 참조할 핵심 시그니처 -->

From packages/core/src/types/index.ts:
```typescript
export const CollectionTriggerSchema = z.object({
  keyword: z.string().min(1),
  startDate: z.string().datetime(),
  endDate: z.string().datetime(),
  limits: z.object({
    naverArticles: z.number().default(100),
    youtubeVideos: z.number().default(50),
    communityPosts: z.number().default(50),
    commentsPerItem: z.number().default(500),
  }).optional(),
});
export type CollectionTrigger = z.infer<typeof CollectionTriggerSchema>;
```

From packages/core/src/queue/flows.ts:
```typescript
export async function triggerCollection(params: CollectionTrigger, dbJobId: number)
// 내부에서 5개 소스 children을 항상 생성 (naver, youtube, dcinside, fmkorea, clien)
```

From apps/web/src/server/trpc/routers/analysis.ts (line 26-30):
```typescript
await triggerCollection({
  keyword: input.keyword,
  startDate: new Date(input.startDate).toISOString(),
  endDate: new Date(input.endDate).toISOString(),
}, job.id);
// input.sources가 전달되지 않음
```

From packages/core/src/db/schema/analysis.ts:
```typescript
export const analysisReports = pgTable('analysis_reports', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  jobId: integer('job_id').references(() => collectionJobs.id).notNull(),
  title: text('title').notNull(),
  markdownContent: text('markdown_content').notNull(),
  oneLiner: text('one_liner'),
  metadata: jsonb('metadata').$type<{...}>(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
});
// uniqueIndex on jobId 없음
```

From packages/core/src/analysis/persist-analysis.ts (line 32-38):
```typescript
export async function persistAnalysisReport(data: typeof analysisReports.$inferInsert) {
  const [report] = await db
    .insert(analysisReports)
    .values(data)
    .onConflictDoNothing()  // 버그: 갱신 불가
    .returning();
  return report;
}
```

From apps/web/src/components/auth/login-form.tsx:
```typescript
// line 35: router.push('/');  -- callbackUrl 무시
// line 46: signIn('google', { callbackUrl: '/' });  -- 하드코딩
```

From apps/web/src/server/trpc/routers/team.ts (line 304-324):
```typescript
getPendingInvites: adminProcedure.query(async ({ ctx }) => {
  // WHERE: teamId = ctx.teamId AND expiresAt > now()
  // 누락: acceptedAt IS NULL
  // line 323: JS filter로 expiresAt만 재검증 (acceptedAt 미필터)
});
```

From apps/web/src/app/login/page.tsx:
```typescript
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background">
      <LoginForm />  // Suspense 미래핑
    </main>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: INT-01 -- sources 필드 전달로 선택한 소스만 수집기 실행</name>
  <files>
    packages/core/src/types/index.ts,
    packages/core/src/queue/flows.ts,
    apps/web/src/server/trpc/routers/analysis.ts,
    packages/core/tests/queue.test.ts
  </files>
  <read_first>
    packages/core/src/types/index.ts,
    packages/core/src/queue/flows.ts,
    apps/web/src/server/trpc/routers/analysis.ts,
    packages/core/tests/queue.test.ts
  </read_first>
  <behavior>
    - Test 1: CollectionTriggerSchema.parse()에 sources 필드가 포함된 입력이 유효하게 파싱된다
    - Test 2: CollectionTriggerSchema.parse()에 sources 필드가 없는 입력도 유효하게 파싱된다 (하위 호환)
    - Test 3: sources에 빈 배열은 거부된다 (z.min(1) 적용 시)
  </behavior>
  <action>
    1. `packages/core/src/types/index.ts` -- CollectionTriggerSchema에 sources 필드 추가:
       ```typescript
       sources: z.array(z.enum(['naver', 'youtube', 'dcinside', 'fmkorea', 'clien'])).optional(),
       ```
       위치: `limits` 필드 바로 위에 추가. optional()이므로 기존 호출 하위 호환 유지.

    2. `packages/core/src/queue/flows.ts` -- triggerCollection 함수 수정:
       - line 28 (flow children 생성) 바로 위에 enabledSources 변수 추가:
         ```typescript
         const enabledSources = params.sources ?? ['naver', 'youtube', 'dcinside', 'fmkorea', 'clien'];
         ```
       - 기존 children 배열의 5개 소스 블록을 조건부로 변경. 빈 배열 `children`을 선언하고 각 소스를 `enabledSources.includes()`로 분기:
         ```typescript
         const children = [];
         if (enabledSources.includes('naver')) {
           children.push({
             name: 'normalize-naver',
             queueName: 'pipeline',
             data: { source: 'naver-news', flowId, dbJobId, maxComments: limits.commentsPerItem },
             children: [{
               name: 'collect-naver-articles',
               queueName: 'collectors',
               data: { ...params, source: 'naver-news', maxItems: limits.naverArticles, maxComments: limits.commentsPerItem, flowId, dbJobId },
             }],
           });
         }
         if (enabledSources.includes('youtube')) {
           children.push({
             name: 'normalize-youtube',
             queueName: 'pipeline',
             data: { source: 'youtube', flowId, dbJobId },
             children: [{
               name: 'collect-youtube-videos',
               queueName: 'collectors',
               data: { ...params, source: 'youtube-videos', maxItems: limits.youtubeVideos, flowId, dbJobId },
             }, {
               name: 'collect-youtube-comments',
               queueName: 'collectors',
               data: { ...params, source: 'youtube-comments', maxComments: limits.commentsPerItem, flowId, dbJobId },
             }],
           });
         }
         if (enabledSources.includes('dcinside')) {
           children.push({
             name: 'normalize-community-dcinside',
             queueName: 'pipeline',
             data: { source: 'dcinside', flowId, dbJobId },
             children: [{
               name: 'collect-dcinside',
               queueName: 'collectors',
               data: { ...params, source: 'dcinside', maxItems: limits.communityPosts, maxComments: limits.commentsPerItem, flowId, dbJobId },
             }],
           });
         }
         // fmkorea, clien 동일 패턴
         ```
       - flow.add({ children }) 부분에서 기존 인라인 children 대신 위 변수 사용

    3. `apps/web/src/server/trpc/routers/analysis.ts` -- line 26-30의 triggerCollection 호출에 sources 전달:
       ```typescript
       await triggerCollection({
         keyword: input.keyword,
         startDate: new Date(input.startDate).toISOString(),
         endDate: new Date(input.endDate).toISOString(),
         sources: input.sources,
       }, job.id);
       ```

    4. `packages/core/tests/queue.test.ts` -- 테스트 추가:
       - 기존 describe 블록 안에 sources 필드 파싱 테스트 3개 추가 (behavior 참조)
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core vitest run tests/queue.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/types/index.ts에 `sources: z.array(z.enum(['naver', 'youtube', 'dcinside', 'fmkorea', 'clien'])).optional()` 포함
    - packages/core/src/queue/flows.ts에 `const enabledSources = params.sources ??` 패턴 포함
    - packages/core/src/queue/flows.ts에 `enabledSources.includes('naver')` 패턴 포함 (5개 소스 모두)
    - apps/web/src/server/trpc/routers/analysis.ts에 `sources: input.sources` 포함
    - packages/core/tests/queue.test.ts에 `sources` 관련 테스트가 3개 이상 존재
    - `pnpm --filter @ai-signalcraft/core vitest run tests/queue.test.ts` 종료 코드 0
  </acceptance_criteria>
  <done>CollectionTriggerSchema에 sources 필드가 추가되고, triggerCollection이 선택한 소스만 Flow children에 포함하며, tRPC router가 sources를 전달한다. 테스트 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INT-02 -- persistAnalysisReport를 onConflictDoUpdate로 변경 + jobId unique constraint</name>
  <files>
    packages/core/src/db/schema/analysis.ts,
    packages/core/src/analysis/persist-analysis.ts,
    packages/core/tests/report.test.ts
  </files>
  <read_first>
    packages/core/src/db/schema/analysis.ts,
    packages/core/src/analysis/persist-analysis.ts,
    packages/core/tests/report.test.ts
  </read_first>
  <behavior>
    - Test 1: persistAnalysisReport가 export되고 함수 타입이다
    - Test 2: analysisReports 스키마에 job_id unique index가 정의되어 있다
  </behavior>
  <action>
    1. `packages/core/src/db/schema/analysis.ts` -- analysisReports 테이블에 jobId uniqueIndex 추가:
       - 테이블 정의에 세 번째 인자(constraints 함수) 추가:
         ```typescript
         export const analysisReports = pgTable('analysis_reports', {
           id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
           jobId: integer('job_id').references(() => collectionJobs.id).notNull(),
           title: text('title').notNull(),
           markdownContent: text('markdown_content').notNull(),
           oneLiner: text('one_liner'),
           metadata: jsonb('metadata').$type<{
             keyword: string;
             dateRange: { start: string; end: string };
             modulesCompleted: string[];
             modulesFailed: string[];
             totalTokens: number;
             generatedAt: string;
           }>(),
           createdAt: timestamp('created_at').defaultNow().notNull(),
         }, (table) => [
           uniqueIndex('analysis_reports_job_id_idx').on(table.jobId),
         ]);
         ```
       - 참고: analysisResults 테이블이 이미 같은 패턴(`(table) => [uniqueIndex(...)]`)을 사용하므로 동일하게 적용

    2. `packages/core/src/analysis/persist-analysis.ts` -- persistAnalysisReport 함수를 onConflictDoUpdate로 변경:
       - line 36의 `.onConflictDoNothing()`을 아래로 교체:
         ```typescript
         .onConflictDoUpdate({
           target: [analysisReports.jobId],
           set: {
             title: sql`excluded.title`,
             markdownContent: sql`excluded.markdown_content`,
             oneLiner: sql`excluded.one_liner`,
             metadata: sql`excluded.metadata`,
           },
         })
         ```
       - 함수 상단의 주석도 수정: `종합 분석 리포트 저장` -> `종합 분석 리포트 upsert (jobId 기준)`
       - `.returning()` 유지 -- upsert 성공 시에도 report 객체 반환 보장

    3. `packages/core/tests/report.test.ts` -- 기존 describe 블록 안에 스키마 테스트 추가:
       ```typescript
       it('analysisReports 스키마에 jobId unique index가 정의되어 있다', async () => {
         const { analysisReports } = await import('../src/db/schema/analysis');
         // 테이블이 정의되어 있고 jobId 컬럼이 존재하는지 확인
         expect(analysisReports.jobId).toBeDefined();
       });
       ```

    4. DB 마이그레이션: Drizzle push로 스키마 동기화 필요하지만, 운영 서버 접속이 필요하므로 executor가 `pnpm drizzle-kit push` 명령을 시도하되 DB 연결 실패 시 스킵 가능 (코드 변경이 우선).
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core vitest run tests/report.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/db/schema/analysis.ts에 `uniqueIndex('analysis_reports_job_id_idx').on(table.jobId)` 포함
    - packages/core/src/analysis/persist-analysis.ts에 `onConflictDoUpdate` 포함
    - packages/core/src/analysis/persist-analysis.ts에 `onConflictDoNothing` 미포함
    - packages/core/src/analysis/persist-analysis.ts에 `target: [analysisReports.jobId]` 포함
    - packages/core/src/analysis/persist-analysis.ts에 `excluded.title` 포함
    - `pnpm --filter @ai-signalcraft/core vitest run tests/report.test.ts` 종료 코드 0
  </acceptance_criteria>
  <done>analysisReports에 jobId unique constraint가 추가되고, persistAnalysisReport가 onConflictDoUpdate로 변경되어 Stage 4 후 리포트 재생성 시 기존 리포트를 갱신한다.</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-01 -- login-form.tsx callbackUrl 반영 + getPendingInvites acceptedAt 필터</name>
  <files>
    apps/web/src/components/auth/login-form.tsx,
    apps/web/src/app/login/page.tsx,
    apps/web/src/server/trpc/routers/team.ts
  </files>
  <read_first>
    apps/web/src/components/auth/login-form.tsx,
    apps/web/src/app/login/page.tsx,
    apps/web/src/server/trpc/routers/team.ts
  </read_first>
  <action>
    1. `apps/web/src/components/auth/login-form.tsx` -- callbackUrl 지원 추가:
       - import 추가: `import { useSearchParams } from 'next/navigation';`
       - LoginForm 함수 내부 상단(line 14, `const router = useRouter();` 바로 아래)에 추가:
         ```typescript
         const searchParams = useSearchParams();
         const callbackUrl = searchParams.get('callbackUrl') || '/';
         ```
       - line 35의 `router.push('/')` -> `router.push(callbackUrl)`으로 변경
       - line 46의 `signIn('google', { callbackUrl: '/' })` -> `signIn('google', { callbackUrl })`으로 변경

    2. `apps/web/src/app/login/page.tsx` -- Suspense boundary 추가:
       - import 추가: `import { Suspense } from 'react';`
       - LoginForm을 Suspense로 래핑:
         ```typescript
         import type { Metadata } from 'next';
         import { Suspense } from 'react';
         import { LoginForm } from '@/components/auth/login-form';

         export const metadata: Metadata = {
           title: '로그인 - AI SignalCraft',
         };

         export default function LoginPage() {
           return (
             <main className="flex min-h-screen items-center justify-center bg-background">
               <Suspense fallback={<div className="h-[400px] w-full max-w-sm" />}>
                 <LoginForm />
               </Suspense>
             </main>
           );
         }
         ```
       - fallback은 LoginForm과 유사한 크기의 빈 div로 레이아웃 시프트 방지

    3. `apps/web/src/server/trpc/routers/team.ts` -- getPendingInvites SQL 필터 수정:
       - line 4의 import에 `isNull` 추가: `import { eq, and, gt, isNull } from 'drizzle-orm';`
       - line 305-324의 getPendingInvites 쿼리 수정:
         ```typescript
         getPendingInvites: adminProcedure.query(async ({ ctx }) => {
           const pending = await ctx.db
             .select({
               id: invitations.id,
               email: invitations.email,
               role: invitations.role,
               createdAt: invitations.createdAt,
               expiresAt: invitations.expiresAt,
             })
             .from(invitations)
             .where(
               and(
                 eq(invitations.teamId, ctx.teamId),
                 gt(invitations.expiresAt, new Date()),
                 isNull(invitations.acceptedAt),
               ),
             );
           return pending;
         }),
         ```
       - line 323의 JS `.filter()` 후처리 삭제 -- DB 레벨 필터로 대체
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/auth/login-form.tsx에 `useSearchParams` import 포함
    - apps/web/src/components/auth/login-form.tsx에 `searchParams.get('callbackUrl')` 포함
    - apps/web/src/components/auth/login-form.tsx에 `router.push(callbackUrl)` 포함 (하드코딩 '/' 아님)
    - apps/web/src/components/auth/login-form.tsx에 `signIn('google', { callbackUrl })` 포함
    - apps/web/src/app/login/page.tsx에 `Suspense` import 및 `<Suspense` 태그 포함
    - apps/web/src/server/trpc/routers/team.ts에 `isNull` import 포함
    - apps/web/src/server/trpc/routers/team.ts에 `isNull(invitations.acceptedAt)` 포함
    - apps/web/src/server/trpc/routers/team.ts에 `.filter(` 미포함 (JS 후처리 제거)
    - `pnpm --filter web build` 종료 코드 0 (빌드 성공)
  </acceptance_criteria>
  <done>LoginForm이 callbackUrl 쿼리 파라미터를 읽어 로그인 후 해당 URL로 리다이렉트하고, getPendingInvites가 DB 레벨에서 acceptedAt IS NULL 조건으로 미수락 초대만 반환한다. 빌드 통과.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @ai-signalcraft/core test` -- 모든 core 패키지 테스트 통과
2. `pnpm --filter web build` -- 웹 앱 빌드 성공 (TypeScript 에러 없음)
3. `pnpm -r test` -- 전체 모노리포 테스트 통과
</verification>

<success_criteria>
1. CollectionTriggerSchema에 sources optional 필드가 존재하고, triggerCollection이 sources 기반으로 조건부 Flow children을 생성한다
2. analysisReports 테이블에 jobId uniqueIndex가 정의되고, persistAnalysisReport가 onConflictDoUpdate로 upsert를 수행한다
3. LoginForm이 useSearchParams로 callbackUrl을 읽어 credentials/Google 로그인 후 해당 URL로 리다이렉트한다
4. getPendingInvites가 Drizzle isNull(invitations.acceptedAt) SQL 조건으로 미수락 초대만 반환한다
5. `pnpm -r test` 전체 통과, `pnpm --filter web build` 성공
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-flow-gap-closure/05-01-SUMMARY.md`
</output>
