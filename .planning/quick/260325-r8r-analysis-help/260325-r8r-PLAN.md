---
phase: 260325-r8r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/analysis/trigger-form.tsx
autonomous: true
requirements:
  - HELP-01
must_haves:
  truths:
    - "TriggerForm 카드 하단에 '도움말' 토글 버튼이 표시된다"
    - '버튼 클릭 시 도움말 섹션이 펼쳐지고, 다시 클릭 시 접힌다'
    - '도움말에는 키워드, 소스별 특성, 기간 권장사항, 분석 프로세스 흐름이 포함된다'
  artifacts:
    - path: 'apps/web/src/components/analysis/trigger-form.tsx'
      provides: '도움말 토글 섹션이 포함된 TriggerForm 컴포넌트'
  key_links:
    - from: '도움말 버튼'
      to: '도움말 콘텐츠'
      via: 'useState isHelpOpen 토글'
---

<objective>
TriggerForm 카드에 접이식(collapsible) 도움말 섹션을 추가한다.

Purpose: 처음 사용하는 분석팀 구성원이 각 입력 필드의 의미와 소스별 특성, 권장 기간을 즉시 파악할 수 있도록 한다.
Output: 기본은 접혀있고 "도움말" 버튼 클릭 시 펼쳐지는 도움말 섹션이 포함된 trigger-form.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/analysis/trigger-form.tsx

<interfaces>
<!-- 현재 trigger-form.tsx의 핵심 구조 -->
<!-- Card > CardHeader > CardTitle("분석 실행") -->
<!-- Card > CardContent > form > [키워드, 소스, 기간, 버튼] -->
<!-- shadcn/ui: Button, Card, Input, Label, Checkbox, Popover, Calendar 사용 중 -->
<!-- lucide-react: Loader2, CalendarIcon 사용 중 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn/ui Collapsible 컴포넌트 설치</name>
  <files>apps/web/src/components/ui/collapsible.tsx</files>
  <action>
    shadcn/ui Collapsible 컴포넌트를 설치한다:

    ```bash
    cd /home/gon/projects/ai/ai-signalcraft && pnpm dlx shadcn@latest add collapsible --cwd apps/web
    ```

    설치 후 apps/web/src/components/ui/collapsible.tsx 파일이 생성되는지 확인한다.

  </action>
  <verify>
    <automated>ls /home/gon/projects/ai/ai-signalcraft/apps/web/src/components/ui/collapsible.tsx</automated>
  </verify>
  <done>collapsible.tsx 파일이 존재한다</done>
</task>

<task type="auto">
  <name>Task 2: TriggerForm에 도움말 토글 섹션 추가</name>
  <files>apps/web/src/components/analysis/trigger-form.tsx</files>
  <action>
    trigger-form.tsx를 수정하여 도움말 섹션을 추가한다.

    **import 추가:**
    - `Collapsible, CollapsibleContent, CollapsibleTrigger` from `@/components/ui/collapsible`
    - `HelpCircle, ChevronDown` from `lucide-react`

    **state 추가:**
    ```typescript
    const [isHelpOpen, setIsHelpOpen] = useState(false);
    ```

    **도움말 섹션 위치:** 폼(form) 태그 닫힘(`</form>`) 바로 앞, 실행 버튼(`<Button type="submit">`) 다음에 삽입.

    **도움말 섹션 구현:**
    ```tsx
    <Collapsible open={isHelpOpen} onOpenChange={setIsHelpOpen}>
      <CollapsibleTrigger asChild>
        <Button
          type="button"
          variant="ghost"
          size="sm"
          className="w-full flex items-center justify-center gap-1 text-muted-foreground hover:text-foreground"
        >
          <HelpCircle className="h-4 w-4" />
          도움말
          <ChevronDown
            className={`h-4 w-4 transition-transform duration-200 ${isHelpOpen ? 'rotate-180' : ''}`}
          />
        </Button>
      </CollapsibleTrigger>
      <CollapsibleContent>
        <div className="mt-3 rounded-lg bg-muted/50 p-4 text-sm space-y-3">
          {/* 키워드 */}
          <div>
            <p className="font-medium text-foreground mb-1">키워드</p>
            <p className="text-muted-foreground">
              분석하려는 인물명 또는 주요 키워드를 입력하세요.
            </p>
            <p className="text-muted-foreground mt-1">
              예: "이재명", "윤석열", "삼성전자", "갤럭시 S25"
            </p>
          </div>
          {/* 소스별 특성 */}
          <div>
            <p className="font-medium text-foreground mb-1">소스별 특성</p>
            <ul className="space-y-1 text-muted-foreground">
              <li>• <span className="text-foreground">네이버 뉴스</span> — 뉴스 기사 본문 + 댓글 수집</li>
              <li>• <span className="text-foreground">유튜브</span> — 관련 영상 댓글 수집</li>
              <li>• <span className="text-foreground">커뮤니티</span> (DC갤러리/에펨코리아/클리앙) — 게시글 + 댓글 수집</li>
            </ul>
          </div>
          {/* 기간 */}
          <div>
            <p className="font-medium text-foreground mb-1">분석 기간</p>
            <p className="text-muted-foreground">
              <span className="text-foreground font-medium">권장: 1~2주.</span>{' '}
              기간이 길수록 수집 시간이 증가합니다. 이슈 발생 직전~직후 기간으로 좁히면 더 정확한 분석이 가능합니다.
            </p>
          </div>
          {/* 분석 프로세스 */}
          <div>
            <p className="font-medium text-foreground mb-1">분석 프로세스</p>
            <div className="flex items-center gap-1 text-muted-foreground flex-wrap">
              <span className="rounded bg-muted px-2 py-0.5 text-xs">수집</span>
              <span>→</span>
              <span className="rounded bg-muted px-2 py-0.5 text-xs">전처리</span>
              <span>→</span>
              <span className="rounded bg-muted px-2 py-0.5 text-xs">AI 분석</span>
              <span>→</span>
              <span className="rounded bg-muted px-2 py-0.5 text-xs">리포트 생성</span>
            </div>
            <p className="text-muted-foreground mt-1">
              소스 수와 기간에 따라 수 분에서 수십 분이 소요될 수 있습니다.
            </p>
          </div>
        </div>
      </CollapsibleContent>
    </Collapsible>
    ```

    **주의사항:**
    - `type="button"` 필수 — form submit 방지
    - `isHelpOpen` state는 기존 state 선언 블록에 추가
    - 기존 폼 구조(`space-y-4`)는 변경하지 않음

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter web tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TypeScript 컴파일 오류 없음
    - TriggerForm에 Collapsible 도움말 섹션이 포함됨
    - "도움말" 버튼이 실행 버튼 아래에 표시됨
    - 기본 상태는 접혀있음 (isHelpOpen: false)
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter web tsc --noEmit
```
TypeScript 오류 없이 컴파일되어야 한다.
</verification>

<success_criteria>

- "도움말" 버튼이 실행 버튼 아래에 표시된다
- 클릭 시 도움말 섹션이 펼쳐지고, 재클릭 시 접힌다
- 도움말에 키워드 예시, 소스별 특성, 기간 권장사항, 분석 프로세스 4가지 항목이 포함된다
- TypeScript 컴파일 오류 없음
  </success_criteria>

<output>
완료 후 `.planning/quick/260325-r8r-analysis-help/260325-r8r-SUMMARY.md` 생성
</output>
