---
quick_id: 260327-0b8
status: completed
---

# Quick Task 260327-0b8: 최근 분석 UI 개선 - Summary

## 변경 사항

### 1. 신규 파일: `source-icons.tsx`

- 5개 소스(네이버, 유튜브, DC갤러리, 에펨코리아, 클리앙) 아이콘+라벨+색상 매핑
- `extractSources()`: progress JSONB에서 소스 키 추출
- `summarizeCounts()`: 기사/영상/게시글 + 댓글 건수 합산
- `formatDuration()`: createdAt→updatedAt 소요 시간 포맷 (초/분/시간)
- `SourceBadges`: Tooltip 포함 소스 아이콘 배지 컴포넌트

### 2. `recent-jobs.tsx` 개선

- 날짜 포맷: `yyyy-MM-dd` → `MM-dd HH:mm` (시분 포함)
- 소스 컬럼 추가 (아이콘 표시)
- 수집 건수 컬럼 추가 (기사/게시글 + 댓글)
- 완료된 작업에 소요 시간 표시

### 3. `history-table.tsx` 개선

- 날짜 포맷: `yyyy-MM-dd` → `yyyy-MM-dd HH:mm` (시분 포함)
- 소스 표시: `N`, `Y` 약어 → 컬러 아이콘 + Tooltip
- 수집 건수 컬럼 추가
- 완료된 작업에 소요 시간 표시

## 검증

- TypeScript 컴파일 에러 없음
