---
phase: 02-ai-analysis-engine-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/db/schema/analysis.ts
  - packages/core/src/db/schema/index.ts
  - packages/core/src/analysis/types.ts
  - packages/core/src/analysis/data-loader.ts
  - packages/core/src/analysis/persist-analysis.ts
  - packages/core/src/analysis/index.ts
  - packages/core/tests/analysis-schema.test.ts
autonomous: true
requirements: [ANLZ-01, ANLZ-04, REPT-01]

must_haves:
  truths:
    - 'analysis_results 테이블이 DB에 생성되고 모듈별 JSONB 결과를 저장할 수 있다'
    - 'analysis_reports 테이블이 DB에 생성되고 마크다운 리포트를 저장할 수 있다'
    - 'AnalysisModule 인터페이스가 정의되어 모든 분석 모듈이 일관된 구조를 따른다'
    - 'AI Gateway가 systemPrompt를 지원하고 usage(토큰) 정보를 반환한다'
    - 'DB에서 수집 데이터를 로드하여 분석 입력 형식으로 변환할 수 있다'
  artifacts:
    - path: 'packages/core/src/db/schema/analysis.ts'
      provides: 'analysisResults + analysisReports Drizzle 테이블 스키마'
      contains: 'analysisResults'
    - path: 'packages/core/src/analysis/types.ts'
      provides: 'AnalysisModule 인터페이스 + AnalysisInput 타입'
      exports: ['AnalysisModule', 'AnalysisInput', 'AnalysisModuleResult', 'MODULE_MODEL_MAP']
    - path: 'packages/ai-gateway/src/gateway.ts'
      provides: 'systemPrompt + usage 반환이 추가된 AI Gateway'
      exports: ['analyzeText', 'analyzeStructured']
  key_links:
    - from: 'packages/core/src/analysis/persist-analysis.ts'
      to: 'packages/core/src/db/schema/analysis.ts'
      via: 'Drizzle insert/upsert'
      pattern: 'analysisResults|analysisReports'
    - from: 'packages/core/src/analysis/data-loader.ts'
      to: 'packages/core/src/db/schema/collections.ts'
      via: 'Drizzle select query'
      pattern: 'articles|videos|comments'
---

<objective>
Phase 02 기반 구축: 분석 DB 스키마, AnalysisModule 인터페이스/타입, AI Gateway 확장, 데이터 로더, 분석 결과 persist 함수를 생성한다.

Purpose: 모든 분석 모듈(Plan 02~03)과 리포트 생성(Plan 05)이 의존하는 핵심 계약(contracts)과 인프라를 먼저 확립한다.
Output: analysis.ts 스키마, types.ts 인터페이스, data-loader.ts, persist-analysis.ts, 확장된 gateway.ts (Task 2)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-analysis-engine-report/02-CONTEXT.md
@.planning/phases/02-ai-analysis-engine-report/02-RESEARCH.md

<interfaces>
<!-- 기존 코드에서 추출한 핵심 인터페이스 -->

From packages/ai-gateway/src/gateway.ts:

```typescript
export type AIProvider = 'anthropic' | 'openai';
export interface AIGatewayOptions {
  provider?: AIProvider;
  model?: string;
  maxOutputTokens?: number;
}
export async function analyzeText(prompt: string, options?: AIGatewayOptions): Promise<...>;
export async function analyzeStructured<T>(prompt: string, schema: z.ZodType<T>, options?: AIGatewayOptions): Promise<...>;
```

From packages/core/src/db/schema/collections.ts:

```typescript
export const collectionJobs = pgTable('collection_jobs', { id, keyword, startDate, endDate, status, progress, limits, ... });
export const articles = pgTable('articles', { id, jobId, source, sourceId, url, title, content, author, publisher, publishedAt, ... });
export const videos = pgTable('videos', { id, jobId, source, sourceId, url, title, description, channelId, channelTitle, viewCount, likeCount, commentCount, publishedAt, ... });
export const comments = pgTable('comments', { id, jobId, source, sourceId, parentId, articleId, videoId, content, author, likeCount, dislikeCount, publishedAt, ... });
```

From packages/core/src/pipeline/persist.ts:

```typescript
export async function persistArticles(data: (typeof articles.$inferInsert)[]): Promise<...>;
export async function persistVideos(data: (typeof videos.$inferInsert)[]): Promise<...>;
export async function persistComments(data: (typeof comments.$inferInsert)[]): Promise<...>;
export async function updateJobProgress(jobId: number, progress: Record<string, unknown>, status?: string): Promise<...>;
export async function createCollectionJob(params: { keyword, startDate, endDate, limits? }): Promise<...>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 분석 DB 스키마 + AnalysisModule 인터페이스/타입 + 데이터 로더 + persist 함수</name>
  <files>
    packages/core/src/db/schema/analysis.ts,
    packages/core/src/db/schema/index.ts,
    packages/core/src/analysis/types.ts,
    packages/core/src/analysis/data-loader.ts,
    packages/core/src/analysis/persist-analysis.ts,
    packages/core/src/analysis/index.ts,
    packages/core/tests/analysis-schema.test.ts
  </files>
  <read_first>
    packages/core/src/db/schema/collections.ts,
    packages/core/src/db/schema/index.ts,
    packages/core/src/db/index.ts,
    packages/core/src/pipeline/persist.ts,
    packages/core/src/pipeline/normalize.ts,
    packages/core/src/index.ts,
    packages/ai-gateway/src/gateway.ts,
    docs/prompt.md
  </read_first>
  <behavior>
    - Test: analysisResults 스키마가 export되고 필수 컬럼(id, jobId, module, status, result, usage)을 포함한다
    - Test: analysisReports 스키마가 export되고 필수 컬럼(id, jobId, title, markdownContent, oneLiner, metadata)을 포함한다
    - Test: AnalysisModule 인터페이스가 name, displayName, provider, model, schema, buildPrompt, buildSystemPrompt를 포함한다
    - Test: AnalysisInput 타입이 jobId, keyword, articles, videos, comments, dateRange 필드를 포함한다
    - Test: MODULE_MODEL_MAP이 8개 모듈 + integrated-report 매핑을 포함한다
    - Test: persistAnalysisResult 함수가 export된다
    - Test: persistAnalysisReport 함수가 export된다
    - Test: loadAnalysisInput 함수가 export된다
  </behavior>
  <action>
    **1. packages/core/src/db/schema/analysis.ts 생성 (per D-06, D-07):**

    ```typescript
    import { pgTable, text, timestamp, integer, jsonb, uniqueIndex } from 'drizzle-orm/pg-core';
    import { collectionJobs } from './collections';

    // D-06: 분석 결과 (모듈별)
    export const analysisResults = pgTable('analysis_results', {
      id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
      jobId: integer('job_id').references(() => collectionJobs.id).notNull(),
      module: text('module').notNull(),  // 'macro-view', 'segmentation', 등
      status: text('status', {
        enum: ['pending', 'running', 'completed', 'failed'],
      }).notNull().default('pending'),
      result: jsonb('result'),           // 모듈별 Zod 스키마로 타입 보장
      usage: jsonb('usage').$type<{
        inputTokens: number;
        outputTokens: number;
        totalTokens: number;
        provider: string;
        model: string;
      }>(),
      errorMessage: text('error_message'),
      createdAt: timestamp('created_at').defaultNow().notNull(),
      updatedAt: timestamp('updated_at').defaultNow().notNull(),
    }, (table) => [
      uniqueIndex('analysis_results_job_module_idx').on(table.jobId, table.module),
    ]);

    // D-07: 종합 분석 리포트
    export const analysisReports = pgTable('analysis_reports', {
      id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
      jobId: integer('job_id').references(() => collectionJobs.id).notNull(),
      title: text('title').notNull(),
      markdownContent: text('markdown_content').notNull(),
      oneLiner: text('one_liner'),       // REPT-02: 한 줄 요약
      metadata: jsonb('metadata').$type<{
        keyword: string;
        dateRange: { start: string; end: string };
        modulesCompleted: string[];
        modulesFailed: string[];
        totalTokens: number;
        generatedAt: string;
      }>(),
      createdAt: timestamp('created_at').defaultNow().notNull(),
    });
    ```

    **2. packages/core/src/db/schema/index.ts 수정:**
    기존 `export * from './collections';`에 `export * from './analysis';` 추가.

    **3. packages/core/src/analysis/types.ts 생성 (per D-01, D-03):**

    ```typescript
    import { z } from 'zod';
    import type { AIProvider } from '@ai-signalcraft/ai-gateway';

    // 분석 모듈 공통 인터페이스 (D-01)
    export interface AnalysisModule<T = unknown> {
      readonly name: string;           // 'macro-view', 'segmentation', etc.
      readonly displayName: string;    // '전체 여론 구조 분석'
      readonly provider: AIProvider;   // D-03: 모듈별 AI 모델 지정
      readonly model: string;          // 'gpt-4o-mini' or 'claude-sonnet-4-20250514'
      readonly schema: z.ZodType<T>;   // 모듈별 Zod 스키마

      buildPrompt(data: AnalysisInput): string;
      buildSystemPrompt(): string;
      // 선행 분석 결과가 필요한 모듈용 (모듈5~7)
      buildPromptWithContext?(data: AnalysisInput, priorResults: Record<string, unknown>): string;
    }

    // 분석 입력 데이터 (DB에서 로드)
    export interface AnalysisInput {
      jobId: number;
      keyword: string;
      articles: Array<{
        title: string;
        content: string | null;
        publisher: string | null;
        publishedAt: Date | null;
        source: string;
      }>;
      videos: Array<{
        title: string;
        description: string | null;
        channelTitle: string | null;
        viewCount: number | null;
        likeCount: number | null;
        publishedAt: Date | null;
      }>;
      comments: Array<{
        content: string;
        source: string;
        author: string | null;
        likeCount: number | null;
        dislikeCount: number | null;
        publishedAt: Date | null;
      }>;
      dateRange: { start: Date; end: Date };
    }

    // 분석 모듈 실행 결과
    export interface AnalysisModuleResult<T = unknown> {
      module: string;
      status: 'completed' | 'failed';
      result?: T;
      usage?: {
        inputTokens: number;
        outputTokens: number;
        totalTokens: number;
        provider: string;
        model: string;
      };
      errorMessage?: string;
    }

    // 모듈별 AI 모델 매핑 (D-03)
    export const MODULE_MODEL_MAP: Record<string, { provider: AIProvider; model: string }> = {
      'macro-view':        { provider: 'openai',    model: 'gpt-4o-mini' },
      'segmentation':      { provider: 'openai',    model: 'gpt-4o-mini' },
      'sentiment-framing': { provider: 'openai',    model: 'gpt-4o-mini' },
      'message-impact':    { provider: 'openai',    model: 'gpt-4o-mini' },
      'risk-map':          { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
      'opportunity':       { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
      'strategy':          { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
      'final-summary':     { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
      'integrated-report': { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
    };

    // 모듈 이름 상수
    export const MODULE_NAMES = {
      MACRO_VIEW: 'macro-view',
      SEGMENTATION: 'segmentation',
      SENTIMENT_FRAMING: 'sentiment-framing',
      MESSAGE_IMPACT: 'message-impact',
      RISK_MAP: 'risk-map',
      OPPORTUNITY: 'opportunity',
      STRATEGY: 'strategy',
      FINAL_SUMMARY: 'final-summary',
    } as const;
    ```

    **4. packages/core/src/analysis/data-loader.ts 생성:**
    DB에서 jobId 기반으로 articles, videos, comments를 로드하여 AnalysisInput 형식으로 반환.
    - `db.select()` from articles, videos, comments WHERE jobId 매치
    - collectionJobs에서 keyword, startDate, endDate 조회
    - 토큰 절약을 위해 기사 본문 500자 제한, 댓글 좋아요순 상위 500개 제한 (Pitfall 1 대응)
    - `eq` import from 'drizzle-orm', `desc` for ordering

    **5. packages/core/src/analysis/persist-analysis.ts 생성:**
    - `persistAnalysisResult(data)`: analysisResults 테이블에 upsert (jobId+module unique). persist.ts의 onConflictDoUpdate 패턴 그대로 적용.
    - `persistAnalysisReport(data)`: analysisReports 테이블에 insert. jobId로 기존 리포트 대체(delete+insert 또는 upsert).

    **6. packages/core/src/analysis/index.ts 생성:**
    ```typescript
    export * from './types';
    export * from './data-loader';
    export * from './persist-analysis';
    ```

    **7. packages/core/src/index.ts 수정:**
    `export * from './analysis';` 추가.

    **8. packages/core/tests/analysis-schema.test.ts 작성:**
    - analysisResults, analysisReports 스키마 export 검증
    - AnalysisModule, AnalysisInput, MODULE_MODEL_MAP 타입/값 검증
    - persistAnalysisResult, persistAnalysisReport, loadAnalysisInput export 검증

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/db/schema/analysis.ts contains `export const analysisResults = pgTable('analysis_results'`
    - packages/core/src/db/schema/analysis.ts contains `export const analysisReports = pgTable('analysis_reports'`
    - packages/core/src/db/schema/analysis.ts contains `uniqueIndex('analysis_results_job_module_idx')`
    - packages/core/src/analysis/types.ts contains `export interface AnalysisModule`
    - packages/core/src/analysis/types.ts contains `export interface AnalysisInput`
    - packages/core/src/analysis/types.ts contains `export const MODULE_MODEL_MAP`
    - packages/core/src/analysis/types.ts contains `'macro-view'` and `'risk-map'` and `'integrated-report'`
    - packages/core/src/analysis/data-loader.ts contains `export async function loadAnalysisInput`
    - packages/core/src/analysis/persist-analysis.ts contains `export async function persistAnalysisResult`
    - packages/core/src/analysis/persist-analysis.ts contains `export async function persistAnalysisReport`
    - packages/core/src/analysis/index.ts contains `export * from './types'`
    - packages/core/src/db/schema/index.ts contains `export * from './analysis'`
    - `pnpm -r build` exits 0
    - `pnpm --filter @ai-signalcraft/core test` exits 0
  </acceptance_criteria>
  <done>analysis_results/analysis_reports DB 스키마가 정의되고, AnalysisModule 인터페이스/타입이 export되고, 데이터 로더와 persist 함수가 구현되어 빌드/테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: AI Gateway 확장 -- systemPrompt + usage 반환</name>
  <files>
    packages/ai-gateway/src/gateway.ts,
    packages/ai-gateway/src/index.ts
  </files>
  <read_first>
    packages/ai-gateway/src/gateway.ts,
    packages/ai-gateway/src/index.ts,
    packages/ai-gateway/package.json,
    packages/ai-gateway/tsconfig.json
  </read_first>
  <action>
    AI Gateway의 `analyzeStructured()`와 `analyzeText()`를 확장한다 (per D-03, D-04).

    **1. AIGatewayOptions에 systemPrompt 추가:**
    ```typescript
    export interface AIGatewayOptions {
      provider?: AIProvider;
      model?: string;
      maxOutputTokens?: number;
      systemPrompt?: string;  // 신규: 분석 모듈별 시스템 프롬프트
    }
    ```

    **2. analyzeStructured 확장:**
    - `system: options.systemPrompt` 파라미터를 generateObject 호출에 추가
    - 반환값을 `{ object: result.object, usage: result.usage, finishReason: result.finishReason }`로 확장
    - 기존 반환 타입과 호환을 위해 기존 필드도 유지 (spread)

    **3. analyzeText 확장:**
    - `system: options.systemPrompt` 파라미터를 generateText 호출에 추가
    - 반환값에 `usage` 필드 접근 가능하도록 확인 (AI SDK v6에서 이미 반환)

    **4. 주의:** AI SDK v6의 generateObject/generateText의 정확한 반환 타입을 확인하여 usage 필드 매핑. AI SDK v6에서 usage는 `{ promptTokens, completionTokens, totalTokens }` 형태일 수 있으므로 RESEARCH.md의 `{ inputTokens, outputTokens, totalTokens }`와 비교하여 올바른 필드명 사용.

    **IMPORTANT:** AI SDK 공식 문서(https://sdk.vercel.ai/docs/reference/ai-sdk-core/generate-object)를 참조하여 정확한 API 확인. 메모리에 의존하지 말 것.

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/ai-gateway build</automated>
  </verify>
  <acceptance_criteria>
    - packages/ai-gateway/src/gateway.ts contains `systemPrompt?: string`
    - packages/ai-gateway/src/gateway.ts contains `system: options.systemPrompt` or `system:`
    - packages/ai-gateway/src/gateway.ts의 analyzeStructured 함수가 usage를 반환
    - packages/ai-gateway/src/gateway.ts의 analyzeText 함수가 usage를 반환
    - `pnpm --filter @ai-signalcraft/ai-gateway build` exits 0
  </acceptance_criteria>
  <done>AI Gateway가 systemPrompt를 지원하고, usage(토큰 사용량)를 반환하며, 빌드 통과</done>
</task>

</tasks>

<verification>
- `pnpm -r build` 전체 빌드 통과
- `pnpm --filter @ai-signalcraft/core test` 테스트 통과
- analysis_results, analysis_reports 스키마가 packages/core/src/db/schema/analysis.ts에 정의됨
- AnalysisModule 인터페이스가 packages/core/src/analysis/types.ts에 정의됨
- AI Gateway가 systemPrompt + usage 반환을 지원함
</verification>

<success_criteria>

1. `pnpm -r build` exits 0
2. `pnpm --filter @ai-signalcraft/core test` exits 0
3. analysis.ts 스키마, types.ts 인터페이스, data-loader.ts, persist-analysis.ts가 모두 생성되고 export됨
4. AI Gateway가 systemPrompt 옵션을 지원하고 usage를 반환함
   </success_criteria>

<output>
After completion, create `.planning/phases/02-ai-analysis-engine-report/02-01-SUMMARY.md`
</output>
