---
phase: 02-ai-analysis-engine-report
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - packages/core/src/report/generator.ts
  - packages/core/src/report/pdf-exporter.ts
  - packages/core/src/report/index.ts
  - packages/core/src/analysis/runner.ts
  - packages/core/src/index.ts
  - packages/core/tests/report.test.ts
  - packages/core/tests/analysis-runner.test.ts
autonomous: true
requirements: [REPT-01, REPT-02, REPT-03]

must_haves:
  truths:
    - '8개 모듈 분석 결과를 통합한 마크다운 종합 리포트가 자동 생성된다'
    - '리포트에 최종 전략 요약과 한 줄 요약(oneLiner)이 포함된다'
    - '리포트가 analysis_reports 테이블에 저장된다'
    - '마크다운 리포트를 PDF로 내보내기할 수 있다'
    - '부분 실패 시 가용한 모듈 결과만으로 리포트가 생성되고 누락 섹션이 명시된다'
  artifacts:
    - path: 'packages/core/src/report/generator.ts'
      provides: '통합 리포트 마크다운 생성기'
      exports: ['generateIntegratedReport']
    - path: 'packages/core/src/report/pdf-exporter.ts'
      provides: 'PDF 내보내기 (Playwright 기반)'
      exports: ['exportToPdf']
  key_links:
    - from: 'packages/core/src/report/generator.ts'
      to: 'packages/ai-gateway/src/gateway.ts'
      via: 'analyzeText로 자연어 리포트 생성'
      pattern: 'analyzeText'
    - from: 'packages/core/src/report/generator.ts'
      to: 'packages/core/src/analysis/persist-analysis.ts'
      via: 'persistAnalysisReport로 DB 저장'
      pattern: 'persistAnalysisReport'
    - from: 'packages/core/src/analysis/runner.ts'
      to: 'packages/core/src/report/generator.ts'
      via: '분석 완료 후 리포트 생성 호출'
      pattern: 'generateIntegratedReport'
---

<objective>
AI 종합 리포트 생성기와 PDF 내보내기를 구현하고, 분석 러너와 연결하여 분석 완료 후 자동으로 리포트를 생성한다.

Purpose: 8개 분석 모듈 결과를 AI가 통합하여 자연어 마크다운 리포트를 생성하고(D-04 2단계), PDF로 내보내기한다(D-05).
Output: generator.ts, pdf-exporter.ts, 확장된 runner.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-analysis-engine-report/02-CONTEXT.md
@.planning/phases/02-ai-analysis-engine-report/02-RESEARCH.md
@.planning/phases/02-ai-analysis-engine-report/02-04-SUMMARY.md

<interfaces>
<!-- Plan 01~04에서 생성된 핵심 인터페이스 -->

From packages/core/src/analysis/runner.ts:

```typescript
export async function runAnalysisPipeline(jobId: number): Promise<{
  results: Record<string, AnalysisModuleResult>;
  completedModules: string[];
  failedModules: string[];
}>;
```

From packages/core/src/analysis/persist-analysis.ts:

```typescript
export async function persistAnalysisReport(data: {
  jobId: number;
  title: string;
  markdownContent: string;
  oneLiner?: string;
  metadata?: { keyword, dateRange, modulesCompleted, modulesFailed, totalTokens, generatedAt };
}): Promise<...>;
```

From packages/ai-gateway/src/gateway.ts:

```typescript
export async function analyzeText(prompt: string, options?: AIGatewayOptions): Promise<{ text, usage, ... }>;
```

From packages/core/src/analysis/types.ts:

```typescript
export interface AnalysisModuleResult<T = unknown> {
  module: string;
  status: 'completed' | 'failed';
  result?: T;
  usage?: { inputTokens; outputTokens; totalTokens; provider; model };
  errorMessage?: string;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 통합 리포트 생성기 + PDF 내보내기</name>
  <files>
    packages/core/src/report/generator.ts,
    packages/core/src/report/pdf-exporter.ts,
    packages/core/src/report/index.ts,
    packages/core/src/index.ts,
    packages/core/tests/report.test.ts,
    packages/core/tests/analysis-runner.test.ts
  </files>
  <read_first>
    packages/core/src/analysis/runner.ts,
    packages/core/src/analysis/types.ts,
    packages/core/src/analysis/persist-analysis.ts,
    packages/ai-gateway/src/gateway.ts,
    docs/prompt.md
  </read_first>
  <behavior>
    - Test: generateIntegratedReport 함수가 export된다
    - Test: exportToPdf 함수가 export된다
    - Test: generateIntegratedReport가 jobId, keyword, results 파라미터를 받는다
  </behavior>
  <action>
    **1. packages/core/src/report/generator.ts 생성 (per D-04, D-05):**

    ```typescript
    import { analyzeText } from '@ai-signalcraft/ai-gateway';
    import { persistAnalysisReport } from '../analysis/persist-analysis';
    import type { AnalysisModuleResult } from '../analysis/types';
    import { MODULE_MODEL_MAP } from '../analysis/types';

    export interface ReportGenerationInput {
      jobId: number;
      keyword: string;
      dateRange: { start: Date; end: Date };
      results: Record<string, AnalysisModuleResult>;
      completedModules: string[];
      failedModules: string[];
    }

    // D-04 2단계: 모든 모듈 결과를 AI에 넘겨 자연어 종합 리포트 생성
    export async function generateIntegratedReport(input: ReportGenerationInput): Promise<{
      markdownContent: string;
      oneLiner: string;
      totalTokens: number;
    }> {
      // 모듈 결과를 JSON으로 직렬화
      const resultsJson = JSON.stringify(
        Object.fromEntries(
          Object.entries(input.results)
            .filter(([_, r]) => r.status === 'completed')
            .map(([k, r]) => [k, r.result])
        ),
        null,
        2,
      );

      // 누락 섹션 명시
      const failedSection = input.failedModules.length > 0
        ? `\n\n> **주의:** 다음 분석 모듈이 실패하여 해당 섹션은 포함되지 않았습니다: ${input.failedModules.join(', ')}`
        : '';

      // final-summary 결과에서 oneLiner 추출
      const finalSummaryResult = input.results['final-summary'];
      const oneLiner = (finalSummaryResult?.status === 'completed' && finalSummaryResult?.result)
        ? (finalSummaryResult.result as any).oneLiner ?? ''
        : '';

      const config = MODULE_MODEL_MAP['integrated-report'];

      const prompt = `당신은 정치·여론·미디어 전략 보고서를 작성하는 최고 수준의 데이터 전략가입니다.

아래의 분석 결과 데이터를 기반으로 **종합 전략 리포트**를 마크다운 형식으로 작성하세요.

## 리포트 정보

- 분석 대상: ${input.keyword}
- 분석 기간: ${input.dateRange.start.toISOString().split('T')[0]} ~ ${input.dateRange.end.toISOString().split('T')[0]}

## 분석 결과 데이터

${resultsJson}

## 작성 지침

1.  **핵심 -> 분석 -> 전략** 순서로 작성
2.  각 섹션에 해당하는 분석 모듈 결과를 자연어로 풀어서 설명
3.  섹션 구조:
    - # 종합 분석 리포트: [키워드]
    - ## 한 줄 요약 (final-summary의 oneLiner 활용)
    - ## 1. 전체 여론 구조 (macro-view 결과)
    - ## 2. 집단별 반응 분석 (segmentation 결과)
    - ## 3. 감정 및 프레임 분석 (sentiment-framing 결과)
    - ## 4. 메시지 효과 분석 (message-impact 결과)
    - ## 5. 리스크 분석 (risk-map 결과)
    - ## 6. 기회 분석 (opportunity 결과)
    - ## 7. 전략 도출 (strategy 결과)
    - ## 8. 최종 전략 요약 (final-summary 결과)
4.  전략 중심으로 작성, 단순 요약 금지
5.  근거 없는 추측 금지${failedSection}`;

          const result = await analyzeText(prompt, {
            provider: config.provider,
            model: config.model,
            systemPrompt: '당신은 정치·여론·미디어 전략을 설계하는 최고 수준의 데이터 전략가이자 선거 캠프 수석 분석관입니다. 단순 요약이 아니라, 실제 의사결정자가 즉시 전략을 실행할 수 있는 수준의 분석 보고서를 작성합니다.',
            maxOutputTokens: 16384,
          });

          const markdownContent = result.text;

          // 총 토큰 계산
          const moduleTokens = Object.values(input.results)
            .filter(r => r.usage)
            .reduce((sum, r) => sum + (r.usage?.totalTokens ?? 0), 0);
          const reportTokens = (result.usage as any)?.totalTokens ?? 0;
          const totalTokens = moduleTokens + reportTokens;

          // DB에 리포트 저장 (D-07)
          await persistAnalysisReport({
            jobId: input.jobId,
            title: `${input.keyword} 종합 분석 리포트`,
            markdownContent,
            oneLiner,
            metadata: {
              keyword: input.keyword,
              dateRange: {
                start: input.dateRange.start.toISOString(),
                end: input.dateRange.end.toISOString(),
              },
              modulesCompleted: input.completedModules,
              modulesFailed: input.failedModules,
              totalTokens,
              generatedAt: new Date().toISOString(),
            },
          });

          return { markdownContent, oneLiner, totalTokens };
        }
        ```

        **2. packages/core/src/report/pdf-exporter.ts 생성 (per D-05, REPT-03):**

        Playwright를 사용하여 마크다운 -> HTML -> PDF 변환. 추가 의존성 없이 기존 Playwright 활용.

        ```typescript
        import { chromium } from 'playwright';

        export interface PdfExportOptions {
          title?: string;
          format?: 'A4' | 'Letter';
          margin?: { top?: string; bottom?: string; left?: string; right?: string };
        }

        export async function exportToPdf(
          markdownContent: string,
          outputPath: string,
          options: PdfExportOptions = {},
        ): Promise<void> {
          // 마크다운 -> HTML 변환 (간단한 변환기 사용 또는 직접 구현)
          // marked 라이브러리가 없으므로 Playwright에서 마크다운을 렌더링하는 HTML 템플릿 사용
          const html = buildHtmlFromMarkdown(markdownContent, options.title ?? '분석 리포트');

          const browser = await chromium.launch();
          const page = await browser.newPage();
          await page.setContent(html, { waitUntil: 'networkidle' });
          await page.pdf({
            path: outputPath,
            format: options.format ?? 'A4',
            margin: options.margin ?? { top: '20mm', bottom: '20mm', left: '15mm', right: '15mm' },
            printBackground: true,
          });
          await browser.close();
        }

        function buildHtmlFromMarkdown(markdown: string, title: string): string {
          // 기본 마크다운 -> HTML 변환 (# -> h1, ## -> h2, **bold**, *italic*, - list, etc.)
          // 간단한 정규식 기반 변환 + CSS 스타일링
          let html = markdown
            .replace(/^### (.*$)/gm, '<h3>$1</h3>')
            .replace(/^## (.*$)/gm, '<h2>$1</h2>')
            .replace(/^# (.*$)/gm, '<h1>$1</h1>')
            .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
            .replace(/\*(.*?)\*/g, '<em>$1</em>')
            .replace(/^- (.*$)/gm, '<li>$1</li>')
            .replace(/^> (.*$)/gm, '<blockquote>$1</blockquote>')
            .replace(/\n\n/g, '</p><p>')
            .replace(/\n/g, '<br/>');

          // li 태그를 ul로 감싸기
          html = html.replace(/(<li>.*?<\/li>(?:\s*<li>.*?<\/li>)*)/gs, '<ul>$1</ul>');

          return `<!DOCTYPE html>
        <html lang="ko">
        <head>
          <meta charset="UTF-8">
          <title>${title}</title>
          <style>
            body { font-family: 'Noto Sans KR', sans-serif; line-height: 1.8; color: #333; max-width: 800px; margin: 0 auto; padding: 20px; }
            h1 { color: #1a1a2e; border-bottom: 2px solid #16213e; padding-bottom: 10px; }
            h2 { color: #16213e; margin-top: 30px; }
            h3 { color: #0f3460; }
            blockquote { border-left: 4px solid #e94560; padding-left: 15px; color: #666; background: #f9f9f9; margin: 15px 0; padding: 10px 15px; }
            ul { padding-left: 20px; }
            li { margin: 5px 0; }
            strong { color: #1a1a2e; }
            table { border-collapse: collapse; width: 100%; margin: 15px 0; }
            th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
            th { background: #16213e; color: white; }
          </style>
        </head>
        <body><p>${html}</p></body>
        </html>`;
        }
        ```

        **3. packages/core/src/report/index.ts:**
        ```typescript
        export * from './generator';
        export * from './pdf-exporter';
        ```

        **4. packages/core/src/index.ts 수정:**
        `export * from './report';` 추가.

        **5. packages/core/src/analysis/runner.ts 수정:**
        `runAnalysisPipeline` 끝에 리포트 생성 호출 추가:

        ```typescript
        import { generateIntegratedReport } from '../report/generator';

        // runAnalysisPipeline 함수 끝에:
        // D-04: 모든 분석 완료 후 통합 리포트 생성
        const report = await generateIntegratedReport({
          jobId: input.jobId,
          keyword: input.keyword,
          dateRange: input.dateRange,
          results: allResults,
          completedModules,
          failedModules,
        });

        return { results: allResults, completedModules, failedModules, report };
        ```

        **6. packages/core/tests/analysis-runner.test.ts 업데이트:**
        runner.ts의 runAnalysisPipeline 반환 타입이 변경되었으므로(report 필드 추가), 기존 Plan 04에서 생성된 analysis-runner.test.ts의 관련 테스트를 업데이트한다. 반환값에 report 필드가 포함됨을 검증하는 테스트 추가.

      </action>
      <verify>
        <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test && pnpm -r build</automated>
      </verify>
      <acceptance_criteria>
        - packages/core/src/report/generator.ts contains `export async function generateIntegratedReport`
        - packages/core/src/report/generator.ts contains `analyzeText`
        - packages/core/src/report/generator.ts contains `persistAnalysisReport`
        - packages/core/src/report/pdf-exporter.ts contains `export async function exportToPdf`
        - packages/core/src/report/pdf-exporter.ts contains `chromium`
        - packages/core/src/report/index.ts contains `export * from './generator'`
        - packages/core/src/report/index.ts contains `export * from './pdf-exporter'`
        - packages/core/src/index.ts contains `export * from './report'`
        - packages/core/src/analysis/runner.ts contains `generateIntegratedReport`
        - packages/core/tests/analysis-runner.test.ts가 report 필드를 포함한 반환 타입을 검증한다
        - `pnpm -r build` exits 0
        - `pnpm --filter @ai-signalcraft/core test` exits 0
      </acceptance_criteria>
      <done>AI 종합 리포트가 마크다운으로 생성되고 DB에 저장되며, Playwright 기반 PDF 내보내기가 가능하고, 분석 러너에서 자동으로 리포트 생성이 호출됨</done>
    </task>

</tasks>

<verification>
- `pnpm -r build` 전체 빌드 통과
- `pnpm --filter @ai-signalcraft/core test` 테스트 통과
- generateIntegratedReport가 모든 모듈 결과를 통합하여 마크다운 생성
- exportToPdf가 Playwright로 PDF 변환
- 분석 러너가 리포트 생성까지 포함한 완전한 파이프라인 실행
</verification>

<success_criteria>

1. `pnpm -r build` exits 0
2. `pnpm --filter @ai-signalcraft/core test` exits 0
3. 통합 리포트 생성기가 8개 모듈 결과를 AI로 통합하여 마크다운 생성
4. PDF 내보내기가 Playwright 기반으로 구현
5. 분석 러너가 리포트 생성을 자동 호출
   </success_criteria>

<output>
After completion, create `.planning/phases/02-ai-analysis-engine-report/02-05-SUMMARY.md`
</output>
