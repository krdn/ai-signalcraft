---
phase: 02-ai-analysis-engine-report
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/core/src/analysis/schemas/risk-map.schema.ts
  - packages/core/src/analysis/schemas/opportunity.schema.ts
  - packages/core/src/analysis/schemas/strategy.schema.ts
  - packages/core/src/analysis/schemas/final-summary.schema.ts
  - packages/core/src/analysis/modules/risk-map.ts
  - packages/core/src/analysis/modules/opportunity.ts
  - packages/core/src/analysis/modules/strategy.ts
  - packages/core/src/analysis/modules/final-summary.ts
  - packages/core/src/analysis/modules/index.ts
  - packages/core/src/analysis/schemas/index.ts
  - packages/core/tests/analysis-modules-stage2.test.ts
autonomous: true
requirements: [DEEP-03, DEEP-04, DEEP-05, REPT-02]

must_haves:
  truths:
    - '모듈5(risk-map)가 Top 3 리스크와 영향도/확산 가능성을 분석한다'
    - '모듈6(opportunity)가 긍정 요소와 미활용 영역을 분석한다'
    - '모듈7(strategy)가 타겟/메시지/콘텐츠/리스크 대응 전략을 도출한다'
    - '모듈8(final-summary)가 현재 상태 + 승부 핵심 한 줄 요약을 생성한다'
    - '모듈5~8은 buildPromptWithContext로 선행 분석 결과를 참조할 수 있다'
  artifacts:
    - path: 'packages/core/src/analysis/modules/risk-map.ts'
      provides: '리스크 분석 모듈'
      exports: ['riskMapModule']
    - path: 'packages/core/src/analysis/modules/strategy.ts'
      provides: '전략 도출 모듈'
      exports: ['strategyModule']
    - path: 'packages/core/src/analysis/modules/final-summary.ts'
      provides: '최종 전략 요약 모듈'
      exports: ['finalSummaryModule']
  key_links:
    - from: 'packages/core/src/analysis/modules/risk-map.ts'
      to: 'packages/core/src/analysis/types.ts'
      via: 'implements AnalysisModule with buildPromptWithContext'
      pattern: 'buildPromptWithContext'
    - from: 'packages/core/src/analysis/modules/final-summary.ts'
      to: 'packages/core/src/analysis/types.ts'
      via: 'implements AnalysisModule with buildPromptWithContext'
      pattern: 'buildPromptWithContext'
---

<objective>
Stage 2 분석 모듈 4개 구현: 모듈5(리스크), 모듈6(기회), 모듈7(전략), 모듈8(최종 요약). 이 모듈들은 Stage 1 결과를 참조하여 심층 분석을 수행한다.

Purpose: Stage 1 결과를 바탕으로 리스크/기회/전략을 도출하고, 최종 한 줄 요약을 생성한다 (per D-10 2단계+3단계).
Output: 4개 분석 모듈 파일 + 4개 Zod 스키마 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ai-analysis-engine-report/02-CONTEXT.md
@.planning/phases/02-ai-analysis-engine-report/02-RESEARCH.md
@.planning/phases/02-ai-analysis-engine-report/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 -->

From packages/core/src/analysis/types.ts:

```typescript
export interface AnalysisModule<T = unknown> {
  readonly name: string;
  readonly displayName: string;
  readonly provider: AIProvider;
  readonly model: string;
  readonly schema: z.ZodType<T>;
  buildPrompt(data: AnalysisInput): string;
  buildSystemPrompt(): string;
  buildPromptWithContext?(data: AnalysisInput, priorResults: Record<string, unknown>): string;
}

export const MODULE_MODEL_MAP; // risk-map, opportunity, strategy, final-summary -> anthropic claude-sonnet-4-20250514
```

From Plan 02 schemas (병렬 생성이므로 타입만 참조):

- MacroViewResult: overallDirection, summary, timeline, inflectionPoints, dailyMentionTrend
- SegmentationResult: platformSegments, audienceGroups, highInfluenceGroup
- SentimentFramingResult: sentimentRatio, topKeywords, positiveFrames, negativeFrames, frameConflict
- MessageImpactResult: successMessages, failureMessages, highSpreadContentTypes
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage 2 Zod 스키마 4개 + 분석 모듈 4개 구현</name>
  <files>
    packages/core/src/analysis/schemas/risk-map.schema.ts,
    packages/core/src/analysis/schemas/opportunity.schema.ts,
    packages/core/src/analysis/schemas/strategy.schema.ts,
    packages/core/src/analysis/schemas/final-summary.schema.ts,
    packages/core/src/analysis/schemas/index.ts,
    packages/core/src/analysis/modules/risk-map.ts,
    packages/core/src/analysis/modules/opportunity.ts,
    packages/core/src/analysis/modules/strategy.ts,
    packages/core/src/analysis/modules/final-summary.ts,
    packages/core/src/analysis/modules/index.ts,
    packages/core/tests/analysis-modules-stage2.test.ts
  </files>
  <read_first>
    packages/core/src/analysis/types.ts,
    packages/core/src/analysis/modules/index.ts,
    packages/core/src/analysis/schemas/index.ts,
    packages/core/src/analysis/index.ts,
    packages/ai-gateway/src/gateway.ts,
    docs/prompt.md
  </read_first>
  <behavior>
    - Test: riskMapModule은 name='risk-map', provider='anthropic'이다
    - Test: opportunityModule은 name='opportunity', provider='anthropic'이다
    - Test: strategyModule은 name='strategy', provider='anthropic'이다
    - Test: finalSummaryModule은 name='final-summary', provider='anthropic'이다
    - Test: 각 모듈에 buildPromptWithContext 메서드가 존재한다
    - Test: buildPromptWithContext(mockInput, mockPriorResults)가 priorResults 키워드를 포함한 문자열을 반환한다
    - Test: RiskMapSchema.parse(validData)가 성공한다
    - Test: FinalSummarySchema에 oneLiner 필드가 존재한다
  </behavior>
  <action>
    **1. risk-map.schema.ts (모듈5: DEEP-03):**
    ```typescript
    export const RiskMapSchema = z.object({
      topRisks: z.array(z.object({
        rank: z.number(),
        title: z.string(),
        description: z.string(),
        impactLevel: z.enum(['critical', 'high', 'medium', 'low']),
        spreadProbability: z.number().describe('0~1 확산 가능성'),
        currentStatus: z.string(),
        triggerConditions: z.array(z.string()),
      })).max(5).describe('Top 3~5 리스크'),
      overallRiskLevel: z.enum(['critical', 'high', 'medium', 'low']),
      riskTrend: z.enum(['increasing', 'stable', 'decreasing']),
    });
    export type RiskMapResult = z.infer<typeof RiskMapSchema>;
    ```

    **2. opportunity.schema.ts (모듈6: DEEP-04):**
    ```typescript
    export const OpportunitySchema = z.object({
      positiveAssets: z.array(z.object({
        title: z.string(),
        description: z.string(),
        expandability: z.enum(['high', 'medium', 'low']),
        currentUtilization: z.enum(['fully', 'partially', 'unused']),
        recommendation: z.string(),
      })).describe('확장 가능한 긍정 요소'),
      untappedAreas: z.array(z.object({
        area: z.string(),
        potential: z.string(),
        approach: z.string(),
      })).describe('미활용 영역'),
      priorityOpportunity: z.object({
        title: z.string(),
        reason: z.string(),
        actionPlan: z.string(),
      }),
    });
    export type OpportunityResult = z.infer<typeof OpportunitySchema>;
    ```

    **3. strategy.schema.ts (모듈7: DEEP-05):**
    ```typescript
    export const StrategySchema = z.object({
      targetStrategy: z.object({
        primaryTarget: z.string(),
        secondaryTargets: z.array(z.string()),
        approach: z.string(),
      }).describe('타겟 전략'),
      messageStrategy: z.object({
        coreMessage: z.string(),
        supportingMessages: z.array(z.string()),
        toneAndManner: z.string(),
      }).describe('메시지 전략'),
      contentStrategy: z.object({
        recommendedFormats: z.array(z.string()),
        keyTopics: z.array(z.string()),
        distributionChannels: z.array(z.string()),
      }).describe('콘텐츠 전략'),
      riskResponse: z.object({
        immediateActions: z.array(z.string()),
        preventiveActions: z.array(z.string()),
        contingencyPlan: z.string(),
      }).describe('리스크 대응'),
    });
    export type StrategyResult = z.infer<typeof StrategySchema>;
    ```

    **4. final-summary.schema.ts (모듈8: REPT-02):**
    ```typescript
    export const FinalSummarySchema = z.object({
      oneLiner: z.string().describe('현재 상태 + 승부 핵심 한 줄 요약'),
      currentState: z.object({
        summary: z.string(),
        sentiment: z.enum(['positive', 'negative', 'mixed']),
        keyFactor: z.string(),
      }),
      criticalActions: z.array(z.object({
        priority: z.number(),
        action: z.string(),
        expectedImpact: z.string(),
        timeline: z.string(),
      })).max(5).describe('최우선 실행 과제'),
      outlook: z.object({
        shortTerm: z.string(),
        mediumTerm: z.string(),
        keyVariable: z.string(),
      }),
    });
    export type FinalSummaryResult = z.infer<typeof FinalSummarySchema>;
    ```

    **5. 각 모듈 파일 (modules/risk-map.ts 등):**
    - AnalysisModule<T> 구현
    - provider: 'anthropic', model: 'claude-sonnet-4-20250514' (per D-03, 심층 분석)
    - **buildPromptWithContext** 필수 구현: priorResults에서 Stage 1 결과(macro-view, segmentation, sentiment-framing, message-impact)를 JSON.stringify로 포함
    - buildPrompt도 기본 구현 (priorResults 없이 독립 실행 가능하게)
    - strategy 모듈은 risk, opportunity 결과도 priorResults에서 참조

    **6. schemas/index.ts 수정:**
    기존 Stage 1 스키마 exports에 risk-map, opportunity, strategy, final-summary 스키마 추가.

    **7. modules/index.ts 수정:**
    기존 Stage 1 모듈 exports에 riskMapModule, opportunityModule, strategyModule, finalSummaryModule 추가.

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/analysis/modules/risk-map.ts contains `export const riskMapModule`
    - packages/core/src/analysis/modules/opportunity.ts contains `export const opportunityModule`
    - packages/core/src/analysis/modules/strategy.ts contains `export const strategyModule`
    - packages/core/src/analysis/modules/final-summary.ts contains `export const finalSummaryModule`
    - packages/core/src/analysis/schemas/risk-map.schema.ts contains `topRisks`
    - packages/core/src/analysis/schemas/strategy.schema.ts contains `targetStrategy`
    - packages/core/src/analysis/schemas/final-summary.schema.ts contains `oneLiner`
    - 각 모듈 파일에 `buildPromptWithContext` 메서드가 포함됨
    - 각 모듈의 provider가 'anthropic'으로 설정됨
    - `pnpm -r build` exits 0
    - `pnpm --filter @ai-signalcraft/core test` exits 0
  </acceptance_criteria>
  <done>Stage 2 분석 모듈 4개가 buildPromptWithContext로 선행 결과를 참조하고, Claude Sonnet 모델을 사용하도록 설정되어 빌드/테스트 통과</done>
</task>

</tasks>

<verification>
- `pnpm -r build` 전체 빌드 통과
- `pnpm --filter @ai-signalcraft/core test` 테스트 통과
- 8개 분석 모듈 전체가 modules/index.ts에서 export됨
- 8개 Zod 스키마 전체가 schemas/index.ts에서 export됨
</verification>

<success_criteria>

1. `pnpm -r build` exits 0
2. `pnpm --filter @ai-signalcraft/core test` exits 0
3. riskMapModule, opportunityModule, strategyModule, finalSummaryModule이 export됨
4. 모든 모듈에 buildPromptWithContext가 구현됨
   </success_criteria>

<output>
After completion, create `.planning/phases/02-ai-analysis-engine-report/02-03-SUMMARY.md`
</output>
