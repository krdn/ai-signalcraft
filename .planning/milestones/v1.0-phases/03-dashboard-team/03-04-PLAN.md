---
phase: 03-dashboard-team
plan: 04
type: execute
wave: 3
depends_on: ['03-02']
files_modified:
  - apps/web/src/components/report/report-viewer.tsx
  - apps/web/src/components/report/section-nav.tsx
  - apps/web/src/components/report/report-view.tsx
  - apps/web/src/server/trpc/routers/report.ts
  - apps/web/src/server/trpc/router.ts
  - apps/web/src/app/page.tsx
autonomous: true
requirements:
  - DASH-05
  - DASH-06

# DASH-06 coverage note: REQUIREMENTS.md DASH-06은 "분석 히스토리 목록 (과거 분석 결과 조회, 비교)"이나,
# 03-CONTEXT.md Deferred Ideas에서 "분석 결과 비교 뷰 (A vs B 나란히)"가 deferred 처리됨.
# Plan 02에서 히스토리 "조회"를 구현하고, 이 Plan에서는 리포트 뷰어를 통해 과거 결과를 상세 열람하는 기능을 제공한다.
# "비교" 기능은 향후 Phase로 이월한다.

must_haves:
  truths:
    - 'AI 리포트가 마크다운 렌더링으로 전문 표시된다'
    - '왼쪽 섹션 네비게이션에서 클릭 시 해당 섹션으로 스크롤된다'
    - 'PDF 내보내기 버튼이 상단에 표시되고 클릭 시 다운로드된다'
    - '리포트가 없는 상태에서 적절한 빈 상태 메시지가 표시된다'
  artifacts:
    - path: 'apps/web/src/components/report/report-viewer.tsx'
      provides: '마크다운 리포트 렌더러'
      contains: 'ReactMarkdown'
    - path: 'apps/web/src/components/report/section-nav.tsx'
      provides: '섹션 네비게이션'
      contains: 'scroll'
    - path: 'apps/web/src/server/trpc/routers/report.ts'
      provides: '리포트 tRPC 라우터'
      exports: ['reportRouter']
  key_links:
    - from: 'apps/web/src/components/report/report-view.tsx'
      to: 'apps/web/src/server/trpc/routers/report.ts'
      via: 'trpc.report.getByJobId 쿼리'
      pattern: "report\\.getByJobId"
    - from: 'apps/web/src/components/report/section-nav.tsx'
      to: 'apps/web/src/components/report/report-viewer.tsx'
      via: 'scrollIntoView on section click'
      pattern: 'scrollIntoView'
---

<objective>
AI 리포트 탭의 마크다운 뷰어 + 섹션 네비게이션 + PDF 내보내기 기능을 구현한다.

Purpose: Phase 2에서 생성된 AI 종합 리포트를 섹션별로 탐색하고 PDF로 내보낼 수 있게 한다. 히스토리에서 선택한 과거 분석의 리포트도 이 뷰어로 열람한다.
Output: 마크다운 리포트 뷰어 + 좌측 섹션 네비게이션 + PDF 내보내기 버튼

**DASH-06 scope:** 이 Plan은 히스토리에서 선택한 분석의 리포트 상세 열람을 지원하여 DASH-06 "조회" 기능을 보완한다. "비교" 기능은 03-CONTEXT.md Deferred Ideas에서 deferred 처리됨.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dashboard-team/03-CONTEXT.md
@.planning/phases/03-dashboard-team/03-UI-SPEC.md
@.planning/phases/03-dashboard-team/03-02-SUMMARY.md

<interfaces>
From packages/core/src/db/schema/analysis.ts:
```typescript
export const analysisReports = pgTable('analysis_reports', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  jobId: integer('job_id').references(() => collectionJobs.id).notNull(),
  title: text('title').notNull(),
  markdownContent: text('markdown_content').notNull(),
  oneLiner: text('one_liner'),  // 한 줄 요약
  metadata: jsonb('metadata').$type<{
    keyword: string;
    dateRange: { start: string; end: string };
    modulesCompleted: string[];
    modulesFailed: string[];
    totalTokens: number;
    generatedAt: string;
  }>(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
});
```

Phase 2 리포트 마크다운 구조 (docs/prompt.md 기반):

- `## 1. 종합 개요` (macro-view)
- `## 2. 감성 및 프레임 분석` (sentiment-framing)
- `## 3. 집단별 반응 분석` (segmentation)
- `## 4. 메시지 효과 분석` (message-impact)
- `## 5. 리스크 분석` (risk-map)
- `## 6. 기회 분석` (opportunity)
- `## 7. 전략 제언` (strategy)
- `## 8. 최종 전략 요약` (final-summary)

From apps/web/src/server/trpc/init.ts (Plan 01):

```typescript
export const protectedProcedure, router;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 리포트 tRPC 라우터 + 마크다운 뷰어 + 섹션 네비 + PDF 내보내기</name>
  <files>
    apps/web/src/server/trpc/routers/report.ts,
    apps/web/src/server/trpc/router.ts,
    apps/web/src/components/report/report-viewer.tsx,
    apps/web/src/components/report/section-nav.tsx,
    apps/web/src/components/report/report-view.tsx,
    apps/web/src/app/page.tsx
  </files>
  <read_first>
    packages/core/src/db/schema/analysis.ts,
    packages/core/src/report/index.ts,
    apps/web/src/server/trpc/router.ts,
    apps/web/src/app/page.tsx,
    docs/prompt.md,
    .planning/phases/03-dashboard-team/03-UI-SPEC.md
  </read_first>
  <action>
1. apps/web/src/server/trpc/routers/report.ts:
```typescript
import { z } from 'zod';
import { protectedProcedure, router } from '../init';
import { db, analysisReports } from '@ai-signalcraft/core';
import { eq } from 'drizzle-orm';

export const reportRouter = router({
getByJobId: protectedProcedure
.input(z.object({ jobId: z.number() }))
.query(async ({ input, ctx }) => {
const [report] = await ctx.db.select()
.from(analysisReports)
.where(eq(analysisReports.jobId, input.jobId))
.limit(1);
return report ?? null;
}),
});

````

2. router.ts 수정 -- reportRouter 추가:
```typescript
import { reportRouter } from './routers/report';
// appRouter에 report: reportRouter 추가
````

3. apps/web/src/components/report/section-nav.tsx -- 'use client':
   - Props: `sections: Array<{ id: string; title: string }>`, `activeSection: string`
   - 왼쪽 사이드바: 200px 고정 너비, sticky, secondary background (per UI-SPEC)
   - 각 섹션: 클릭 시 `document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })` 호출
   - 활성 섹션: accent color 좌측 border (2px) + accent text
   - Heading 14px semibold
   - 모바일(< 768px): 수평 스크롤 탭으로 변환 (per UI-SPEC Responsive)

4. apps/web/src/components/report/report-viewer.tsx -- 'use client':
   - Props: `markdownContent: string`
   - react-markdown + remark-gfm으로 마크다운 렌더링 (per D-10)
   - 커스텀 components 설정:
     - h2: id를 섹션 제목에서 자동 생성 (예: "1-종합-개요"), Heading 18px semibold
     - h3: 16px semibold
     - p: Body 14px regular
     - table: shadcn Table 스타일
     - code: Geist Mono, secondary background
     - ul/ol: 들여쓰기 + marker
   - IntersectionObserver로 현재 보이는 섹션 감지하여 activeSection 상위 전달
   - ScrollArea 래핑 (shadcn scroll-area)
   - max-w-3xl (per UI-SPEC)

5. apps/web/src/components/report/report-view.tsx -- 'use client':
   - Props: `jobId: number`
   - trpc.report.getByJobId 쿼리
   - 마크다운에서 `## N. 제목` 패턴을 파싱하여 sections 배열 생성
   - 레이아웃 (per D-09 UI-SPEC):
     ```
     flex flex-row
     [SectionNav (200px fixed)] [ReportViewer (flex-1, max-w-3xl)]
     ```
   - 상단 우측: "PDF 내보내기" Button (secondary variant)
     - 클릭 시 `/api/report/pdf?jobId={jobId}` 다운로드 또는 Phase 2의 PDF 생성 로직 호출
     - Phase 2의 `packages/core/src/report/` export에 PDF 생성 함수가 있다면 tRPC mutation으로 호출
     - 없으면 window.print() 기반 간이 PDF 또는 별도 API route
   - 로딩: Skeleton (text blocks) -- per UI-SPEC States Matrix
   - 빈 상태: "리포트 없음" placeholder
   - 리포트 메타데이터 표시:
     - 제목: report.title (Display 28px semibold)
     - 한 줄 요약: report.oneLiner (Body 14px, accent border-left)
     - 생성일: report.metadata.generatedAt (Label 12px, muted)

6. page.tsx 업데이트:
   - "AI 리포트" 탭에 ReportView 컴포넌트 연결, jobId 전달
     </action>
     <verify>
     <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/web build 2>&1 | tail -5</automated>
     </verify>
     <acceptance_criteria>
   - apps/web/src/server/trpc/routers/report.ts에 `reportRouter` export가 포함된다
   - apps/web/src/server/trpc/routers/report.ts에 `getByJobId` 프로시저가 포함된다
   - apps/web/src/server/trpc/router.ts에 `report: reportRouter` 가 포함된다
   - apps/web/src/components/report/report-viewer.tsx에 `ReactMarkdown` 또는 `react-markdown` import가 포함된다
   - apps/web/src/components/report/report-viewer.tsx에 `remark-gfm` 또는 `remarkGfm` 가 포함된다
   - apps/web/src/components/report/section-nav.tsx에 `scrollIntoView` 가 포함된다
   - apps/web/src/components/report/report-view.tsx에 `"PDF 내보내기"` 문자열이 포함된다
   - apps/web/src/components/report/report-view.tsx에 `SectionNav` 와 `ReportViewer` import가 포함된다
   - apps/web/src/app/page.tsx에 `ReportView` import가 포함된다
   - `pnpm --filter @ai-signalcraft/web build` exit 0
     </acceptance_criteria>
     <done>AI 리포트 탭에서 마크다운 렌더링 + 섹션 네비 + PDF 내보내기가 동작한다</done>
     </task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/web build` exit 0
- report tRPC 라우터가 appRouter에 등록됨
- 마크다운 리포트가 커스텀 컴포넌트로 렌더링됨
- 섹션 클릭 시 smooth scroll 동작
</verification>

<success_criteria>

- AI 리포트가 react-markdown으로 전문 렌더링됨
- 왼쪽 섹션 네비에서 8개 섹션으로 빠른 이동 가능
- IntersectionObserver로 스크롤 시 활성 섹션 자동 업데이트
- PDF 내보내기 버튼이 동작함
- 모바일에서 섹션 네비가 수평 탭으로 변환됨
  </success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-team/03-04-SUMMARY.md`
</output>
