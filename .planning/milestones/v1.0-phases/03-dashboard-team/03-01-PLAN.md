---
phase: 03-dashboard-team
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/postcss.config.mjs
  - apps/web/src/app/globals.css
  - apps/web/components.json
  - apps/web/src/lib/utils.ts
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/vitest.config.ts
  - apps/web/src/__tests__/setup.ts
  - apps/web/src/__tests__/app-smoke.test.tsx
  - packages/core/src/db/schema/auth.ts
  - packages/core/src/db/schema/index.ts
  - packages/core/src/index.ts
  - apps/web/src/server/trpc/init.ts
  - apps/web/src/server/trpc/router.ts
  - apps/web/src/app/api/trpc/[trpc]/route.ts
  - apps/web/src/lib/trpc.ts
  - apps/web/src/components/providers.tsx
  - apps/web/src/server/auth.ts
  - apps/web/src/app/api/auth/[...nextauth]/route.ts
  - apps/web/.env.example
autonomous: true
requirements:
  - TEAM-01
  - DASH-01

must_haves:
  truths:
    - 'pnpm --filter web build 가 에러 없이 완료된다'
    - 'pnpm --filter web test -- --run 이 최소 1개 테스트를 green으로 통과한다'
    - 'shadcn/ui 컴포넌트가 다크모드 기본으로 렌더링된다'
    - 'tRPC /api/trpc 엔드포인트가 응답한다'
    - 'NextAuth /api/auth 엔드포인트가 응답한다'
    - 'users, teams, team_members, invitations 테이블이 DB에 생성된다'
  artifacts:
    - path: 'apps/web/components.json'
      provides: 'shadcn/ui 설정'
      contains: 'tailwindcss'
    - path: 'apps/web/src/server/trpc/init.ts'
      provides: 'tRPC 초기화'
      exports: ['publicProcedure', 'protectedProcedure', 'router']
    - path: 'packages/core/src/db/schema/auth.ts'
      provides: '인증/팀 DB 스키마'
      contains: 'users'
    - path: 'apps/web/src/server/auth.ts'
      provides: 'NextAuth 설정'
      exports: ['auth', 'handlers']
    - path: 'apps/web/src/__tests__/app-smoke.test.tsx'
      provides: 'Wave 0 smoke test'
      contains: 'SignalCraft'
  key_links:
    - from: 'apps/web/src/app/layout.tsx'
      to: 'apps/web/src/components/providers.tsx'
      via: 'ThemeProvider + TRPCProvider 래핑'
      pattern: 'Providers'
    - from: 'apps/web/src/app/api/trpc/[trpc]/route.ts'
      to: 'apps/web/src/server/trpc/router.ts'
      via: 'fetchRequestHandler'
      pattern: 'appRouter'
    - from: 'apps/web/src/server/auth.ts'
      to: 'packages/core/src/db/schema/auth.ts'
      via: 'DrizzleAdapter'
      pattern: 'DrizzleAdapter'
---

<objective>
Phase 3 기반 스캐폴딩: shadcn/ui + Tailwind 4 + tRPC 11 + NextAuth v5 + 인증/팀 DB 스키마를 apps/web에 설치하고 초기화한다.

Purpose: 이후 모든 Plan이 의존하는 UI 프레임워크, API 레이어, 인증 시스템, DB 스키마의 기초를 확립한다.
Output: 다크모드 기본 Next.js 앱 셸 + tRPC 라우터 + NextAuth 설정 + auth/team DB 테이블
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-team/03-CONTEXT.md
@.planning/phases/03-dashboard-team/03-RESEARCH.md
@.planning/phases/03-dashboard-team/03-UI-SPEC.md

<interfaces>
<!-- 기존 core 패키지 export (web에서 import할 것) -->
From packages/core/src/index.ts:
```typescript
export { db } from './db';
export type { Database } from './db';
export * from './db/schema';
export { triggerCollection, createCollectorWorker, createPipelineWorker, redisConnection } from './queue';
export * from './types';
export * from './analysis';
export * from './report';
```

From packages/core/src/db/schema/collections.ts:

```typescript
export const collectionJobs = pgTable('collection_jobs', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  keyword: text('keyword').notNull(),
  startDate: timestamp('start_date').notNull(),
  endDate: timestamp('end_date').notNull(),
  status: text('status', { enum: ['pending', 'running', 'completed', 'partial_failure', 'failed'] })
    .notNull()
    .default('pending'),
  progress: jsonb('progress').$type<{
    naver: { status: string; articles: number; comments: number };
    youtube: { status: string; videos: number; comments: number };
  }>(),
  // ...
});
```

From packages/core/src/db/schema/analysis.ts:

```typescript
export const analysisResults = pgTable('analysis_results', { ... });
export const analysisReports = pgTable('analysis_reports', { ... });
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 의존성 설치 + shadcn/ui 초기화 + Tailwind 4 + 다크모드 테마 + smoke test</name>
  <files>
    apps/web/package.json,
    apps/web/postcss.config.mjs,
    apps/web/src/app/globals.css,
    apps/web/components.json,
    apps/web/src/lib/utils.ts,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/components/providers.tsx,
    apps/web/vitest.config.ts,
    apps/web/src/__tests__/setup.ts,
    apps/web/src/__tests__/app-smoke.test.tsx,
    apps/web/.env.example
  </files>
  <read_first>
    apps/web/package.json,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    .planning/phases/03-dashboard-team/03-UI-SPEC.md,
    .planning/phases/03-dashboard-team/03-RESEARCH.md
  </read_first>
  <action>
1. 의존성 설치 (apps/web):
```bash
pnpm --filter @ai-signalcraft/web add @trpc/server@latest @trpc/client@latest @trpc/tanstack-react-query@latest @tanstack/react-query@latest next-auth@beta @auth/drizzle-adapter bcryptjs recharts react-markdown remark-gfm @isoterik/react-word-cloud next-themes resend zod @ai-signalcraft/core@workspace:*

pnpm --filter @ai-signalcraft/web add -D @types/bcryptjs tailwindcss @tailwindcss/postcss vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react

````

2. shadcn/ui 초기화 (apps/web 디렉토리에서):
```bash
cd apps/web && npx shadcn@latest init --defaults
````

이 명령은 components.json, globals.css (CSS 변수 포함), lib/utils.ts (cn 함수), postcss.config.mjs를 자동 생성한다.

3. globals.css 수정 -- UI-SPEC 색상 토큰 적용:
   - Dominant dark: `--background: 240 10% 4%` (near-black)
   - Secondary dark: `--card: 240 6% 10%` (dark card)
   - Accent: `--primary: 217 91% 60%` (blue-500)
   - Destructive: `--destructive: 0 84% 60%` (red-500)
   - Card border: `--border: 240 4% 16%`
   - Muted text: `--muted-foreground: 240 5% 65%`
   - 차트 변수 추가:
     - `--chart-1: 142 71% 45%` (green - positive)
     - `--chart-2: 0 84% 60%` (red - negative)
     - `--chart-3: 240 5% 64%` (gray - neutral)
     - `--chart-4: 217 91% 60%` (blue - trend)
     - `--chart-5: 38 92% 50%` (amber - warning)
   - `.dark` 클래스를 기본으로 적용 (per D-03)

4. layout.tsx 수정:
   - `lang="ko"` 설정
   - Geist Sans + Geist Mono 폰트 import
   - `<Providers>` 컴포넌트로 래핑 (ThemeProvider + QueryClientProvider + TRPCProvider)
   - `defaultTheme="dark"` 설정 (per D-03)

5. providers.tsx 생성:
   - `'use client'` 선언
   - ThemeProvider (next-themes) -- attribute="class", defaultTheme="dark"
   - QueryClientProvider (TanStack Query)
   - tRPC 클라이언트 Provider

6. page.tsx -- 임시 "SignalCraft Dashboard" 텍스트 (후속 Plan에서 탭 구조로 교체)

7. vitest.config.ts 생성:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/__tests__/setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, './src') },
  },
});
```

8. src/**tests**/setup.ts:

```typescript
import '@testing-library/jest-dom';
```

9. src/**tests**/app-smoke.test.tsx -- Wave 0 최소 smoke test (Nyquist 충족):

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import Page from '../app/page';

describe('App smoke test', () => {
  it('renders the main page without crashing', () => {
    render(<Page />);
    expect(screen.getByText(/SignalCraft/i)).toBeInTheDocument();
  });
});
```

**참고:** Providers 래핑이 필요하면 테스트용 wrapper를 setup.ts에 추가하거나, page.tsx가 서버 컴포넌트이면 간단한 import + existence 테스트로 대체 가능. 핵심은 `pnpm --filter web test -- --run`이 최소 1개 테스트를 실행하여 green으로 통과하는 것.

10. .env.example 생성 -- 필요한 환경 변수 목록:

```
DATABASE_URL=postgresql://user:pass@192.168.0.5:5433/signalcraft
REDIS_URL=redis://192.168.0.5:6380
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
RESEND_API_KEY=your-resend-api-key
```

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/web build 2>&1 | tail -5 && pnpm --filter @ai-signalcraft/web test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/__tests__/app-smoke.test.tsx 파일이 존재한다
    - `pnpm --filter @ai-signalcraft/web test -- --run` 이 최소 1개 테스트 green으로 통과한다
    - apps/web/components.json 파일이 존재하고 "tailwindcss" 문자열을 포함한다
    - apps/web/src/app/globals.css에 `--chart-1: 142 71% 45%` 가 포함된다
    - apps/web/src/app/globals.css에 `--background: 240 10% 4%` 가 포함된다
    - apps/web/src/app/layout.tsx에 `lang="ko"` 가 포함된다
    - apps/web/src/app/layout.tsx에 `Providers` import가 포함된다
    - apps/web/src/components/providers.tsx에 `ThemeProvider` 가 포함된다
    - apps/web/src/components/providers.tsx에 `QueryClientProvider` 가 포함된다
    - apps/web/vitest.config.ts 파일이 존재한다
    - apps/web/.env.example에 `NEXTAUTH_SECRET` 가 포함된다
    - `pnpm --filter @ai-signalcraft/web build` 가 exit 0으로 완료된다
  </acceptance_criteria>
  <done>shadcn/ui + Tailwind 4 + 다크모드 테마 + 테스트 인프라가 설치되고 빌드 + 테스트가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: Auth/Team DB 스키마 + tRPC 초기화 + NextAuth 설정</name>
  <files>
    packages/core/src/db/schema/auth.ts,
    packages/core/src/db/schema/index.ts,
    packages/core/src/index.ts,
    apps/web/src/server/trpc/init.ts,
    apps/web/src/server/trpc/router.ts,
    apps/web/src/app/api/trpc/[trpc]/route.ts,
    apps/web/src/lib/trpc.ts,
    apps/web/src/server/auth.ts,
    apps/web/src/app/api/auth/[...nextauth]/route.ts
  </files>
  <read_first>
    packages/core/src/db/schema/collections.ts,
    packages/core/src/db/schema/analysis.ts,
    packages/core/src/db/schema/index.ts,
    packages/core/src/index.ts,
    packages/core/src/db/index.ts,
    .planning/phases/03-dashboard-team/03-RESEARCH.md
  </read_first>
  <action>
1. packages/core/src/db/schema/auth.ts 생성 -- NextAuth DrizzleAdapter 표준 + 팀 확장:

```typescript
import { pgTable, text, timestamp, integer, primaryKey, uniqueIndex } from 'drizzle-orm/pg-core';
import type { AdapterAccountType } from 'next-auth/adapters';

// NextAuth 필수 테이블
export const users = pgTable('users', {
  id: text('id')
    .primaryKey()
    .$defaultFn(() => crypto.randomUUID()),
  name: text('name'),
  email: text('email').notNull().unique(),
  emailVerified: timestamp('email_verified', { mode: 'date' }),
  image: text('image'),
  hashedPassword: text('hashed_password'), // Credentials 전용
  role: text('role', { enum: ['admin', 'member'] })
    .notNull()
    .default('member'),
  createdAt: timestamp('created_at').defaultNow().notNull(),
});

export const accounts = pgTable(
  'accounts',
  {
    userId: text('user_id')
      .references(() => users.id, { onDelete: 'cascade' })
      .notNull(),
    type: text('type').$type<AdapterAccountType>().notNull(),
    provider: text('provider').notNull(),
    providerAccountId: text('provider_account_id').notNull(),
    refresh_token: text('refresh_token'),
    access_token: text('access_token'),
    expires_at: integer('expires_at'),
    token_type: text('token_type'),
    scope: text('scope'),
    id_token: text('id_token'),
    session_state: text('session_state'),
  },
  (account) => [
    {
      name: 'accounts_pk',
      columns: [account.provider, account.providerAccountId],
      type: 'primaryKey' as const,
    },
  ],
);

export const sessions = pgTable('sessions', {
  sessionToken: text('session_token').primaryKey(),
  userId: text('user_id')
    .references(() => users.id, { onDelete: 'cascade' })
    .notNull(),
  expires: timestamp('expires', { mode: 'date' }).notNull(),
});

export const verificationTokens = pgTable(
  'verification_tokens',
  {
    identifier: text('identifier').notNull(),
    token: text('token').notNull(),
    expires: timestamp('expires', { mode: 'date' }).notNull(),
  },
  (vt) => [
    {
      name: 'verification_tokens_pk',
      columns: [vt.identifier, vt.token],
      type: 'primaryKey' as const,
    },
  ],
);

// 팀 기능 테이블
export const teams = pgTable('teams', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  name: text('name').notNull(),
  createdBy: text('created_by')
    .references(() => users.id)
    .notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
});

export const teamMembers = pgTable(
  'team_members',
  {
    id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
    teamId: integer('team_id')
      .references(() => teams.id, { onDelete: 'cascade' })
      .notNull(),
    userId: text('user_id')
      .references(() => users.id, { onDelete: 'cascade' })
      .notNull(),
    role: text('role', { enum: ['admin', 'member'] })
      .notNull()
      .default('member'),
    joinedAt: timestamp('joined_at').defaultNow().notNull(),
  },
  (table) => [uniqueIndex('team_members_team_user_idx').on(table.teamId, table.userId)],
);

export const invitations = pgTable('invitations', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  teamId: integer('team_id')
    .references(() => teams.id, { onDelete: 'cascade' })
    .notNull(),
  email: text('email').notNull(),
  token: text('token').notNull().unique(),
  role: text('role', { enum: ['admin', 'member'] })
    .notNull()
    .default('member'),
  expiresAt: timestamp('expires_at').notNull(),
  acceptedAt: timestamp('accepted_at'),
  createdAt: timestamp('created_at').defaultNow().notNull(),
});
```

2. packages/core/src/db/schema/index.ts 수정 -- auth.ts export 추가:

```typescript
export * from './auth';
```

3. packages/core/src/index.ts -- auth 스키마가 `export * from './db/schema'`로 이미 포함되는지 확인. 안 되면 추가.

4. collectionJobs에 teamId FK 추가 (nullable):
   packages/core/src/db/schema/collections.ts의 collectionJobs에 `teamId: integer('team_id')` 추가.
   auth.ts의 teams를 import하여 `.references(() => teams.id)` 적용.
   **주의:** 기존 collectionJobs import 순서 문제 가능 -- 순환 참조 방지를 위해 teams 참조는 문자열 기반이거나 별도 migration으로 처리할 수도 있음. 순환이면 참조 없이 integer만 추가하고 migration SQL에서 FK 추가.

5. DB 마이그레이션 실행: `pnpm --filter @ai-signalcraft/core drizzle-kit push` 또는 프로젝트에 맞는 마이그레이션 명령

6. apps/web/src/server/trpc/init.ts:

```typescript
import { initTRPC, TRPCError } from '@trpc/server';
import { auth } from '../auth';
import { db } from '@ai-signalcraft/core';

export const createTRPCContext = async () => {
  const session = await auth();
  return { session, db };
};

const t = initTRPC.context<Awaited<ReturnType<typeof createTRPCContext>>>().create();

export const router = t.router;
export const publicProcedure = t.procedure;
export const protectedProcedure = t.procedure.use(async ({ ctx, next }) => {
  if (!ctx.session?.user) throw new TRPCError({ code: 'UNAUTHORIZED' });
  return next({ ctx: { ...ctx, session: ctx.session } });
});
```

7. apps/web/src/server/trpc/router.ts:

```typescript
import { router } from './init';
export const appRouter = router({});
export type AppRouter = typeof appRouter;
```

8. apps/web/src/app/api/trpc/[trpc]/route.ts:

```typescript
import { fetchRequestHandler } from '@trpc/server/adapters/fetch';
import { appRouter } from '@/server/trpc/router';
import { createTRPCContext } from '@/server/trpc/init';

const handler = (req: Request) =>
  fetchRequestHandler({
    endpoint: '/api/trpc',
    req,
    router: appRouter,
    createContext: createTRPCContext,
  });

export { handler as GET, handler as POST };
```

9. apps/web/src/lib/trpc.ts (클라이언트):

```typescript
import { createTRPCClient, httpBatchLink } from '@trpc/client';
import { createTRPCOptionsProxy } from '@trpc/tanstack-react-query';
import type { AppRouter } from '@/server/trpc/router';

export const trpcClient = createTRPCClient<AppRouter>({
  links: [httpBatchLink({ url: '/api/trpc' })],
});

export const trpc = createTRPCOptionsProxy<AppRouter>(trpcClient);
```

10. apps/web/src/server/auth.ts:

```typescript
import NextAuth from 'next-auth';
import Credentials from 'next-auth/providers/credentials';
import Google from 'next-auth/providers/google';
import { DrizzleAdapter } from '@auth/drizzle-adapter';
import { db, users } from '@ai-signalcraft/core';
import { eq } from 'drizzle-orm';
import bcrypt from 'bcryptjs';

export const { handlers, auth, signIn, signOut } = NextAuth({
  adapter: DrizzleAdapter(db),
  session: { strategy: 'jwt' }, // Credentials 사용 시 JWT 필수
  providers: [
    Credentials({
      credentials: {
        email: { label: 'Email', type: 'email' },
        password: { label: 'Password', type: 'password' },
      },
      async authorize(credentials) {
        if (!credentials?.email || !credentials?.password) return null;
        const user = await db
          .select()
          .from(users)
          .where(eq(users.email, credentials.email as string))
          .limit(1);
        if (!user[0]?.hashedPassword) return null;
        const valid = await bcrypt.compare(credentials.password as string, user[0].hashedPassword);
        if (!valid) return null;
        return { id: user[0].id, email: user[0].email, name: user[0].name, role: user[0].role };
      },
    }),
    Google({
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
    }),
  ],
  callbacks: {
    async jwt({ token, user }) {
      if (user) {
        token.role = (user as any).role;
      }
      return token;
    },
    async session({ session, token }) {
      if (session.user) {
        session.user.id = token.sub!;
        (session.user as any).role = token.role;
      }
      return session;
    },
  },
  pages: { signIn: '/login' },
});
```

11. apps/web/src/app/api/auth/[...nextauth]/route.ts:

```typescript
import { handlers } from '@/server/auth';
export const { GET, POST } = handlers;
```

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/web build 2>&1 | tail -5 && pnpm tsc --noEmit -p packages/core/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/db/schema/auth.ts에 `export const users = pgTable('users'` 가 포함된다
    - packages/core/src/db/schema/auth.ts에 `export const teams = pgTable('teams'` 가 포함된다
    - packages/core/src/db/schema/auth.ts에 `export const teamMembers = pgTable('team_members'` 가 포함된다
    - packages/core/src/db/schema/auth.ts에 `export const invitations = pgTable('invitations'` 가 포함된다
    - apps/web/src/server/trpc/init.ts에 `export const protectedProcedure` 가 포함된다
    - apps/web/src/server/trpc/router.ts에 `export type AppRouter` 가 포함된다
    - apps/web/src/server/auth.ts에 `DrizzleAdapter` 가 포함된다
    - apps/web/src/server/auth.ts에 `strategy: 'jwt'` 가 포함된다
    - apps/web/src/app/api/trpc/[trpc]/route.ts 파일이 존재한다
    - apps/web/src/app/api/auth/[...nextauth]/route.ts 파일이 존재한다
    - `pnpm --filter @ai-signalcraft/web build` exit 0
  </acceptance_criteria>
  <done>tRPC 빈 라우터 + NextAuth(Credentials + Google) + 인증/팀 DB 스키마가 설정되고 빌드 통과</done>
</task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/web build` exit 0
- `pnpm --filter @ai-signalcraft/web test -- --run` exit 0 (최소 1개 테스트 green)
- `pnpm tsc --noEmit -p packages/core/tsconfig.json` exit 0
- apps/web/components.json 존재
- apps/web/src/server/auth.ts 존재
- apps/web/src/server/trpc/router.ts 존재
- packages/core/src/db/schema/auth.ts 존재
</verification>

<success_criteria>

- Next.js 앱이 shadcn/ui + Tailwind 4 + 다크모드 기본으로 빌드된다
- tRPC /api/trpc 엔드포인트가 설정된다
- NextAuth /api/auth 엔드포인트가 설정된다
- users, teams, team_members, invitations DB 스키마가 정의된다
- Vitest 테스트 인프라가 설정되고 최소 1개 smoke test가 통과한다
  </success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-team/03-01-SUMMARY.md`
</output>
