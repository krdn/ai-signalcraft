---
phase: 03-dashboard-team
plan: 03
type: execute
wave: 3
depends_on: ['03-02']
files_modified:
  - apps/web/src/components/dashboard/sentiment-chart.tsx
  - apps/web/src/components/dashboard/trend-chart.tsx
  - apps/web/src/components/dashboard/word-cloud.tsx
  - apps/web/src/components/dashboard/risk-cards.tsx
  - apps/web/src/components/dashboard/opportunity-cards.tsx
  - apps/web/src/components/dashboard/platform-compare.tsx
  - apps/web/src/components/dashboard/dashboard-view.tsx
  - apps/web/src/server/trpc/routers/analysis.ts
  - apps/web/src/app/page.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04
  - DASH-07
  - DASH-05

must_haves:
  truths:
    - '감성 비율 Donut 차트가 긍정/부정/중립 비율로 렌더링된다'
    - '시계열 Line 차트가 일별 트렌드를 표시한다'
    - '워드클라우드가 키워드 빈도에 따라 크기가 다르게 렌더링된다'
    - '리스크 카드가 영향도 프로그레스 바와 함께 긴급도별 정렬/색상으로 표시된다'
    - '기회 카드가 유사한 레이아웃으로 표시된다'
  artifacts:
    - path: 'apps/web/src/components/dashboard/sentiment-chart.tsx'
      provides: '감성 비율 Donut 차트'
      contains: 'PieChart'
    - path: 'apps/web/src/components/dashboard/trend-chart.tsx'
      provides: '시계열 Line 차트'
      contains: 'LineChart'
    - path: 'apps/web/src/components/dashboard/word-cloud.tsx'
      provides: '워드클라우드'
      contains: 'WordCloud'
    - path: 'apps/web/src/components/dashboard/risk-cards.tsx'
      provides: '리스크 카드 리스트'
      contains: 'Progress'
    - path: 'apps/web/src/components/dashboard/dashboard-view.tsx'
      provides: '대시보드 2열 그리드 레이아웃'
      contains: 'grid-cols-2'
  key_links:
    - from: 'apps/web/src/components/dashboard/dashboard-view.tsx'
      to: 'apps/web/src/server/trpc/routers/analysis.ts'
      via: 'trpc.analysis.getResults 쿼리'
      pattern: "analysis\\.getResults"
    - from: 'apps/web/src/components/dashboard/sentiment-chart.tsx'
      to: 'Recharts PieChart'
      via: 'shadcn ChartContainer'
      pattern: 'ChartContainer'
---

<objective>
결과 대시보드 탭의 6개 시각화 컴포넌트(감성 Donut, 시계열 Line, 워드클라우드, 플랫폼 비교, 리스크 카드, 기회 카드)를 구현하고 2열 그리드에 배치한다.

Purpose: AI 분석 결과를 직관적인 차트와 카드로 시각화하여 분석팀이 즉시 인사이트를 파악할 수 있게 한다.
Output: 6개 시각화 컴포넌트 + 2열 대시보드 그리드 레이아웃
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dashboard-team/03-CONTEXT.md
@.planning/phases/03-dashboard-team/03-UI-SPEC.md
@.planning/phases/03-dashboard-team/03-RESEARCH.md
@.planning/phases/03-dashboard-team/03-02-SUMMARY.md

<interfaces>
From packages/core/src/db/schema/analysis.ts:
```typescript
export const analysisResults = pgTable('analysis_results', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  jobId: integer('job_id').references(() => collectionJobs.id).notNull(),
  module: text('module').notNull(),  // 'macro-view', 'segmentation', 'sentiment-framing', 'message-impact', 'risk-map', 'opportunity', 'strategy', 'final-summary'
  status: text('status', { enum: ['pending', 'running', 'completed', 'failed'] }),
  result: jsonb('result'),  // 모듈별 Zod 스키마 결과
  errorMessage: text('error_message'),
});
```

analysis.getResults tRPC 쿼리 (Plan 02에서 생성):

- input: { jobId: number }
- output: analysisResults[] (모든 모듈 결과 배열)

각 모듈의 result JSONB 구조 (Phase 2에서 정의된 Zod 스키마):

- macro-view: { overallSentiment: { positive, negative, neutral }, keyTopics, ... }
- segmentation: { platformAnalysis: [...], sentimentByPlatform: [...] }
- sentiment-framing: { sentimentRatio: { positive, negative, neutral }, frames: [...], dailyMentionTrend: [...] }
- message-impact: { topMessages: [...] }
- risk-map: { risks: [{ title, description, impact, urgency, spreadPotential }] }
- opportunity: { opportunities: [{ title, description, impact, feasibility }] }
- strategy: { recommendations: [...] }
- final-summary: { oneLiner, overallAssessment, ... }

Chart CSS 변수 (Plan 01에서 설정):

- --chart-1: 142 71% 45% (green - positive)
- --chart-2: 0 84% 60% (red - negative)
- --chart-3: 240 5% 64% (gray - neutral)
- --chart-4: 217 91% 60% (blue - trend)
- --chart-5: 38 92% 50% (amber - warning)

Risk/Opportunity 색상 코딩:

- Critical/High: hsl(0 84% 60%) red
- Medium: hsl(38 92% 50%) amber
- Low: hsl(142 71% 45%) green
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: 감성/트렌드 차트 + 워드클라우드 + 플랫폼 비교 컴포넌트</name>
  <files>
    apps/web/src/components/dashboard/sentiment-chart.tsx,
    apps/web/src/components/dashboard/trend-chart.tsx,
    apps/web/src/components/dashboard/word-cloud.tsx,
    apps/web/src/components/dashboard/platform-compare.tsx
  </files>
  <read_first>
    apps/web/src/components/ui/chart.tsx,
    packages/core/src/analysis/modules/sentiment-framing.ts,
    packages/core/src/analysis/modules/macro-view.ts,
    packages/core/src/analysis/modules/segmentation.ts,
    .planning/phases/03-dashboard-team/03-UI-SPEC.md
  </read_first>
  <action>
1. shadcn chart 컴포넌트 설치 (아직 없다면):
```bash
cd apps/web && npx shadcn@latest add chart
```

2. apps/web/src/components/dashboard/sentiment-chart.tsx -- 'use client' (per D-06):
   - Props: `data: { positive: number; negative: number; neutral: number }` (감성 비율 퍼센트)
   - shadcn ChartContainer + Recharts PieChart
   - Donut 차트 (innerRadius={60}) -- 가운데 비워서 도넛 형태
   - 3색 Cell: chart-1(green=긍정), chart-2(red=부정), chart-3(gray=중립)
   - ChartTooltip + ChartTooltipContent로 호버 시 비율 표시
   - ChartLegend로 범례 하단 표시
   - Card 래핑: 제목 "감성 비율", Heading 18px semibold
   - 빈 상태: "데이터 없음" placeholder (per UI-SPEC States Matrix)
   - Card min-height: 280px (per UI-SPEC)

3. apps/web/src/components/dashboard/trend-chart.tsx -- 'use client' (per D-06):
   - Props: `data: Array<{ date: string; mentions: number; positive: number; negative: number; neutral: number }>`
   - shadcn ChartContainer + Recharts LineChart
   - X축: date (일별), Y축: 건수
   - 4개 Line: 전체 언급(chart-4 blue), 긍정(chart-1 green), 부정(chart-2 red), 중립(chart-3 gray)
   - ChartTooltip으로 날짜별 수치 표시
   - CartesianGrid, XAxis, YAxis (Label 12px regular, Geist Mono for tick values)
   - Card 래핑: 제목 "시계열 트렌드"
   - 빈 상태: "데이터 없음" placeholder
   - `prefers-reduced-motion` 시 애니메이션 비활성화 (per UI-SPEC Accessibility)

4. apps/web/src/components/dashboard/word-cloud.tsx -- 'use client' + dynamic import (per Research Pitfall 5):
   - Props: `words: Array<{ text: string; value: number }>`
   - `dynamic(() => import('@isoterik/react-word-cloud'), { ssr: false })` 로 SSR 비활성화
   - d3-cloud 기반 spiral 배치
   - 색상: accent(blue), muted 변형 (큰 단어일수록 진한 색)
   - Card 래핑: 제목 "키워드 / 연관어" (per D-07)
   - 빈 상태: "키워드 없음" placeholder

5. apps/web/src/components/dashboard/platform-compare.tsx -- 'use client':
   - Props: `data: Array<{ platform: string; positive: number; negative: number; neutral: number }>`
   - Recharts BarChart (Stacked bar -- 소스별 감성 비교)
   - 플랫폼별 bar: 네이버, 유튜브 등
   - 3색 stack: chart-1, chart-2, chart-3
   - Card 래핑: 제목 "소스별 감성 비교"
   - 빈 상태: "데이터 없음"
     </action>
     <verify>
     <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/web build 2>&1 | tail -5</automated>
     </verify>
     <acceptance_criteria>
   - apps/web/src/components/dashboard/sentiment-chart.tsx에 `PieChart` 와 `ChartContainer` 가 포함된다
   - apps/web/src/components/dashboard/sentiment-chart.tsx에 `innerRadius` 가 포함된다 (도넛 형태)
   - apps/web/src/components/dashboard/trend-chart.tsx에 `LineChart` 와 `ChartContainer` 가 포함된다
   - apps/web/src/components/dashboard/word-cloud.tsx에 `ssr: false` 가 포함된다
   - apps/web/src/components/dashboard/word-cloud.tsx에 `dynamic` 가 포함된다
   - apps/web/src/components/dashboard/platform-compare.tsx에 `BarChart` 가 포함된다
   - `pnpm --filter @ai-signalcraft/web build` exit 0
     </acceptance_criteria>
     <done>4개 시각화 컴포넌트(감성 Donut, 시계열 Line, 워드클라우드, 플랫폼 비교 Bar)가 빌드 통과한다</done>
     </task>

<task type="auto">
  <name>Task 2: 리스크/기회 카드 + 대시보드 그리드 조합 + 탭 연결</name>
  <files>
    apps/web/src/components/dashboard/risk-cards.tsx,
    apps/web/src/components/dashboard/opportunity-cards.tsx,
    apps/web/src/components/dashboard/dashboard-view.tsx,
    apps/web/src/server/trpc/routers/analysis.ts,
    apps/web/src/app/page.tsx
  </files>
  <read_first>
    packages/core/src/analysis/modules/risk-map.ts,
    packages/core/src/analysis/modules/opportunity.ts,
    apps/web/src/server/trpc/routers/analysis.ts,
    apps/web/src/components/dashboard/sentiment-chart.tsx,
    apps/web/src/app/page.tsx,
    .planning/phases/03-dashboard-team/03-UI-SPEC.md
  </read_first>
  <action>
1. apps/web/src/components/dashboard/risk-cards.tsx -- 'use client' (per D-08):
   - Props: `risks: Array<{ title: string; description: string; impact: number; urgency: string; spreadPotential: string }>`
   - 긴급도별 정렬 (critical > high > medium > low)
   - 각 리스크: shadcn Card
     - 제목: risk.title (Heading 18px semibold)
     - 설명: risk.description (Body 14px)
     - 영향도: shadcn Progress 컴포넌트 (0-100%)
       - progress bar 색상: impact > 70이면 red(destructive), 40-70이면 amber(chart-5), < 40이면 green(chart-1)
     - urgency Badge: Critical=red, High=amber, Medium=blue, Low=green
   - Card 래핑: 제목 "리스크 분석" (per D-08)
   - 빈 상태: "리스크 없음" placeholder

2. apps/web/src/components/dashboard/opportunity-cards.tsx -- 'use client' (per D-08):
   - 리스크 카드와 동일 레이아웃 패턴
   - Props: `opportunities: Array<{ title: string; description: string; impact: number; feasibility: string }>`
   - feasibility별 정렬/색상
   - 영향도: Progress bar (동일 색상 코딩)
   - Card 래핑: 제목 "기회 분석"
   - 빈 상태: "기회 없음" placeholder

3. apps/web/src/components/dashboard/dashboard-view.tsx -- 'use client':
   - Props: `jobId: number` (선택된 분석 작업 ID)
   - trpc.analysis.getResults 쿼리로 해당 jobId의 모든 모듈 결과 조회
   - 결과를 모듈별로 파싱:
     - 'sentiment-framing' -> sentimentRatio, dailyMentionTrend
     - 'macro-view' -> keyTopics (워드클라우드)
     - 'segmentation' -> platformAnalysis (플랫폼 비교)
     - 'risk-map' -> risks
     - 'opportunity' -> opportunities
   - 2열 그리드 레이아웃 (per D-05, UI-SPEC):
     ```
     grid grid-cols-1 lg:grid-cols-2 gap-8
     [Sentiment Donut] [Trend Line]
     [Word Cloud]      [Platform Compare]
     [Risk Cards]      [Opportunity Cards]
     ```
   - 로딩: 각 카드 Skeleton (per UI-SPEC States Matrix)
   - 에러: Error Card + "다시 시도"
   - 빈 상태: "분석 결과가 없습니다" + "분석 실행 탭에서 새 분석을 시작하거나, 히스토리에서 이전 결과를 선택하세요." (per UI-SPEC)

4. analysis.ts tRPC 라우터 확장 -- getResults가 모듈별 결과를 반환하되, result JSONB를 타입 안전하게 파싱하는 유틸 추가 (필요 시).

5. page.tsx 업데이트:
   - "결과 대시보드" 탭에 DashboardView 컴포넌트 연결
   - jobId 상태 관리: 분석 실행 후 또는 히스토리에서 선택 시 activeJobId 설정
   - 탭 간 jobId 공유 (state lifting 또는 URL query param)
     </action>
     <verify>
     <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/web build 2>&1 | tail -5</automated>
     </verify>
     <acceptance_criteria>
   - apps/web/src/components/dashboard/risk-cards.tsx에 `Progress` import가 포함된다
   - apps/web/src/components/dashboard/risk-cards.tsx에 urgency별 색상 분기가 포함된다 (최소 "red", "amber" 또는 "destructive" 문자열)
   - apps/web/src/components/dashboard/opportunity-cards.tsx에 `Progress` 가 포함된다
   - apps/web/src/components/dashboard/dashboard-view.tsx에 `grid-cols-2` 가 포함된다
   - apps/web/src/components/dashboard/dashboard-view.tsx에 `SentimentChart` `TrendChart` `WordCloud` `RiskCards` `OpportunityCards` import가 포함된다
   - apps/web/src/components/dashboard/dashboard-view.tsx에 `"분석 결과가 없습니다"` 빈 상태 문자열이 포함된다
   - apps/web/src/app/page.tsx에 `DashboardView` import가 포함된다
   - `pnpm --filter @ai-signalcraft/web build` exit 0
     </acceptance_criteria>
     <done>6개 시각화 컴포넌트가 2열 그리드에 배치되고 jobId 기반으로 결과를 조회/표시한다</done>
     </task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/web build` exit 0
- 결과 대시보드 탭에 6개 카드가 2x3 그리드로 배치됨
- 각 차트가 분석 결과 데이터를 올바른 형태로 소비함
</verification>

<success_criteria>

- 감성 Donut 차트가 3색(긍정/부정/중립)으로 렌더링됨
- 시계열 Line 차트가 일별 트렌드를 4개 선으로 표시함
- 워드클라우드가 SSR 없이 클라이언트에서 렌더링됨
- 리스크/기회 카드가 영향도 Progress bar + 색상 코딩으로 표시됨
- 2열 반응형 그리드 (lg 2열, mobile 1열)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-team/03-03-SUMMARY.md`
</output>
