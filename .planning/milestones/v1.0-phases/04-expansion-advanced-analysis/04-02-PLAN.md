---
phase: 04-expansion-advanced-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/analysis/schemas/approval-rating.schema.ts
  - packages/core/src/analysis/schemas/frame-war.schema.ts
  - packages/core/src/analysis/schemas/crisis-scenario.schema.ts
  - packages/core/src/analysis/schemas/win-simulation.schema.ts
  - packages/core/src/analysis/schemas/index.ts
  - packages/core/src/analysis/modules/approval-rating.ts
  - packages/core/src/analysis/modules/frame-war.ts
  - packages/core/src/analysis/modules/crisis-scenario.ts
  - packages/core/src/analysis/modules/win-simulation.ts
  - packages/core/src/analysis/modules/index.ts
  - packages/core/src/analysis/types.ts
  - packages/core/src/analysis/runner.ts
  - packages/core/src/report/generator.ts
  - packages/core/tests/advn-schema.test.ts
autonomous: true
requirements:
  - ADVN-01
  - ADVN-02
  - ADVN-03
  - ADVN-04

must_haves:
  truths:
    - "AI 지지율 추정치가 감정 비율과 플랫폼 편향을 보정하여 범위(range)로 산출되고 면책 문구가 포함된다"
    - "프레임 전쟁 분석이 지배적/위협/반전 가능 프레임을 식별하여 구조화된 결과를 생성한다"
    - "위기 대응 시나리오 3개(확산/통제/역전)가 발생 조건과 대응 전략을 포함하여 생성된다"
    - "승리 확률 시뮬레이션이 승리/패배 조건과 핵심 전략을 도출한다"
    - "4개 고급 분석 모듈이 Stage 4로 기존 파이프라인 뒤에 실행되며 실패해도 기존 리포트에 영향 없다"
    - "통합 리포트에 고급 분석 섹션이 결과가 있을 때만 자연 추가된다"
  artifacts:
    - path: "packages/core/src/analysis/schemas/approval-rating.schema.ts"
      provides: "AI 지지율 추정 Zod 스키마"
      exports: ["ApprovalRatingSchema", "ApprovalRatingResult"]
    - path: "packages/core/src/analysis/modules/approval-rating.ts"
      provides: "AI 지지율 추정 모듈"
      exports: ["approvalRatingModule"]
    - path: "packages/core/src/analysis/schemas/frame-war.schema.ts"
      provides: "프레임 전쟁 Zod 스키마"
      exports: ["FrameWarSchema", "FrameWarResult"]
    - path: "packages/core/src/analysis/modules/frame-war.ts"
      provides: "프레임 전쟁 모듈"
      exports: ["frameWarModule"]
    - path: "packages/core/src/analysis/schemas/crisis-scenario.schema.ts"
      provides: "위기 시나리오 Zod 스키마"
      exports: ["CrisisScenarioSchema", "CrisisScenarioResult"]
    - path: "packages/core/src/analysis/modules/crisis-scenario.ts"
      provides: "위기 시나리오 모듈"
      exports: ["crisisScenarioModule"]
    - path: "packages/core/src/analysis/schemas/win-simulation.schema.ts"
      provides: "승리 시뮬레이션 Zod 스키마"
      exports: ["WinSimulationSchema", "WinSimulationResult"]
    - path: "packages/core/src/analysis/modules/win-simulation.ts"
      provides: "승리 시뮬레이션 모듈"
      exports: ["winSimulationModule"]
  key_links:
    - from: "packages/core/src/analysis/runner.ts"
      to: "packages/core/src/analysis/modules/approval-rating.ts"
      via: "STAGE4_MODULES import"
      pattern: "STAGE4_MODULES"
    - from: "packages/core/src/analysis/runner.ts"
      to: "packages/core/src/analysis/runner.ts runModule"
      via: "Stage 4 실행 로직"
      pattern: "Stage 4"
    - from: "packages/core/src/report/generator.ts"
      to: "ADVN 모듈 결과"
      via: "optional 고급 분석 섹션 렌더링"
      pattern: "approval-rating|frame-war|crisis-scenario|win-simulation"
---

<objective>
4개 고급 분석 모듈(AI 지지율 추정, 프레임 전쟁, 위기 시나리오, 승리 시뮬레이션)을 구현하고 분석 파이프라인 Stage 4로 통합한다.

Purpose: 기존 8개 분석 모듈(3 Stage)에 4개 고급 분석을 Stage 4로 추가하여 전략적 인사이트를 확장한다. 기존 리포트와 호환을 유지하면서 고급 분석 결과를 선택적으로 통합한다.
Output: 4개 Zod 스키마 + 4개 분석 모듈 + runner Stage 4 확장 + 리포트 생성기 고급 분석 섹션
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-expansion-advanced-analysis/04-CONTEXT.md
@.planning/phases/04-expansion-advanced-analysis/04-RESEARCH.md

<interfaces>
<!-- AnalysisModule 인터페이스 (packages/core/src/analysis/types.ts) -->
```typescript
export interface AnalysisModule<T = unknown> {
  readonly name: string;
  readonly displayName: string;
  readonly provider: AIProvider;
  readonly model: string;
  readonly schema: z.ZodType<T>;
  buildPrompt(data: AnalysisInput): string;
  buildSystemPrompt(): string;
  buildPromptWithContext?(data: AnalysisInput, priorResults: Record<string, unknown>): string;
}

export const MODULE_MODEL_MAP: Record<string, { provider: AIProvider; model: string }>;
export const MODULE_NAMES = { ... } as const;
```

<!-- 분석 러너 Stage 구조 (packages/core/src/analysis/runner.ts) -->
```typescript
export const STAGE1_MODULES: AnalysisModule[];  // 병렬: macro-view, segmentation, sentiment-framing, message-impact
export const STAGE2_MODULES: AnalysisModule[];  // 순차: risk-map, opportunity, strategy
// Stage 3: finalSummaryModule (모든 선행 참조)
export async function runModule<T>(module, input, priorResults?): Promise<AnalysisModuleResult<T>>;
export async function runAnalysisPipeline(jobId: number): Promise<{...}>;
```

<!-- 리스크 맵 스키마 패턴 (참고 구현) -->
```typescript
export const RiskMapSchema = z.object({
  topRisks: z.array(z.object({ rank, title, description, impactLevel, spreadProbability, currentStatus, triggerConditions })).max(5),
  overallRiskLevel: z.enum(['critical', 'high', 'medium', 'low']),
  riskTrend: z.enum(['increasing', 'stable', 'decreasing']),
});
```

<!-- 리포트 생성기 (packages/core/src/report/generator.ts) -->
```typescript
export interface ReportGenerationInput {
  jobId: number; keyword: string;
  dateRange: { start: Date; end: Date };
  results: Record<string, AnalysisModuleResult>;
  completedModules: string[]; failedModules: string[];
}
export async function generateIntegratedReport(input): Promise<{ markdownContent, oneLiner, totalTokens }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4개 고급 분석 Zod 스키마 + 모듈 구현</name>
  <files>
    packages/core/src/analysis/schemas/approval-rating.schema.ts,
    packages/core/src/analysis/schemas/frame-war.schema.ts,
    packages/core/src/analysis/schemas/crisis-scenario.schema.ts,
    packages/core/src/analysis/schemas/win-simulation.schema.ts,
    packages/core/src/analysis/schemas/index.ts,
    packages/core/src/analysis/modules/approval-rating.ts,
    packages/core/src/analysis/modules/frame-war.ts,
    packages/core/src/analysis/modules/crisis-scenario.ts,
    packages/core/src/analysis/modules/win-simulation.ts,
    packages/core/src/analysis/modules/index.ts,
    packages/core/src/analysis/types.ts,
    packages/core/tests/advn-schema.test.ts
  </files>
  <read_first>
    packages/core/src/analysis/types.ts,
    packages/core/src/analysis/schemas/risk-map.schema.ts,
    packages/core/src/analysis/modules/risk-map.ts,
    packages/core/src/analysis/modules/strategy.ts,
    packages/core/src/analysis/modules/prompt-utils.ts,
    packages/core/src/analysis/schemas/index.ts,
    packages/core/src/analysis/modules/index.ts
  </read_first>
  <behavior>
    - Test: ApprovalRatingSchema.parse({valid data}) 성공, estimatedRange.min/max 0~100 범위
    - Test: ApprovalRatingSchema에 disclaimer 필드가 string 타입으로 존재
    - Test: ApprovalRatingSchema에 confidence 필드가 'high'|'medium'|'low' enum
    - Test: FrameWarSchema.parse({valid data}) 성공, dominantFrames/threateningFrames/reversibleFrames 배열 포함
    - Test: CrisisScenarioSchema.parse({valid data}) 성공, scenarios 배열이 정확히 3개 (spread/control/reverse)
    - Test: WinSimulationSchema.parse({valid data}) 성공, winProbability 0~100, winConditions/loseConditions 배열
    - Test: 잘못된 입력에 대해 각 스키마가 ZodError를 throw
  </behavior>
  <action>
    **1. ADVN-01 AI 지지율 추정 스키마 (approval-rating.schema.ts):**

    ```typescript
    export const ApprovalRatingSchema = z.object({
      estimatedRange: z.object({
        min: z.number().min(0).max(100),
        max: z.number().min(0).max(100),
      }).describe('AI 추정 지지율 범위 (%)'),
      confidence: z.enum(['high', 'medium', 'low']),
      methodology: z.object({
        sentimentRatio: z.object({ positive: z.number(), neutral: z.number(), negative: z.number() }),
        platformBiasCorrection: z.array(z.object({
          platform: z.string(),
          biasDirection: z.enum(['left', 'right', 'neutral']),
          correctionFactor: z.number(),
        })),
        spreadFactor: z.number().describe('확산력 가중치'),
      }),
      disclaimer: z.string().describe('면책 문구 -- 반드시 포함'),
      reasoning: z.string(),
    });
    ```

    **2. ADVN-02 프레임 전쟁 스키마 (frame-war.schema.ts):**

    ```typescript
    export const FrameWarSchema = z.object({
      dominantFrames: z.array(z.object({
        name: z.string(), description: z.string(),
        strength: z.number().min(0).max(100),
        supportingEvidence: z.array(z.string()),
      })).max(5).describe('지배적 프레임 TOP 5'),
      threateningFrames: z.array(z.object({
        name: z.string(), description: z.string(),
        threatLevel: z.enum(['critical', 'high', 'medium', 'low']),
        counterStrategy: z.string(),
      })).max(5),
      reversibleFrames: z.array(z.object({
        name: z.string(), currentPerception: z.string(),
        potentialShift: z.string(), requiredAction: z.string(),
      })).max(3).describe('반전 가능 프레임'),
      battlefieldSummary: z.string(),
    });
    ```

    **3. ADVN-03 위기 시나리오 스키마 (crisis-scenario.schema.ts):**

    3개 시나리오 고정 (per D-06):
    ```typescript
    export const CrisisScenarioSchema = z.object({
      scenarios: z.tuple([
        z.object({ type: z.literal('spread'), ... }),     // 확산 (worst)
        z.object({ type: z.literal('control'), ... }),    // 통제 (moderate)
        z.object({ type: z.literal('reverse'), ... }),    // 역전 (best)
      ]),
      currentRiskLevel: z.enum(['critical', 'high', 'medium', 'low']),
      recommendedAction: z.string(),
    });
    ```
    각 시나리오 객체: `{ type, name, probability (0~100), triggerConditions: string[], expectedOutcome: string, responseStrategy: string[], timeframe: string }`

    **4. ADVN-04 승리 시뮬레이션 스키마 (win-simulation.schema.ts):**

    ```typescript
    export const WinSimulationSchema = z.object({
      winProbability: z.number().min(0).max(100),
      confidenceLevel: z.enum(['high', 'medium', 'low']),
      winConditions: z.array(z.object({
        condition: z.string(), currentStatus: z.enum(['met', 'partial', 'unmet']),
        importance: z.enum(['critical', 'high', 'medium']),
      })).min(3).max(7),
      loseConditions: z.array(z.object({
        condition: z.string(), currentRisk: z.enum(['high', 'medium', 'low']),
        mitigation: z.string(),
      })).min(2).max(5),
      keyStrategies: z.array(z.object({
        strategy: z.string(), expectedImpact: z.string(), priority: z.number(),
      })).min(3).max(5),
      simulationSummary: z.string(),
    });
    ```

    **5. 4개 분석 모듈 구현 (risk-map.ts 패턴 계승):**

    각 모듈: AnalysisModule<SchemaType> 구현. buildSystemPrompt()에 역할 정의, buildPrompt()에 데이터 요약, buildPromptWithContext()에 모든 선행 결과(Stage 1~3 + ADVN 선행) 참조.

    - approval-rating.ts: 감정 비율 + 플랫폼별 데이터 분포를 프롬프트에 포함. system prompt에 "면책 문구를 반드시 포함하세요. 추정치는 범위로 표현하세요. 이것은 과학적 여론조사를 대체하지 않는 AI 참고치입니다" 명시. (per D-05)
    - frame-war.ts: sentiment-framing 모듈 결과를 참조하여 프레임 간 충돌/지배 구조 분석.
    - crisis-scenario.ts: risk-map + ADVN-01 결과 참조. 3개 시나리오 고정 (per D-06).
    - win-simulation.ts: 모든 선행 결과(Stage 1~3 + ADVN-01~03) 참조. 최종 단계 모듈.

    **6. types.ts에 MODULE_MODEL_MAP 확장:**

    ```typescript
    'approval-rating':   { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
    'frame-war':         { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
    'crisis-scenario':   { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
    'win-simulation':    { provider: 'anthropic', model: 'claude-sonnet-4-20250514' },
    ```

    MODULE_NAMES에 추가:
    ```typescript
    APPROVAL_RATING: 'approval-rating',
    FRAME_WAR: 'frame-war',
    CRISIS_SCENARIO: 'crisis-scenario',
    WIN_SIMULATION: 'win-simulation',
    ```

    **7. schemas/index.ts, modules/index.ts에 export 추가.**
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core vitest run tests/advn-schema.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/analysis/schemas/approval-rating.schema.ts 파일이 `export const ApprovalRatingSchema` 와 `disclaimer: z.string()` 를 포함한다
    - packages/core/src/analysis/schemas/frame-war.schema.ts 파일이 `export const FrameWarSchema` 와 `dominantFrames`, `threateningFrames`, `reversibleFrames` 를 포함한다
    - packages/core/src/analysis/schemas/crisis-scenario.schema.ts 파일이 `export const CrisisScenarioSchema` 와 `z.literal('spread')`, `z.literal('control')`, `z.literal('reverse')` 를 포함한다
    - packages/core/src/analysis/schemas/win-simulation.schema.ts 파일이 `export const WinSimulationSchema` 와 `winProbability`, `winConditions`, `loseConditions` 를 포함한다
    - packages/core/src/analysis/types.ts 파일이 `'approval-rating'`, `'frame-war'`, `'crisis-scenario'`, `'win-simulation'` 을 MODULE_MODEL_MAP에 포함한다
    - packages/core/src/analysis/modules/index.ts 파일이 `approvalRatingModule`, `frameWarModule`, `crisisScenarioModule`, `winSimulationModule` export를 포함한다
    - advn-schema.test.ts가 exit 0으로 통과한다
  </acceptance_criteria>
  <done>4개 고급 분석 스키마와 모듈이 구현되고 Zod 스키마 검증 테스트가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: runner Stage 4 확장 + 리포트 생성기 고급 분석 통합</name>
  <files>
    packages/core/src/analysis/runner.ts,
    packages/core/src/report/generator.ts
  </files>
  <read_first>
    packages/core/src/analysis/runner.ts,
    packages/core/src/report/generator.ts,
    packages/core/src/analysis/modules/index.ts,
    packages/core/src/analysis/types.ts
  </read_first>
  <action>
    **1. runner.ts에 Stage 4 추가:**

    ```typescript
    import {
      approvalRatingModule, frameWarModule,
      crisisScenarioModule, winSimulationModule,
    } from './modules';

    // Stage 4: 고급 분석 (ADVN 모듈)
    // ADVN-01(approval-rating), ADVN-02(frame-war): 병렬 (독립)
    // ADVN-03(crisis-scenario): ADVN-01 + risk-map 의존
    // ADVN-04(win-simulation): ADVN-01~03 전체 의존
    export const STAGE4_PARALLEL: AnalysisModule[] = [approvalRatingModule, frameWarModule];
    export const STAGE4_SEQUENTIAL: AnalysisModule[] = [crisisScenarioModule, winSimulationModule];
    ```

    `runAnalysisPipeline()` 함수에 Stage 3(final-summary) 뒤에 Stage 4 로직 추가:

    ```typescript
    // Stage 4a: ADVN-01, ADVN-02 병렬 실행
    const stage4aSettled = await Promise.allSettled(
      STAGE4_PARALLEL.map((m) => runModule(m, input, priorResults)),
    );
    for (const settled of stage4aSettled) {
      if (settled.status === 'fulfilled') {
        allResults[settled.value.module] = settled.value;
        if (settled.value.status === 'completed') {
          priorResults[settled.value.module] = settled.value.result;
        }
      }
    }

    // Stage 4b: ADVN-03, ADVN-04 순차 실행
    for (const module of STAGE4_SEQUENTIAL) {
      const result = await runModule(module, input, priorResults);
      allResults[result.module] = result;
      if (result.status === 'completed') {
        priorResults[result.module] = result.result;
      }
    }
    ```

    Stage 4 전체가 실패해도 기존 리포트는 이미 Stage 3에서 생성되었으므로 영향 없음. Stage 4 완료 후 리포트를 재생성(고급 분석 섹션 추가)할지 여부를 advnCompleted 플래그로 결정:

    ```typescript
    const advnCompleted = STAGE4_PARALLEL.concat(STAGE4_SEQUENTIAL)
      .some(m => allResults[m.name]?.status === 'completed');

    if (advnCompleted) {
      // 고급 분석 포함 리포트 재생성
      report = await generateIntegratedReport({ ...input, results: allResults, completedModules, failedModules });
    }
    ```

    **2. generator.ts 수정 -- 고급 분석 섹션 선택적 포함:**

    리포트 생성 프롬프트에 고급 분석 결과가 있을 때만 추가 섹션을 삽입:

    ```typescript
    const advnModules = ['approval-rating', 'frame-war', 'crisis-scenario', 'win-simulation'];
    const advnResults = Object.entries(input.results)
      .filter(([k, r]) => advnModules.includes(k) && r.status === 'completed');

    let advnSection = '';
    if (advnResults.length > 0) {
      advnSection = `

    ## 고급 분석 결과
    다음 고급 분석 모듈 결과도 리포트에 자연스럽게 통합하세요:
    ${advnResults.map(([k, r]) => `### ${k}\n${JSON.stringify(r.result, null, 2)}`).join('\n\n')}

    고급 분석 섹션에서는:
    - AI 지지율 추정 결과에 면책 문구를 반드시 포함
    - 프레임 전쟁 분석의 시각적 구조(지배적 vs 위협 vs 반전 가능)
    - 위기 시나리오 3개를 표 형태로 정리
    - 승리 확률과 핵심 전략을 명확히 구분`;
    }
    ```

    이 advnSection을 기존 프롬프트 끝에 추가. advnResults가 비어있으면 아무것도 추가하지 않아 기존 리포트와 동일.
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/analysis/runner.ts 파일이 `STAGE4_PARALLEL` 와 `STAGE4_SEQUENTIAL` export를 포함한다
    - packages/core/src/analysis/runner.ts 파일이 `Stage 4` 주석과 `approvalRatingModule`, `winSimulationModule` import를 포함한다
    - packages/core/src/report/generator.ts 파일이 `approval-rating` 문자열과 `고급 분석` 텍스트를 포함한다
    - packages/core/src/report/generator.ts에서 advnResults.length가 0일 때 기존 로직이 변경 없이 유지된다
    - TypeScript 컴파일(tsc --noEmit)이 에러 없이 통과한다
  </acceptance_criteria>
  <done>분석 파이프라인이 Stage 4(ADVN-01~04)를 실행하고, 완료 시 통합 리포트에 고급 분석 섹션이 자동 추가된다. ADVN 모듈 실패 시에도 기존 리포트는 정상 유지된다.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/core vitest run -x` -- 분석 모듈 테스트 전체 통과
- `pnpm --filter @ai-signalcraft/core tsc --noEmit` -- 타입 에러 없음
- runner.ts의 runAnalysisPipeline이 Stage 1 -> 2 -> 3 -> 4a(병렬) -> 4b(순차) 순서로 실행
- ADVN 모듈 전체 실패 시에도 기존 8개 모듈 기반 리포트가 정상 생성됨
</verification>

<success_criteria>
- 4개 고급 분석 Zod 스키마가 정의되고 유효성 검증 테스트가 통과한다
- 4개 고급 분석 모듈이 AnalysisModule 인터페이스를 구현한다
- runner.ts에 Stage 4가 추가되어 ADVN-01~04가 올바른 의존 관계로 실행된다
- 통합 리포트에 고급 분석 결과가 선택적으로 포함된다
- ADVN 모듈 실패가 기존 리포트에 영향을 주지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/04-expansion-advanced-analysis/04-02-SUMMARY.md`
</output>
