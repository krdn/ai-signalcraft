---
phase: 04-expansion-advanced-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/collectors/src/types/community.ts
  - packages/collectors/src/utils/community-parser.ts
  - packages/collectors/src/adapters/dcinside.ts
  - packages/collectors/src/adapters/fmkorea.ts
  - packages/collectors/src/adapters/clien.ts
  - packages/collectors/src/adapters/index.ts
  - packages/collectors/src/adapters/registry.ts
  - packages/core/src/pipeline/normalize.ts
  - packages/core/src/pipeline/worker-handlers.ts
  - packages/collectors/tests/dcinside.test.ts
  - packages/collectors/tests/fmkorea.test.ts
  - packages/collectors/tests/clien.test.ts
autonomous: true
requirements:
  - COLL-05
  - COLL-06
  - COLL-07
  - COLL-08

must_haves:
  truths:
    - "DC갤러리에서 키워드 검색 후 게시글과 댓글이 수집되어 articles/comments 테이블에 정규화 저장된다"
    - "에펨코리아에서 키워드 검색 후 게시글과 댓글이 수집되어 articles/comments 테이블에 정규화 저장된다"
    - "클리앙에서 키워드 검색 후 게시글과 댓글이 수집되어 articles/comments 테이블에 정규화 저장된다"
    - "개별 수집기 실패가 다른 수집기 및 전체 파이프라인에 영향을 주지 않는다"
    - "COLL-05(X 수집기)는 v2로 이월되어 구현하지 않는다"
  artifacts:
    - path: "packages/collectors/src/adapters/dcinside.ts"
      provides: "DC갤러리 수집기"
      exports: ["DCInsideCollector"]
    - path: "packages/collectors/src/adapters/fmkorea.ts"
      provides: "에펨코리아 수집기"
      exports: ["FMKoreaCollector"]
    - path: "packages/collectors/src/adapters/clien.ts"
      provides: "클리앙 수집기"
      exports: ["ClienCollector"]
    - path: "packages/collectors/src/types/community.ts"
      provides: "커뮤니티 공통 타입"
      exports: ["CommunityPost", "CommunityComment"]
    - path: "packages/collectors/src/utils/community-parser.ts"
      provides: "커뮤니티 공통 유틸"
      exports: ["parseDateText", "randomDelay"]
  key_links:
    - from: "packages/collectors/src/adapters/dcinside.ts"
      to: "packages/collectors/src/adapters/base.ts"
      via: "implements Collector<CommunityPost>"
      pattern: "implements Collector"
    - from: "packages/collectors/src/adapters/index.ts"
      to: "packages/collectors/src/adapters/dcinside.ts"
      via: "export { DCInsideCollector }"
      pattern: "export.*DCInsideCollector"
    - from: "packages/core/src/pipeline/normalize.ts"
      to: "packages/collectors/src/types/community.ts"
      via: "normalizeCommunityPost/Comment"
      pattern: "normalizeCommunity"
---

<objective>
커뮤니티(DC갤러리, 에펨코리아, 클리앙) 수집기 3종을 구현하고 파이프라인에 통합한다.

Purpose: 기존 네이버/유튜브 2개 소스에 커뮤니티 3개 소스를 추가하여 여론 분석 범위를 확장한다. COLL-05(X 수집기)는 D-01 결정에 따라 v2로 이월.
Output: 3개 커뮤니티 수집기 + 공통 타입/유틸 + 정규화 함수 + 파이프라인 핸들러
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-expansion-advanced-analysis/04-CONTEXT.md
@.planning/phases/04-expansion-advanced-analysis/04-RESEARCH.md

<interfaces>
<!-- 수집기 베이스 인터페이스 (packages/collectors/src/adapters/base.ts) -->
```typescript
export interface CollectionResult<T> {
  source: string;
  items: T[];
  totalCollected: number;
  errors: Error[];
  metadata: { startedAt: Date; completedAt: Date; pagesFetched: number; };
}

export interface Collector<T = unknown> {
  readonly source: string;
  collect(options: CollectionOptions): AsyncGenerator<T[], void, unknown>;
}

export type CollectionOptions = {
  keyword: string;
  startDate: string; // datetime
  endDate: string;   // datetime
  maxItems?: number;
  maxComments?: number;
};
```

<!-- 어댑터 레지스트리 (packages/collectors/src/adapters/registry.ts) -->
```typescript
export function registerCollector(collector: Collector): void;
export function getCollector(source: string): Collector | undefined;
export function getAllCollectors(): Collector[];
```

<!-- 기존 어댑터 export (packages/collectors/src/adapters/index.ts) -->
```typescript
export { NaverNewsCollector, type NaverArticle } from './naver-news';
export { NaverCommentsCollector, type NaverComment } from './naver-comments';
export { YoutubeVideosCollector, type YoutubeVideo } from './youtube-videos';
export { YoutubeCommentsCollector, type YoutubeComment } from './youtube-comments';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 커뮤니티 공통 타입/유틸 + 3개 수집기 구현</name>
  <files>
    packages/collectors/src/types/community.ts,
    packages/collectors/src/utils/community-parser.ts,
    packages/collectors/src/adapters/dcinside.ts,
    packages/collectors/src/adapters/fmkorea.ts,
    packages/collectors/src/adapters/clien.ts,
    packages/collectors/src/adapters/index.ts,
    packages/collectors/tests/dcinside.test.ts,
    packages/collectors/tests/fmkorea.test.ts,
    packages/collectors/tests/clien.test.ts
  </files>
  <read_first>
    packages/collectors/src/adapters/base.ts,
    packages/collectors/src/adapters/naver-news.ts,
    packages/collectors/src/adapters/registry.ts,
    packages/collectors/src/adapters/index.ts,
    packages/collectors/src/utils/naver-parser.ts
  </read_first>
  <behavior>
    - Test: CommunityPost 타입이 sourceId, url, title, content, author, boardName, publishedAt, rawData 필드를 포함한다
    - Test: CommunityComment 타입이 sourceId, parentId, content, author, likeCount, dislikeCount, publishedAt, rawData 필드를 포함한다
    - Test: parseDateText('2시간 전')이 현재 시간 -2시간의 Date를 반환한다
    - Test: parseDateText('2026.03.24')이 해당 날짜 Date를 반환한다
    - Test: parseDateText('03.24 15:30')이 올해 해당 일시 Date를 반환한다
    - Test: randomDelay(2000, 4000)이 2000~4000ms 범위 숫자를 반환한다
    - Test: DCInsideCollector.source === 'dcinside'
    - Test: FMKoreaCollector.source === 'fmkorea'
    - Test: ClienCollector.source === 'clien'
    - Test: 각 수집기가 Collector 인터페이스를 구현한다 (collect 메서드가 AsyncGenerator 반환)
  </behavior>
  <action>
    **1. 공통 타입 정의 (packages/collectors/src/types/community.ts):**

    ```typescript
    export interface CommunityPost {
      sourceId: string;       // 게시글 고유 ID (예: 'dc_123456')
      url: string;            // 게시글 URL
      title: string;          // 제목
      content: string;        // 본문 텍스트
      author: string;         // 작성자 닉네임
      boardName: string;      // 갤러리/게시판 이름
      publishedAt: Date;      // 작성일시
      viewCount: number;      // 조회수
      commentCount: number;   // 댓글 수
      likeCount: number;      // 추천 수
      rawData: Record<string, unknown>;  // 원본 데이터
      comments: CommunityComment[];      // 수집된 댓글
    }

    export interface CommunityComment {
      sourceId: string;       // 댓글 고유 ID
      parentId: string | null; // 대댓글인 경우 부모 댓글 ID
      content: string;        // 댓글 내용
      author: string;         // 작성자
      likeCount: number;      // 추천
      dislikeCount: number;   // 비추천
      publishedAt: Date;      // 작성일시
      rawData: Record<string, unknown>;
    }
    ```

    **2. 공통 유틸 (packages/collectors/src/utils/community-parser.ts):**

    - `parseDateText(text: string): Date` -- "N시간 전", "N분 전", "YYYY.MM.DD", "MM.DD HH:mm" 등 커뮤니티 날짜 형식 파싱. 네이버 파서의 parseDateText와 유사하되 DC/에펨/클리앙 형식 추가.
    - `randomDelay(min: number, max: number): Promise<void>` -- 반봇 대응 랜덤 딜레이. ms 범위 내 setTimeout.
    - `sanitizeContent(html: string): string` -- HTML 태그 제거, 텍스트만 추출.
    - `buildSearchUrl(site: 'dcinside' | 'fmkorea' | 'clien', keyword: string, page: number): string` -- 사이트별 검색 URL 생성.

    **3. DC갤러리 수집기 (packages/collectors/src/adapters/dcinside.ts):**

    - `export class DCInsideCollector implements Collector<CommunityPost>`
    - `source = 'dcinside'`
    - `collect()`: Playwright로 `search.dcinside.com/post/p/{page}/sort/accuracy/q/{keyword}` 검색. Cheerio로 게시글 목록 파싱(`.ub-content .gall_tit a`). 각 게시글 상세 페이지에서 본문(`.write_div`) + 댓글(`.reply_content .usertxt`) 수집. 페이지당 yield. 반봇: 페이지 간 2~3초, 게시글 간 1~2초 랜덤 딜레이. 마이너 갤러리(`/mgallery/`) 자동 감지. Fallback 셀렉터 배열로 DOM 변경 대응.
    - browserContext: locale='ko-KR', timezoneId='Asia/Seoul', userAgent 로테이션

    **4. 에펨코리아 수집기 (packages/collectors/src/adapters/fmkorea.ts):**

    - `export class FMKoreaCollector implements Collector<CommunityPost>`
    - `source = 'fmkorea'`
    - `collect()`: Playwright로 `fmkorea.com/?mid=politics&act=IS&is_keyword={keyword}&where=document&page={page}` 검색. XE/Rhymix 기반 구조. 게시글 목록 파싱 후 상세 페이지에서 본문 + 댓글 수집. 주요 게시판: politics, best, humor. 반봇: 페이지 간 2~4초, 게시글 간 1~2초.

    **5. 클리앙 수집기 (packages/collectors/src/adapters/clien.ts):**

    - `export class ClienCollector implements Collector<CommunityPost>`
    - `source = 'clien'`
    - `collect()`: Playwright로 `clien.net/service/search?q={keyword}&sort=recency&p={page}` 검색. 403 보호가 강하므로 반드시 Playwright(Axios 차단됨). 게시글 목록(`.list-title`), 상세 본문, 댓글 수집. 주요 게시판: park, news, cm_politics. 반봇: 페이지 간 3~5초, 게시글 간 1.5~2.5초 (가장 엄격).

    **6. index.ts 확장:**

    기존 export에 추가:
    ```typescript
    export { DCInsideCollector, type CommunityPost, type CommunityComment } from './dcinside';
    export { FMKoreaCollector } from './fmkorea';
    export { ClienCollector } from './clien';
    ```
    CommunityPost/CommunityComment 타입은 types/community.ts에서 정의하고 dcinside.ts에서 re-export.

    **7. 테스트:** 각 수집기의 HTML 파싱 로직을 단위 테스트. 고정 HTML fixture를 사용하여 네트워크 없이 셀렉터/파서 검증. Collector 인터페이스 적합성 타입 체크.
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/collectors vitest run tests/dcinside.test.ts tests/fmkorea.test.ts tests/clien.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - packages/collectors/src/types/community.ts 파일이 존재하고 `export interface CommunityPost` 와 `export interface CommunityComment` 를 포함한다
    - packages/collectors/src/utils/community-parser.ts 파일이 존재하고 `export function parseDateText` 와 `export function randomDelay` 를 포함한다
    - packages/collectors/src/adapters/dcinside.ts 파일이 `export class DCInsideCollector` 와 `source = 'dcinside'` 를 포함한다
    - packages/collectors/src/adapters/fmkorea.ts 파일이 `export class FMKoreaCollector` 와 `source = 'fmkorea'` 를 포함한다
    - packages/collectors/src/adapters/clien.ts 파일이 `export class ClienCollector` 와 `source = 'clien'` 를 포함한다
    - packages/collectors/src/adapters/index.ts 파일이 `DCInsideCollector`, `FMKoreaCollector`, `ClienCollector` export를 포함한다
    - 3개 테스트 파일이 존재하고 vitest run이 exit 0으로 통과한다
  </acceptance_criteria>
  <done>3개 커뮤니티 수집기가 Collector 인터페이스를 구현하고, HTML 파싱 테스트가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: 커뮤니티 데이터 정규화 + 파이프라인 통합 + 레지스트리 등록</name>
  <files>
    packages/core/src/pipeline/normalize.ts,
    packages/core/src/pipeline/worker-handlers.ts,
    packages/collectors/src/adapters/registry.ts
  </files>
  <read_first>
    packages/core/src/pipeline/normalize.ts,
    packages/core/src/pipeline/worker-handlers.ts,
    packages/collectors/src/adapters/registry.ts,
    packages/collectors/src/types/community.ts,
    packages/core/src/db/schema.ts
  </read_first>
  <action>
    **1. normalize.ts에 커뮤니티 정규화 함수 추가:**

    ```typescript
    import type { CommunityPost, CommunityComment } from '@ai-signalcraft/collectors';

    export function normalizeCommunityPost(
      post: CommunityPost,
      jobId: number,
      source: 'dcinside' | 'fmkorea' | 'clien',
    ): typeof articles.$inferInsert {
      return {
        jobId,
        source,
        sourceId: post.sourceId,
        url: post.url,
        title: post.title,
        content: post.content,
        author: post.author,
        publisher: post.boardName,   // 갤러리/게시판 이름을 publisher로 매핑
        publishedAt: post.publishedAt,
        rawData: post.rawData,
      };
    }

    export function normalizeCommunityComment(
      comment: CommunityComment,
      jobId: number,
      source: 'dcinside' | 'fmkorea' | 'clien',
      articleDbId?: number,
    ): typeof comments.$inferInsert {
      return {
        jobId,
        source,
        sourceId: comment.sourceId,
        parentId: comment.parentId,
        articleId: articleDbId ?? null,
        videoId: null,
        content: comment.content,
        author: comment.author,
        likeCount: comment.likeCount,
        dislikeCount: comment.dislikeCount,
        publishedAt: comment.publishedAt,
        rawData: comment.rawData,
      };
    }
    ```

    **2. worker-handlers.ts에 커뮤니티 수집 핸들러 추가:**

    기존 `handleCollectNaver`, `handleCollectYoutube` 패턴과 동일하게:
    - `handleCollectDcinside(job)`: DCInsideCollector 인스턴스 생성, collect() 실행, normalizeCommunityPost/Comment로 정규화, DB 저장. sourceId->dbId 매핑 테이블로 댓글 FK 연결 (Phase 1 패턴 계승).
    - `handleCollectFmkorea(job)`: FMKoreaCollector 동일 패턴
    - `handleCollectClien(job)`: ClienCollector 동일 패턴

    각 핸들러는 기존 naver/youtube 핸들러의 try/catch + progress 업데이트 + 부분 실패 허용 패턴을 동일하게 적용.

    **3. registry.ts에 커뮤니티 수집기 등록:**

    파일 하단 또는 별도 init 함수에서:
    ```typescript
    registerCollector(new DCInsideCollector());
    registerCollector(new FMKoreaCollector());
    registerCollector(new ClienCollector());
    ```

    **4. collectionJobs progress 타입 확장:**

    기존 progress JSONB 타입이 naver/youtube만 지원하는 경우, `Record<string, { status: string; posts: number; comments: number }>` 형태로 일반화. 기존 데이터는 JSONB superset이므로 마이그레이션 불필요.
  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/pipeline/normalize.ts 파일이 `export function normalizeCommunityPost` 와 `export function normalizeCommunityComment` 를 포함한다
    - packages/core/src/pipeline/worker-handlers.ts 파일이 `handleCollectDcinside`, `handleCollectFmkorea`, `handleCollectClien` 함수를 포함한다
    - TypeScript 컴파일(tsc --noEmit)이 에러 없이 통과한다
    - `registerCollector(new DCInsideCollector())` 패턴으로 3개 수집기가 레지스트리에 등록된다
  </acceptance_criteria>
  <done>커뮤니티 수집 데이터가 articles/comments 테이블로 정규화되고, BullMQ 워커 핸들러를 통해 파이프라인에서 실행 가능하다</done>
</task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/collectors vitest run -x` -- 수집기 테스트 전체 통과
- `pnpm --filter @ai-signalcraft/core tsc --noEmit` -- 타입 에러 없음
- 3개 수집기가 Collector 인터페이스를 올바르게 구현 (source 속성, collect AsyncGenerator)
- 기존 naver/youtube 수집기가 영향받지 않음
</verification>

<success_criteria>
- DC갤러리, 에펨코리아, 클리앙 수집기가 각각 독립 어댑터로 구현되어 있다
- 커뮤니티 게시글/댓글이 기존 articles/comments 테이블에 정규화 저장된다
- BullMQ 워커 핸들러를 통해 파이프라인에서 커뮤니티 수집이 트리거 가능하다
- 개별 수집기 실패가 전체 파이프라인에 영향을 주지 않는다
- COLL-05(X 수집기)는 구현하지 않는다 (v2 이월, per D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-expansion-advanced-analysis/04-01-SUMMARY.md`
</output>
