---
phase: 04-expansion-advanced-analysis
plan: 03
type: execute
wave: 2
depends_on: ['04-01', '04-02']
files_modified:
  - apps/web/src/components/analysis/trigger-form.tsx
  - apps/web/src/components/layout/top-nav.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/components/advanced/advanced-view.tsx
  - apps/web/src/components/advanced/approval-rating-card.tsx
  - apps/web/src/components/advanced/frame-war-chart.tsx
  - apps/web/src/components/advanced/crisis-scenarios.tsx
  - apps/web/src/components/advanced/win-simulation-card.tsx
  - apps/web/src/server/trpc/routers/analysis.ts
  - apps/web/src/components/dashboard/platform-compare.tsx
autonomous: false
requirements:
  - COLL-06
  - COLL-07
  - COLL-08
  - ADVN-01
  - ADVN-02
  - ADVN-03
  - ADVN-04

must_haves:
  truths:
    - '분석 실행 트리거 폼에서 DC갤러리, 에펨코리아, 클리앙을 소스로 선택할 수 있다'
    - "상단 네비게이션에 5번째 탭 '고급 분석'이 표시된다"
    - '고급 분석 탭에서 AI 지지율 추정, 프레임 전쟁, 위기 시나리오, 승리 시뮬레이션 결과가 시각화된다'
    - 'AI 지지율 카드에 면책 문구가 표시된다'
    - '위기 시나리오 섹션에 3개 시나리오(확산/통제/역전)가 카드 형태로 표시된다'
    - '결과 대시보드의 플랫폼 비교 차트에 커뮤니티 소스가 자동 반영된다'
  artifacts:
    - path: 'apps/web/src/components/advanced/advanced-view.tsx'
      provides: '고급 분석 탭 메인 컴포넌트'
      exports: ['AdvancedView']
    - path: 'apps/web/src/components/advanced/approval-rating-card.tsx'
      provides: 'AI 지지율 카드'
      exports: ['ApprovalRatingCard']
    - path: 'apps/web/src/components/advanced/frame-war-chart.tsx'
      provides: '프레임 전쟁 시각화'
      exports: ['FrameWarChart']
    - path: 'apps/web/src/components/advanced/crisis-scenarios.tsx'
      provides: '위기 시나리오 카드 3종'
      exports: ['CrisisScenarios']
    - path: 'apps/web/src/components/advanced/win-simulation-card.tsx'
      provides: '승리 시뮬레이션 카드'
      exports: ['WinSimulationCard']
  key_links:
    - from: 'apps/web/src/app/page.tsx'
      to: 'apps/web/src/components/advanced/advanced-view.tsx'
      via: '5번째 탭 패널 렌더링'
      pattern: 'AdvancedView'
    - from: 'apps/web/src/components/layout/top-nav.tsx'
      to: 'TAB_LABELS 배열'
      via: "'고급 분석' 추가"
      pattern: '고급 분석'
    - from: 'apps/web/src/components/analysis/trigger-form.tsx'
      to: 'sources enum'
      via: "'dcinside', 'fmkorea', 'clien' 추가"
      pattern: 'dcinside.*fmkorea.*clien'
---

<objective>
대시보드에 커뮤니티 소스 선택 UI와 고급 분석 전용 탭을 추가하여 Phase 4 기능을 사용자에게 노출한다.

Purpose: 커뮤니티 수집기(Plan 01)와 고급 분석 모듈(Plan 02)의 결과를 대시보드 UI에서 트리거하고 시각화한다. (per D-08, D-09, D-10)
Output: 트리거 폼 확장 + 5번째 탭(고급 분석) + 4개 시각화 컴포넌트 + 플랫폼 비교 차트 확장
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-expansion-advanced-analysis/04-CONTEXT.md
@.planning/phases/04-expansion-advanced-analysis/04-RESEARCH.md
@.planning/phases/04-expansion-advanced-analysis/04-01-SUMMARY.md
@.planning/phases/04-expansion-advanced-analysis/04-02-SUMMARY.md

<interfaces>
<!-- TopNav 탭 구조 (apps/web/src/components/layout/top-nav.tsx) -->
```typescript
const TAB_LABELS = ['분석 실행', '결과 대시보드', 'AI 리포트', '히스토리'] as const;
// -> ['분석 실행', '결과 대시보드', 'AI 리포트', '히스토리', '고급 분석'] 으로 확장

interface TopNavProps {
activeTab: number;
onTabChange: (index: number) => void;
}

````

<!-- 메인 페이지 탭 구조 (apps/web/src/app/page.tsx) -->
```typescript
// 현재 4개 탭: AnalysisTab(0), DashboardTab(1), ReportTab(2), HistoryTabPanel(3)
// -> 5번째 탭: AdvancedTab(4) 추가
````

<!-- 트리거 폼 소스 타입 (apps/web/src/components/analysis/trigger-form.tsx) -->

```typescript
// 현재: sources: ('naver' | 'youtube')[]
// -> sources: ('naver' | 'youtube' | 'dcinside' | 'fmkorea' | 'clien')[] 확장
```

<!-- tRPC analysis 라우터 (apps/web/src/server/trpc/routers/analysis.ts) -->

```typescript
// sources 입력 스키마 확장 필요
```

<!-- ADVN 결과 스키마 (Plan 02에서 생성) -->

```typescript
// ApprovalRatingResult: { estimatedRange: {min, max}, confidence, methodology, disclaimer, reasoning }
// FrameWarResult: { dominantFrames, threateningFrames, reversibleFrames, battlefieldSummary }
// CrisisScenarioResult: { scenarios: [spread, control, reverse], currentRiskLevel, recommendedAction }
// WinSimulationResult: { winProbability, confidenceLevel, winConditions, loseConditions, keyStrategies, simulationSummary }
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 트리거 폼 소스 확장 + tRPC 라우터 + 플랫폼 비교 차트 확장</name>
  <files>
    apps/web/src/components/analysis/trigger-form.tsx,
    apps/web/src/server/trpc/routers/analysis.ts,
    apps/web/src/components/dashboard/platform-compare.tsx
  </files>
  <read_first>
    apps/web/src/components/analysis/trigger-form.tsx,
    apps/web/src/server/trpc/routers/analysis.ts,
    apps/web/src/components/dashboard/platform-compare.tsx,
    apps/web/src/components/dashboard/dashboard-view.tsx
  </read_first>
  <action>
    **1. trigger-form.tsx 수정 (per D-09):**

    sources 타입을 `('naver' | 'youtube' | 'dcinside' | 'fmkorea' | 'clien')[]`로 확장.

    기존 체크박스 UI를 그룹화:
    ```typescript
    const SOURCE_OPTIONS = [
      { group: '뉴스/영상', items: [
        { id: 'naver', label: '네이버 뉴스' },
        { id: 'youtube', label: '유튜브' },
      ]},
      { group: '커뮤니티', items: [
        { id: 'dcinside', label: 'DC갤러리' },
        { id: 'fmkorea', label: '에펨코리아' },
        { id: 'clien', label: '클리앙' },
      ]},
    ];
    ```

    "전체 선택" 체크박스가 5개 소스 모두 토글하도록 수정:
    ```typescript
    const ALL_SOURCES = ['naver', 'youtube', 'dcinside', 'fmkorea', 'clien'];
    const handleAllToggle = (checked: boolean) => {
      setSources(checked ? [...ALL_SOURCES] : []);
    };
    ```

    초기값은 기존과 동일하게 `['naver', 'youtube']` 유지 (커뮤니티는 선택적).

    mutationFn의 input 타입도 sources 배열 타입 확장:
    ```typescript
    mutationFn: (input: {
      keyword: string;
      sources: ('naver' | 'youtube' | 'dcinside' | 'fmkorea' | 'clien')[];
      startDate: string;
      endDate: string;
    }) => trpcClient.analysis.trigger.mutate(input),
    ```

    **2. analysis.ts tRPC 라우터 수정:**

    trigger mutation의 input 스키마에서 sources를 확장:
    ```typescript
    // 기존: sources: z.array(z.enum(['naver', 'youtube']))
    // 변경:
    sources: z.array(z.enum(['naver', 'youtube', 'dcinside', 'fmkorea', 'clien']))
    ```

    결과 조회 라우터에서 고급 분석 결과(ADVN 모듈)도 반환하도록 확장. analysis_results 테이블에서 module이 'approval-rating', 'frame-war', 'crisis-scenario', 'win-simulation'인 결과도 포함.

    **3. platform-compare.tsx 수정 (per D-09):**

    기존 플랫폼 비교 차트에 커뮤니티 소스 자동 반영. 데이터에 source가 'dcinside', 'fmkorea', 'clien'인 항목이 있으면 차트에 자동 포함.

    소스별 레이블 매핑:
    ```typescript
    const SOURCE_LABELS: Record<string, string> = {
      naver: '네이버 뉴스',
      youtube: '유튜브',
      dcinside: 'DC갤러리',
      fmkorea: '에펨코리아',
      clien: '클리앙',
    };
    ```

    소스별 색상도 추가 (shadcn 차트 테마 변수 활용):
    ```typescript
    const SOURCE_COLORS: Record<string, string> = {
      naver: 'var(--chart-1)',
      youtube: 'var(--chart-2)',
      dcinside: 'var(--chart-3)',
      fmkorea: 'var(--chart-4)',
      clien: 'var(--chart-5)',
    };
    ```

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/web tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/analysis/trigger-form.tsx 파일이 `'dcinside'`, `'fmkorea'`, `'clien'` 문자열을 포함한다
    - apps/web/src/components/analysis/trigger-form.tsx 파일이 `커뮤니티` 그룹 레이블을 포함한다
    - apps/web/src/server/trpc/routers/analysis.ts 파일이 `z.enum(['naver', 'youtube', 'dcinside', 'fmkorea', 'clien'])` 또는 동등한 Zod 스키마를 포함한다
    - apps/web/src/components/dashboard/platform-compare.tsx 파일이 `DC갤러리`, `에펨코리아`, `클리앙` 레이블을 포함한다
    - TypeScript 컴파일(tsc --noEmit)이 에러 없이 통과한다
  </acceptance_criteria>
  <done>트리거 폼에서 5개 소스를 선택할 수 있고, 플랫폼 비교 차트에 커뮤니티 소스가 자동 반영된다</done>
</task>

<task type="auto">
  <name>Task 2: 고급 분석 탭 UI + 4개 시각화 컴포넌트 (per D-08)</name>
  <files>
    apps/web/src/components/layout/top-nav.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/components/advanced/advanced-view.tsx,
    apps/web/src/components/advanced/approval-rating-card.tsx,
    apps/web/src/components/advanced/frame-war-chart.tsx,
    apps/web/src/components/advanced/crisis-scenarios.tsx,
    apps/web/src/components/advanced/win-simulation-card.tsx
  </files>
  <read_first>
    apps/web/src/components/layout/top-nav.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/components/dashboard/dashboard-view.tsx,
    apps/web/src/components/dashboard/risk-cards.tsx,
    apps/web/src/components/dashboard/sentiment-chart.tsx,
    apps/web/src/server/trpc/routers/analysis.ts
  </read_first>
  <action>
    **1. top-nav.tsx 수정 (per D-08):**

    TAB_LABELS 배열에 '고급 분석' 추가:
    ```typescript
    const TAB_LABELS = ['분석 실행', '결과 대시보드', 'AI 리포트', '히스토리', '고급 분석'] as const;
    ```

    **2. page.tsx 수정:**

    5번째 탭 패널 추가:
    ```typescript
    import { AdvancedView } from '@/components/advanced/advanced-view';

    // 5번째 탭 패널 컴포넌트
    function AdvancedTab({ jobId }: { jobId: number | null }) {
      return <AdvancedView jobId={jobId} />;
    }
    ```

    TabLayout의 panels 배열에 인덱스 4로 AdvancedTab 추가. 기존 0~3 탭 로직은 변경 없음.

    **3. advanced-view.tsx (고급 분석 탭 메인):**

    `'use client'` 컴포넌트. jobId prop을 받아 tRPC로 고급 분석 결과 조회. TanStack Query 사용.
    - jobId가 null이면 "분석 결과를 선택하세요" 안내
    - 데이터 로딩 중이면 Skeleton UI
    - 결과가 있으면 4개 시각화 컴포넌트를 2x2 그리드로 배치:
      - 좌상: ApprovalRatingCard
      - 우상: FrameWarChart
      - 좌하: CrisisScenarios
      - 우하: WinSimulationCard
    - ADVN 결과가 없는(이전 분석 등) jobId면 "이 분석에는 고급 분석 결과가 없습니다" 안내

    레이아웃: `grid grid-cols-1 lg:grid-cols-2 gap-4`

    **4. approval-rating-card.tsx (ADVN-01 시각화):**

    shadcn Card 기반. 내용:
    - 추정 지지율 범위를 큰 숫자(예: "45~55%")로 표시
    - 신뢰도(confidence) 배지 (high=green, medium=yellow, low=red)
    - 감정 비율(sentimentRatio) 도넛 차트 (Recharts PieChart)
    - 플랫폼 편향 보정 테이블
    - **면책 문구를 카드 하단에 회색 텍스트로 반드시 표시** (per D-05)
    - reasoning을 접이식(Collapsible) 영역으로

    **5. frame-war-chart.tsx (ADVN-02 시각화):**

    프레임 전쟁 시각화:
    - 지배적 프레임 TOP5를 BarChart로 (strength 기준 내림차순)
    - 위협 프레임을 경고 카드(빨간 테두리)로
    - 반전 가능 프레임을 기회 카드(파란 테두리)로
    - battlefieldSummary를 상단 요약문으로

    **6. crisis-scenarios.tsx (ADVN-03 시각화):**

    3개 시나리오를 가로 3열 카드로 (per D-06):
    - 확산(spread): 빨간 테마, AlertTriangle 아이콘
    - 통제(control): 노란 테마, Shield 아이콘
    - 역전(reverse): 초록 테마, TrendingUp 아이콘
    각 카드: 시나리오명, 발생 확률(%), 트리거 조건 목록, 대응 전략 목록, 예상 결과, 소요 기간
    하단: 현재 위험도(currentRiskLevel) 배지 + 권고 행동(recommendedAction)

    **7. win-simulation-card.tsx (ADVN-04 시각화):**

    승리 시뮬레이션:
    - 승리 확률을 큰 원형 프로그레스로 표시 (RadialBarChart 또는 커스텀 SVG)
    - 신뢰도 배지
    - 승리 조건 체크리스트 (met=check, partial=minus, unmet=x 아이콘)
    - 패배 조건 경고 리스트 (리스크 레벨별 색상)
    - 핵심 전략 우선순위 목록 (priority 기준 정렬)
    - simulationSummary를 하단 요약문으로

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/web tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/layout/top-nav.tsx 파일이 `'고급 분석'` 문자열을 TAB_LABELS 배열에 포함한다
    - apps/web/src/app/page.tsx 파일이 `AdvancedView` import와 `AdvancedTab` 컴포넌트를 포함한다
    - apps/web/src/components/advanced/advanced-view.tsx 파일이 존재하고 `export function AdvancedView` 를 포함한다
    - apps/web/src/components/advanced/approval-rating-card.tsx 파일이 존재하고 `면책` 또는 `disclaimer` 문자열을 포함한다
    - apps/web/src/components/advanced/frame-war-chart.tsx 파일이 존재하고 `dominantFrames` 접근 코드를 포함한다
    - apps/web/src/components/advanced/crisis-scenarios.tsx 파일이 존재하고 `spread`, `control`, `reverse` 문자열을 포함한다
    - apps/web/src/components/advanced/win-simulation-card.tsx 파일이 존재하고 `winProbability` 접근 코드를 포함한다
    - TypeScript 컴파일(tsc --noEmit)이 에러 없이 통과한다
  </acceptance_criteria>
  <done>대시보드에 5번째 '고급 분석' 탭이 추가되고, 4개 시각화 컴포넌트가 ADVN 분석 결과를 표시한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 전체 UI 시각적/기능적 검증</name>
  <files>none</files>
  <action>
    사용자가 브라우저에서 Phase 4 전체 UI를 시각적/기능적으로 검증한다.
    Task 1~2에서 구현된 트리거 폼 소스 확장, 고급 분석 탭, 4개 시각화 컴포넌트, 플랫폼 비교 차트 확장을 확인한다.
  </action>
  <what-built>
    Phase 4 전체 기능:
    1. 트리거 폼에 커뮤니티 3종(DC갤러리/에펨코리아/클리앙) 소스 선택 추가
    2. 상단 네비에 5번째 '고급 분석' 탭 추가
    3. 고급 분석 탭에 4개 시각화 카드 (AI 지지율/프레임 전쟁/위기 시나리오/승리 시뮬레이션)
    4. 플랫폼 비교 차트에 커뮤니티 소스 반영
    5. AI 지지율 카드에 면책 문구 표시
  </what-built>
  <how-to-verify>
    1. `pnpm dev` 실행 후 http://localhost:3000 접속
    2. 로그인 후 '분석 실행' 탭에서 소스 체크박스가 5개(네이버/유튜브/DC갤러리/에펨코리아/클리앙)인지 확인
    3. '커뮤니티' 그룹 레이블이 표시되는지 확인
    4. 상단 네비에 5개 탭(분석 실행/결과 대시보드/AI 리포트/히스토리/고급 분석)이 표시되는지 확인
    5. '고급 분석' 탭 클릭 시 4개 시각화 카드가 2x2 그리드로 배치되는지 확인
    6. (이전 분석 결과가 있다면) 결과 대시보드의 플랫폼 비교 차트가 정상 렌더링되는지 확인
    7. 다크모드 전환 시 고급 분석 탭이 정상 렌더링되는지 확인
  </how-to-verify>
  <verify>사용자가 브라우저에서 7개 항목을 시각적으로 확인</verify>
  <done>사용자가 "approved"를 입력하여 Phase 4 UI가 정상 동작함을 확인</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @ai-signalcraft/web tsc --noEmit` -- 타입 에러 없음
- `pnpm --filter @ai-signalcraft/web build` -- 빌드 성공
- 5개 탭이 정상 표시되고 각 탭 클릭 시 올바른 컨텐츠 렌더링
- 트리거 폼에서 5개 소스 선택 가능
- 고급 분석 탭에 4개 시각화 컴포넌트 렌더링
</verification>

<success_criteria>

- 트리거 폼에서 커뮤니티 3종 소스를 선택하여 분석을 트리거할 수 있다
- 상단 네비에 '고급 분석' 탭이 5번째로 표시된다
- 고급 분석 탭에서 4개 시각화 컴포넌트가 정상 렌더링된다
- AI 지지율 카드에 면책 문구가 표시된다
- 위기 시나리오 카드가 확산/통제/역전 3개로 표시된다
- 플랫폼 비교 차트에 커뮤니티 소스가 반영된다
  </success_criteria>

<output>
After completion, create `.planning/phases/04-expansion-advanced-analysis/04-03-SUMMARY.md`
</output>
