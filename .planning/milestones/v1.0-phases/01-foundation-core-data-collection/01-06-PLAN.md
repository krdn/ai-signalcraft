---
phase: 01-foundation-core-data-collection
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/queue/flows.ts
  - packages/core/src/queue/worker-process.ts
  - packages/core/tests/worker.test.ts
autonomous: true
gap_closure: true
requirements: [COLL-02]

must_haves:
  truths:
    - '파이프라인 실행 시 네이버 기사 수집 후 댓글이 자동 수집된다'
    - '수집된 네이버 댓글이 정규화되어 persist 단계로 전달된다'
    - '유튜브 수집 흐름은 기존과 동일하게 동작한다'
  artifacts:
    - path: 'packages/core/src/queue/flows.ts'
      provides: '네이버 댓글 수집이 Flow에서 제거되고 normalize-naver에서 처리'
      contains: 'collect-naver-articles'
    - path: 'packages/core/src/queue/worker-process.ts'
      provides: 'normalize-naver 핸들러에서 기사 URL 추출 후 댓글 수집'
      contains: 'collectForArticle'
    - path: 'packages/core/tests/worker.test.ts'
      provides: '댓글 수집 통합 로직 검증'
  key_links:
    - from: 'packages/core/src/queue/worker-process.ts'
      to: 'packages/collectors/src/adapters/naver-comments.ts'
      via: 'NaverCommentsCollector.collectForArticle() 직접 호출'
      pattern: 'collectForArticle'
    - from: 'packages/core/src/queue/flows.ts'
      to: 'packages/core/src/queue/worker-process.ts'
      via: 'normalize-naver job이 collect-naver-articles 결과를 받아 댓글 수집 수행'
      pattern: 'normalize-naver'
---

<objective>
네이버 댓글 수집 파이프라인 연결 (COLL-02 Gap Closure)

Purpose: NaverCommentsCollector.collect()가 빈 제너레이터를 반환하는 blocker를 해결한다. BullMQ Flow에서 collect-naver-comments 자식 작업을 제거하고, normalize-naver 핸들러에서 기사 수집 결과의 URL을 추출하여 collectForArticle()로 댓글을 수집한다.
Output: 네이버 기사 수집 후 댓글이 자동 수집되어 persist 단계로 전달되는 완전한 파이프라인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-data-collection/01-05-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/collectors/src/adapters/naver-news.ts:

```typescript
export interface NaverArticle {
  sourceId: string; // oid + aid 조합
  url: string;
  title: string;
  content: string | null;
  author: string | null;
  publisher: string;
  publishedAt: Date | null;
  rawData: Record<string, unknown>;
}
```

From packages/collectors/src/adapters/naver-comments.ts:

```typescript
export interface NaverComment {
  sourceId: string;
  parentId: string | null;
  articleSourceId: string;
  content: string;
  author: string;
  likeCount: number;
  dislikeCount: number;
  publishedAt: Date | null;
  rawData: Record<string, unknown>;
}

export class NaverCommentsCollector implements Collector<NaverComment> {
  readonly source = 'naver-comments';
  async *collect(options: CollectionOptions): AsyncGenerator<NaverComment[], void, unknown>; // 빈 제너레이터 (변경 불필요)
  async *collectForArticle(
    articleUrl: string,
    options?: { maxComments?: number },
  ): AsyncGenerator<NaverComment[], void, unknown>; // 실제 댓글 수집 로직
}
```

From packages/collectors/src/adapters/base.ts:

```typescript
export interface Collector<T = unknown> {
  readonly source: string;
  collect(options: CollectionOptions): AsyncGenerator<T[], void, unknown>;
}
```

From packages/core/src/queue/workers.ts:

```typescript
export function createCollectorWorker(processJob: (job: Job) => Promise<any>): Worker;
export function createPipelineWorker(processJob: (job: Job) => Promise<any>): Worker;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flow 구조 변경 + normalize-naver 핸들러에서 댓글 수집 통합</name>
  <files>
    packages/core/src/queue/flows.ts,
    packages/core/src/queue/worker-process.ts
  </files>
  <read_first>
    packages/core/src/queue/flows.ts,
    packages/core/src/queue/worker-process.ts,
    packages/collectors/src/adapters/naver-comments.ts,
    packages/collectors/src/adapters/naver-news.ts
  </read_first>
  <action>
**1. flows.ts 수정 -- collect-naver-comments 자식 작업 제거**

`triggerCollection` 함수에서 `normalize-naver` 노드의 children 배열을 수정한다:

- `collect-naver-comments` 항목을 삭제한다
- `collect-naver-articles`만 남긴다
- `normalize-naver`의 data에 `maxComments: limits.commentsPerItem` 필드를 추가한다 (댓글 수집 상한을 전달하기 위해)

수정 전:

```typescript
children: [
  { name: 'collect-naver-articles', ... },
  { name: 'collect-naver-comments', ... },
],
```

수정 후:

```typescript
children: [
  {
    name: 'collect-naver-articles',
    queueName: 'collectors',
    data: { ...params, source: 'naver-news', maxItems: limits.naverArticles, maxComments: limits.commentsPerItem, flowId, dbJobId },
  },
],
```

`normalize-naver` 노드의 data를 아래와 같이 변경:

```typescript
{
  name: 'normalize-naver',
  queueName: 'pipeline',
  data: { source: 'naver-news', flowId, dbJobId, maxComments: limits.commentsPerItem },
  children: [ /* collect-naver-articles만 */ ],
},
```

**2. worker-process.ts 수정 -- normalize-naver 핸들러에서 댓글 수집**

pipelineWorker의 `if (job.name.startsWith('normalize-'))` 블록 내부를 수정한다.

기존 로직: 자식 작업 결과를 단순히 results 객체로 모아 반환
변경 로직: `normalize-naver` job인 경우, 자식 결과(기사 목록)에서 URL을 추출하고 NaverCommentsCollector.collectForArticle()을 호출하여 댓글을 수집한 뒤, 그 결과도 results에 포함

구체적 구현:

```typescript
if (job.name.startsWith('normalize-')) {
  const childValues = await job.getChildrenValues();
  const results: Record<string, unknown> = {};

  for (const [key, value] of Object.entries(childValues)) {
    const childResult = value as { source: string; items: unknown[]; count: number };
    results[childResult.source] = childResult;
  }

  // normalize-naver: 기사 수집 결과에서 URL 추출 후 댓글 수집
  if (job.name === 'normalize-naver' && results['naver-news']) {
    const articles = (results['naver-news'] as { items: Array<{ url: string }> }).items;
    const maxComments = (job.data.maxComments as number) ?? 500;
    const commentsCollector = new NaverCommentsCollector();
    const allComments: NaverComment[] = [];

    for (const article of articles) {
      if (!article.url) continue;
      try {
        for await (const chunk of commentsCollector.collectForArticle(article.url, {
          maxComments,
        })) {
          allComments.push(...chunk);
        }
      } catch (err) {
        // D-04: 부분 실패 허용 -- 개별 기사 댓글 실패 시 로깅 후 계속
        console.warn(`댓글 수집 실패 (${article.url}):`, err);
      }
      await job.updateProgress({ commentsCollected: allComments.length });
    }

    if (allComments.length > 0) {
      results['naver-comments'] = {
        source: 'naver-comments',
        items: allComments,
        count: allComments.length,
      };
    }
  }

  return { source, dbJobId, normalized: true, results };
}
```

NaverComment 타입을 import에 추가:

```typescript
import {
  NaverNewsCollector,
  NaverCommentsCollector,
  YoutubeVideosCollector,
  YoutubeCommentsCollector,
  registerCollector,
  getCollector,
} from '@ai-signalcraft/collectors';
import type { NaverComment } from '@ai-signalcraft/collectors';
```

collectors 패키지에서 NaverComment 타입이 re-export되는지 확인하고, 안 되면 직접 import 경로를 사용한다. 또한 registerCollector에서 NaverCommentsCollector 등록은 유지한다 (다른 곳에서 사용할 수 있으므로).

주의: `NaverComment` 타입이 collectors 패키지의 index.ts에서 export되지 않는 경우, 단순히 `unknown[]`으로 처리해도 된다 -- persist 단계에서 `as any`로 이미 캐스팅하고 있으므로.
</action>
<verify>
<automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm -r build</automated>
</verify>
<acceptance_criteria> - grep -q "collect-naver-comments" packages/core/src/queue/flows.ts 의 결과가 NOT FOUND (제거됨) - grep -q "collectForArticle" packages/core/src/queue/worker-process.ts 의 결과가 FOUND - grep -q "normalize-naver" packages/core/src/queue/worker-process.ts 의 결과가 FOUND - grep -q "naver-comments" packages/core/src/queue/worker-process.ts 의 결과가 FOUND (댓글 결과를 results에 추가) - grep -q "maxComments" packages/core/src/queue/flows.ts 의 결과가 FOUND (normalize-naver data에 포함) - pnpm -r build 성공 (exit code 0)
</acceptance_criteria>
<done> - flows.ts에서 collect-naver-comments 자식 작업이 제거됨 - normalize-naver data에 maxComments가 포함됨 - worker-process.ts의 normalize-naver 핸들러에서 기사 URL 추출 후 collectForArticle() 호출 - 댓글 수집 결과가 results['naver-comments']에 포함됨 - 개별 기사 댓글 수집 실패 시 로깅 후 계속 (D-04 부분 실패 허용) - TypeScript 빌드 성공
</done>
</task>

<task type="auto">
  <name>Task 2: Worker 테스트 업데이트 + 전체 테스트 통과 확인</name>
  <files>
    packages/core/tests/worker.test.ts
  </files>
  <read_first>
    packages/core/tests/worker.test.ts,
    packages/core/src/queue/worker-process.ts,
    packages/core/src/queue/flows.ts
  </read_first>
  <action>
worker.test.ts에 아래 테스트를 추가한다:

1. **flows.ts에서 collect-naver-comments 제거 확인 테스트:**

```typescript
import { readFileSync } from 'fs';
import { resolve } from 'path';

describe('naver comments pipeline integration', () => {
  it('flows.ts에서 collect-naver-comments가 제거되었다', () => {
    const flowsContent = readFileSync(resolve(__dirname, '../src/queue/flows.ts'), 'utf-8');
    expect(flowsContent).not.toContain("name: 'collect-naver-comments'");
    expect(flowsContent).toContain("name: 'collect-naver-articles'");
  });

  it('worker-process.ts에서 normalize-naver 시 collectForArticle을 호출한다', () => {
    const workerContent = readFileSync(
      resolve(__dirname, '../src/queue/worker-process.ts'),
      'utf-8',
    );
    expect(workerContent).toContain('collectForArticle');
    expect(workerContent).toContain("job.name === 'normalize-naver'");
  });

  it('normalize-naver data에 maxComments가 포함된다', () => {
    const flowsContent = readFileSync(resolve(__dirname, '../src/queue/flows.ts'), 'utf-8');
    // normalize-naver의 data 객체에 maxComments가 있어야 함
    expect(flowsContent).toContain('maxComments');
  });
});
```

기존 worker.test.ts의 테스트가 깨지는지 확인하고, 깨진다면 수정한다.
</action>
<verify>
<automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test</automated>
</verify>
<acceptance_criteria> - grep -q "collect-naver-comments가 제거" packages/core/tests/worker.test.ts 의 결과가 FOUND - grep -q "collectForArticle" packages/core/tests/worker.test.ts 의 결과가 FOUND - pnpm --filter @ai-signalcraft/core test 전체 통과 (exit code 0) - pnpm -r build 성공 (exit code 0)
</acceptance_criteria>
<done> - worker.test.ts에 네이버 댓글 파이프라인 통합 테스트 3개 추가됨 - 기존 테스트 포함 전체 core 패키지 테스트 통과 - TypeScript 빌드 성공
</done>
</task>

</tasks>

<verification>
1. `pnpm -r build` -- 전체 빌드 성공
2. `pnpm --filter @ai-signalcraft/core test` -- 전체 테스트 통과
3. `grep -c "collect-naver-comments" packages/core/src/queue/flows.ts` -- 0 (제거 확인)
4. `grep -c "collectForArticle" packages/core/src/queue/worker-process.ts` -- 1 이상 (호출 확인)
5. 코드 리뷰: normalize-naver 핸들러에서 기사 URL 추출 -> collectForArticle 호출 -> results['naver-comments'] 저장 흐름 확인
</verification>

<success_criteria>

- COLL-02 blocker 해결: 네이버 댓글 수집이 파이프라인에서 실제로 동작
- flows.ts에서 불필요한 collect-naver-comments 자식 작업 제거
- worker-process.ts에서 normalize-naver 핸들러가 기사 수집 결과의 URL로 댓글을 자동 수집
- 기존 유튜브 수집 흐름에 영향 없음
- 전체 빌드 및 테스트 통과
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-data-collection/01-06-SUMMARY.md`
</output>
