---
phase: 01-foundation-core-data-collection
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - packages/core/src/queue/connection.ts
  - packages/core/src/queue/flows.ts
  - packages/core/src/queue/workers.ts
  - packages/core/src/queue/index.ts
  - packages/core/src/types/index.ts
  - packages/core/src/index.ts
  - packages/collectors/src/adapters/base.ts
  - packages/collectors/src/adapters/registry.ts
  - packages/collectors/src/adapters/index.ts
  - packages/collectors/src/index.ts
  - packages/ai-gateway/src/gateway.ts
  - packages/ai-gateway/src/index.ts
  - packages/core/tests/queue.test.ts
  - packages/collectors/tests/adapter.test.ts
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
  - COLL-09

must_haves:
  truths:
    - 'BullMQ FlowProducer로 collect -> normalize -> persist 3단계 파이프라인이 정의된다'
    - 'Collector 인터페이스를 구현하면 자동으로 파이프라인에 등록할 수 있다'
    - 'AI Gateway가 Claude/GPT 프로바이더를 단일 API로 라우팅한다'
  artifacts:
    - path: 'packages/core/src/queue/flows.ts'
      provides: 'triggerCollection FlowProducer 함수'
      exports: ['triggerCollection']
    - path: 'packages/collectors/src/adapters/base.ts'
      provides: 'Collector 인터페이스 + CollectionOptions 타입'
      exports: ['Collector', 'CollectionOptions', 'CollectionResult', 'CollectionOptionsSchema']
    - path: 'packages/ai-gateway/src/gateway.ts'
      provides: 'AI Gateway 추상화'
      exports: ['analyzeWithAI']
  key_links:
    - from: 'packages/core/src/queue/flows.ts'
      to: 'packages/core/src/queue/connection.ts'
      via: 'Redis connection import'
      pattern: 'import.*connection'
    - from: 'packages/collectors/src/adapters/registry.ts'
      to: 'packages/collectors/src/adapters/base.ts'
      via: 'Collector interface import'
      pattern: 'import.*Collector.*base'
---

<objective>
BullMQ 파이프라인 오케스트레이터, Collector Adapter 인터페이스, AI Gateway 골격을 구축한다.

Purpose: 수집기 구현(Plan 03, 04)과 파이프라인 통합(Plan 05)의 기반 계약(contract)을 수립한다.
Output: BullMQ Flow 정의 + Collector 인터페이스 + AI Gateway 골격
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-data-collection/01-CONTEXT.md
@.planning/phases/01-foundation-core-data-collection/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Plan 01에서 생성된 핵심 인터페이스 -->

From packages/core/src/db/schema/collections.ts:

```typescript
export const collectionJobs = pgTable('collection_jobs', { ... });
export const articles = pgTable('articles', { ... });
export const videos = pgTable('videos', { ... });
export const comments = pgTable('comments', { ... });
```

From packages/core/src/db/index.ts:

```typescript
export const db = drizzle(pool, { schema });
export type Database = typeof db;
```

From packages/core/src/index.ts:

```typescript
export { db } from './db';
export type { Database } from './db';
export * from './db/schema';
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BullMQ 파이프라인 오케스트레이터 + Collector Adapter 인터페이스</name>
  <files>
    packages/core/src/queue/connection.ts,
    packages/core/src/queue/flows.ts,
    packages/core/src/queue/workers.ts,
    packages/core/src/queue/index.ts,
    packages/core/src/types/index.ts,
    packages/core/src/index.ts,
    packages/collectors/src/adapters/base.ts,
    packages/collectors/src/adapters/registry.ts,
    packages/collectors/src/adapters/index.ts,
    packages/collectors/src/index.ts
  </files>
  <read_first>
    packages/core/src/index.ts,
    packages/core/src/db/schema/collections.ts,
    packages/core/package.json,
    packages/collectors/package.json,
    .planning/phases/01-foundation-core-data-collection/01-RESEARCH.md
  </read_first>
  <action>
1. `packages/core/src/types/index.ts` -- 공유 타입 정의 (per D-01, D-02, D-03):
```typescript
import { z } from 'zod';

export const CollectionTriggerSchema = z.object({
keyword: z.string().min(1),
startDate: z.string().datetime(), // ISO 8601
endDate: z.string().datetime(),
limits: z.object({
naverArticles: z.number().default(100),
youtubeVideos: z.number().default(50),
commentsPerItem: z.number().default(500),
}).optional(),
});
export type CollectionTrigger = z.infer<typeof CollectionTriggerSchema>;

export type SourceStatus = {
status: 'pending' | 'running' | 'completed' | 'failed';
articles?: number;
comments?: number;
videos?: number;
error?: string;
};

export type JobProgress = {
naver: SourceStatus;
youtube: SourceStatus;
};

````

2. `packages/core/src/queue/connection.ts` -- Redis 연결:
```typescript
import IORedis from 'ioredis';

export const redisConnection = new IORedis(process.env.REDIS_URL || 'redis://localhost:6383', {
  maxRetriesPerRequest: null,  // BullMQ 필수 설정
});
````

3. `packages/core/src/queue/flows.ts` -- FlowProducer (per D-01, D-05):

**IMPORTANT (Blocker Fix):** `triggerCollection`은 두 번째 인자로 `dbJobId: number`를 받는다.
이 값은 `collection_jobs` 테이블의 정수 PK이며, 호출자(CLI 스크립트)가 `createCollectionJob()`으로
DB 레코드를 먼저 생성한 뒤 반환된 `job.id`를 전달한다. `flowId`(string)는 BullMQ 내부 식별자이고,
`dbJobId`(number)는 normalize/persist 함수가 DB FK로 사용하는 값이다. 이 둘을 혼동하면 NaN이 발생한다.

```typescript
import { FlowProducer } from 'bullmq';
import { redisConnection } from './connection';
import type { CollectionTrigger } from '../types';

const flowProducer = new FlowProducer({ connection: redisConnection });

// dbJobId: collection_jobs 테이블의 정수 PK -- 호출자가 createCollectionJob() 후 전달
export async function triggerCollection(params: CollectionTrigger, dbJobId: number) {
  const flowId = `collection-${Date.now()}`;
  const limits = params.limits ?? {
    naverArticles: 100,
    youtubeVideos: 50,
    commentsPerItem: 500,
  };

  // D-05: 3단계 분리 -- collect -> normalize -> persist
  // D-01: 통합 키워드 수집 -- 모든 소스 동시 실행
  // D-04: 부분 실패 허용 -- 소스별 독립 실행
  const flow = await flowProducer.add({
    name: 'persist',
    queueName: 'pipeline',
    data: { flowId, dbJobId, keyword: params.keyword },
    children: [
      {
        name: 'normalize-naver',
        queueName: 'pipeline',
        data: { source: 'naver-news', flowId, dbJobId },
        children: [
          {
            name: 'collect-naver-articles',
            queueName: 'collectors',
            data: {
              ...params,
              source: 'naver-news',
              maxItems: limits.naverArticles,
              maxComments: limits.commentsPerItem,
              flowId,
              dbJobId,
            },
          },
          {
            name: 'collect-naver-comments',
            queueName: 'collectors',
            data: {
              ...params,
              source: 'naver-comments',
              maxComments: limits.commentsPerItem,
              flowId,
              dbJobId,
            },
          },
        ],
      },
      {
        name: 'normalize-youtube',
        queueName: 'pipeline',
        data: { source: 'youtube', flowId, dbJobId },
        children: [
          {
            name: 'collect-youtube-videos',
            queueName: 'collectors',
            data: {
              ...params,
              source: 'youtube-videos',
              maxItems: limits.youtubeVideos,
              flowId,
              dbJobId,
            },
          },
          {
            name: 'collect-youtube-comments',
            queueName: 'collectors',
            data: {
              ...params,
              source: 'youtube-comments',
              maxComments: limits.commentsPerItem,
              flowId,
              dbJobId,
            },
          },
        ],
      },
    ],
  });

  return { flowId, dbJobId, flow };
}
```

4. `packages/core/src/queue/workers.ts` -- Worker 골격 (per D-04, D-06):

```typescript
import { Worker, Job } from 'bullmq';
import { redisConnection } from './connection';

// Worker 프로세스 -- Next.js와 별도 프로세스로 실행 (RESEARCH Pitfall 4 참고)
export function createCollectorWorker(processJob: (job: Job) => Promise<any>) {
  return new Worker('collectors', processJob, {
    connection: redisConnection,
    concurrency: 2,
    limiter: {
      max: 5,
      duration: 10000, // 10초당 최대 5개 작업 (네이버 rate limit 대응)
    },
  });
}

export function createPipelineWorker(processJob: (job: Job) => Promise<any>) {
  return new Worker('pipeline', processJob, {
    connection: redisConnection,
    concurrency: 1,
  });
}
```

5. `packages/core/src/queue/index.ts`:

```typescript
export { redisConnection } from './connection';
export { triggerCollection } from './flows';
export { createCollectorWorker, createPipelineWorker } from './workers';
```

6. `packages/core/src/index.ts` 업데이트 -- queue와 types 모듈 re-export:

```typescript
export { db } from './db';
export type { Database } from './db';
export * from './db/schema';
export {
  triggerCollection,
  createCollectorWorker,
  createPipelineWorker,
  redisConnection,
} from './queue';
export * from './types';
```

7. `packages/collectors/src/adapters/base.ts` -- Collector 인터페이스 (per COLL-09):

```typescript
import { z } from 'zod';

export const CollectionOptionsSchema = z.object({
  keyword: z.string().min(1),
  startDate: z.string().datetime(),
  endDate: z.string().datetime(),
  maxItems: z.number().optional(),
  maxComments: z.number().optional(),
});
export type CollectionOptions = z.infer<typeof CollectionOptionsSchema>;

export interface CollectionResult<T> {
  source: string;
  items: T[];
  totalCollected: number;
  errors: Error[];
  metadata: {
    startedAt: Date;
    completedAt: Date;
    pagesFetched: number;
  };
}

// 모든 수집기가 구현하는 인터페이스
export interface Collector<T = unknown> {
  readonly source: string;
  collect(options: CollectionOptions): AsyncGenerator<T[], void, unknown>;
  // AsyncGenerator로 청크 단위 yield -- 메모리 효율 + 진행률 추적
}
```

8. `packages/collectors/src/adapters/registry.ts` -- 수집기 레지스트리:

```typescript
import type { Collector } from './base';

const collectors = new Map<string, Collector>();

export function registerCollector(collector: Collector): void {
  collectors.set(collector.source, collector);
}

export function getCollector(source: string): Collector | undefined {
  return collectors.get(source);
}

export function getAllCollectors(): Collector[] {
  return Array.from(collectors.values());
}
```

9. `packages/collectors/src/adapters/index.ts`:

```typescript
export {
  CollectionOptionsSchema,
  type CollectionOptions,
  type CollectionResult,
  type Collector,
} from './base';
export { registerCollector, getCollector, getAllCollectors } from './registry';
```

10. `packages/collectors/src/index.ts` 업데이트:

```typescript
export * from './adapters';
```

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/queue/flows.ts에 `export async function triggerCollection(params: CollectionTrigger, dbJobId: number)` 시그니처 존재
    - packages/core/src/queue/flows.ts에 모든 flow children data 객체에 `dbJobId` 필드 포함
    - packages/core/src/queue/flows.ts에 `'collect-naver-articles'`와 `'collect-youtube-videos'` 작업명 존재
    - packages/core/src/queue/flows.ts에 반환값이 `{ flowId, dbJobId, flow }` 형태
    - packages/core/src/queue/connection.ts에 `maxRetriesPerRequest: null` 설정 존재
    - packages/core/src/queue/workers.ts에 `createCollectorWorker`와 `createPipelineWorker` export 존재
    - packages/core/src/types/index.ts에 `CollectionTriggerSchema` 존재
    - packages/collectors/src/adapters/base.ts에 `export interface Collector` 존재
    - packages/collectors/src/adapters/base.ts에 `AsyncGenerator` 반환 타입 존재
    - packages/collectors/src/adapters/registry.ts에 `registerCollector`, `getCollector` 존재
    - packages/core/src/index.ts에서 `triggerCollection`과 `CollectionTriggerSchema` re-export
    - `pnpm -r build` exit code 0
  </acceptance_criteria>
  <done>BullMQ 3단계 파이프라인 FlowProducer + Collector Adapter 인터페이스 + 레지스트리 완성</done>
</task>

<task type="auto">
  <name>Task 2: AI Gateway 골격 + 단위 테스트 스캐폴드</name>
  <files>
    packages/ai-gateway/src/gateway.ts,
    packages/ai-gateway/src/index.ts,
    packages/core/tests/queue.test.ts,
    packages/collectors/tests/adapter.test.ts
  </files>
  <read_first>
    packages/ai-gateway/package.json,
    packages/ai-gateway/src/index.ts,
    packages/collectors/src/adapters/base.ts,
    packages/core/src/queue/flows.ts
  </read_first>
  <action>
1. `packages/ai-gateway/src/gateway.ts` -- AI SDK v6 게이트웨이 (per FOUND-04):
Phase 1에서는 골격만 구현. Phase 2에서 8개 분석 모듈 프롬프트와 연결.

```typescript
import { generateText, generateObject } from 'ai';
import { anthropic } from '@ai-sdk/anthropic';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

export type AIProvider = 'anthropic' | 'openai';

export interface AIGatewayOptions {
  provider?: AIProvider;
  model?: string;
  maxTokens?: number;
}

const DEFAULT_MODELS: Record<AIProvider, string> = {
  anthropic: 'claude-sonnet-4-20250514',
  openai: 'gpt-4o-mini',
};

function getModel(provider: AIProvider, model?: string) {
  const modelName = model ?? DEFAULT_MODELS[provider];
  switch (provider) {
    case 'anthropic':
      return anthropic(modelName);
    case 'openai':
      return openai(modelName);
  }
}

// 텍스트 분석 (Phase 2에서 프롬프트와 연결)
export async function analyzeText(prompt: string, options: AIGatewayOptions = {}) {
  const provider = options.provider ?? 'anthropic';
  const result = await generateText({
    model: getModel(provider, options.model),
    prompt,
    maxTokens: options.maxTokens ?? 4096,
  });
  return result;
}

// 구조화 분석 (Phase 2에서 Zod 스키마와 연결)
export async function analyzeStructured<T>(
  prompt: string,
  schema: z.ZodType<T>,
  options: AIGatewayOptions = {},
) {
  const provider = options.provider ?? 'anthropic';
  const result = await generateObject({
    model: getModel(provider, options.model),
    prompt,
    schema,
    maxTokens: options.maxTokens ?? 4096,
  });
  return result;
}
```

NOTE: AI SDK v6의 최신 API를 확인. RESEARCH.md에서 `Output.object()` 패턴이 언급되었으나 실제 최신 API는 `generateObject`를 사용. 실행 시 AI SDK v6 공식 문서(https://sdk.vercel.ai/docs)를 확인하여 정확한 API 사용.

2. `packages/ai-gateway/src/index.ts` 업데이트:

```typescript
export { analyzeText, analyzeStructured, type AIProvider, type AIGatewayOptions } from './gateway';
```

3. `packages/core/tests/queue.test.ts` -- BullMQ 테스트 스캐폴드:

```typescript
import { describe, it, expect } from 'vitest';

describe('Queue - triggerCollection', () => {
  it('should export triggerCollection function', async () => {
    const { triggerCollection } = await import('../src/queue/flows');
    expect(typeof triggerCollection).toBe('function');
  });

  it('should export CollectionTriggerSchema', async () => {
    const { CollectionTriggerSchema } = await import('../src/types');
    expect(CollectionTriggerSchema).toBeDefined();

    // 유효한 입력 파싱
    const valid = CollectionTriggerSchema.parse({
      keyword: '윤석열',
      startDate: '2026-03-17T00:00:00.000Z',
      endDate: '2026-03-24T00:00:00.000Z',
    });
    expect(valid.keyword).toBe('윤석열');
  });

  it('should reject invalid CollectionTrigger', async () => {
    const { CollectionTriggerSchema } = await import('../src/types');
    expect(() => CollectionTriggerSchema.parse({ keyword: '' })).toThrow();
  });
});
```

4. `packages/collectors/tests/adapter.test.ts` -- Adapter 인터페이스 테스트:

```typescript
import { describe, it, expect } from 'vitest';
import type { Collector, CollectionOptions } from '../src/adapters/base';
import { registerCollector, getCollector, getAllCollectors } from '../src/adapters/registry';

// 테스트용 Mock 수집기
class MockCollector implements Collector<{ id: string; text: string }> {
  readonly source = 'mock-source';

  async *collect(options: CollectionOptions) {
    yield [{ id: '1', text: `result for ${options.keyword}` }];
  }
}

describe('Collector Adapter Interface', () => {
  it('should register and retrieve a collector', () => {
    const mock = new MockCollector();
    registerCollector(mock);
    const retrieved = getCollector('mock-source');
    expect(retrieved).toBeDefined();
    expect(retrieved?.source).toBe('mock-source');
  });

  it('should collect data via AsyncGenerator', async () => {
    const mock = new MockCollector();
    const options: CollectionOptions = {
      keyword: '테스트',
      startDate: '2026-03-17T00:00:00.000Z',
      endDate: '2026-03-24T00:00:00.000Z',
    };

    const chunks: any[] = [];
    for await (const chunk of mock.collect(options)) {
      chunks.push(...chunk);
    }
    expect(chunks).toHaveLength(1);
    expect(chunks[0].text).toContain('테스트');
  });

  it('should return undefined for unregistered collector', () => {
    const result = getCollector('nonexistent');
    expect(result).toBeUndefined();
  });
});
```

  </action>
  <verify>
    <automated>cd /home/gon/projects/ai/ai-signalcraft && pnpm --filter @ai-signalcraft/core test && pnpm --filter @ai-signalcraft/collectors test</automated>
  </verify>
  <acceptance_criteria>
    - packages/ai-gateway/src/gateway.ts에 `export async function analyzeText` 존재
    - packages/ai-gateway/src/gateway.ts에 `export async function analyzeStructured` 존재
    - packages/ai-gateway/src/gateway.ts에 `anthropic`과 `openai` import 존재
    - packages/ai-gateway/src/index.ts에서 `analyzeText`, `analyzeStructured` re-export
    - packages/core/tests/queue.test.ts에 `describe('Queue - triggerCollection'` 존재
    - packages/collectors/tests/adapter.test.ts에 `describe('Collector Adapter Interface'` 존재
    - packages/collectors/tests/adapter.test.ts에 `class MockCollector implements Collector` 존재
    - `pnpm --filter @ai-signalcraft/core test` exit code 0
    - `pnpm --filter @ai-signalcraft/collectors test` exit code 0
  </acceptance_criteria>
  <done>AI Gateway 골격(analyzeText + analyzeStructured)과 queue/adapter 단위 테스트가 통과</done>
</task>

</tasks>

<verification>
- `pnpm -r build` 성공 (전체 빌드)
- `pnpm --filter @ai-signalcraft/core test` 통과 (queue 테스트)
- `pnpm --filter @ai-signalcraft/collectors test` 통과 (adapter 테스트)
- triggerCollection 함수가 (params, dbJobId) 시그니처로 DB 정수 ID를 받아 모든 flow children에 전달
- Collector 인터페이스가 AsyncGenerator 패턴 사용
</verification>

<success_criteria>

- BullMQ FlowProducer가 collect -> normalize -> persist 3단계로 구성 (per D-05)
- triggerCollection이 dbJobId: number를 두 번째 인자로 받아 모든 flow job data에 전달
- Collector 인터페이스에 source, collect(AsyncGenerator) 메서드 존재 (per COLL-09)
- AI Gateway가 anthropic/openai 프로바이더를 단일 API로 라우팅 (per FOUND-04)
- 모든 테스트 통과
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-data-collection/01-02-SUMMARY.md`
</output>
